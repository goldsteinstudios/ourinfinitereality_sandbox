# Site v7.7 redraft — ruling document r1

**Status: DRAFT FOR RULING. Nothing here ships until Will rules on it item by item.**

Machine-drafted (Claude, Fable 5) strictly from `rsm/canonical/chains/v7.7/` + `CLAUDE.md`.
Per the standing division of labor: ontology, referents, and readings are Will's. Every item
below is a *proposal*; the characteristic machine failure mode — find a matching set, declare
an identity — applies to this document itself. Rejected items go to
`rsm/audit/divergence_ledger_r1.md` per its protocol.

**Rules this document obeys (and every proposal should be checked against):**
- `::` = imperfect structural correspondence; forbids substitution, identity, congruence.
  Prose form: "to me, that looks just like…" — never "X is Y."
- Status tags are canon's, never upgraded: [derived] / [candidate] / [falsifiable] / [open].
- No occupancy vocabulary in skeleton register. The two centers are typed **met vs unmet**.
- The DDJ is the **subject of translation**, not a witness; present-day convergence carries
  zero evidential weight (agreement with one's own seed).
- Not sealed. L1 is the open head of the audit.

Rulings per item: **APPROVE** / **AMEND** (with your text) / **REJECT** (→ divergence ledger).

---

## Batch B1 — framework pages

### B1.1 · `src/pages/index.astro:21` — THE THESIS SENTENCE

**Current:**
> Stable things persist by circulating around centers that can't be occupied.

**Problem:** Occupancy typing, struck as ill-typed (`structural_v7.7_r3.md:3` — no motion,
duration, or occupancy in the skeleton; `CLAUDE.md:64-78` — met vs unmet).

**Proposed (two candidates — Will picks or writes his own):**

- **(A) plain register:**
  > Stable things persist by circulating around a center they never meet.
- **(B) with the engine:**
  > Stable things persist by circulating around a center that is never met — and they are
  > generated at a crossing that is met but can never become a frame of its own.

**Canon:** Oₙ = the unmet crossing (arms approach asymptotically, never meet); Pₙ = the
realized crossing, generative, cannot be constituted as a frame — "this impossibility is the
entire engine" (`structural_v7.7_r3.md:70-74, :90`). **Tag carried:** the cross-domain
recurrence claim is [candidate]; the met/unmet typing is the structural chain's own.

### B1.2 · `index.astro:23` — the thesis gloss

**Current:** "The center defines everything — but you can't stand on it. Try to occupy it and
you either destroy the structure or discover there's nothing there to occupy. So everything
orbits instead. The circling *is* the stability."

**Proposed:**
> The center defines everything — but it is never met. Every approach remains an approach;
> the meeting never arrives. And the crossing that *is* met — the generative one — can never
> become a frame of its own. One impossibility, two discharges: rotation and recursion. The
> circling *is* the stability.

**Canon:** `structural_v7.7_r3.md:70-74, :90`; `CLAUDE.md:64-78`.
**Open gloss for Will:** the public-register wording for `Gₙ ∩ Bₙ` — I have deliberately NOT
glossed Gₙ/Bₙ as "gradient field"/"boundary surface" (they are loci; the merge is the seating
error, `CLAUDE.md:81-85`). If the homepage needs a plain gloss for "the crossing that is met,"
that gloss is a referent choice → yours.

### B1.3 · `index.astro:42` — black hole sentence

**Current:** "a point that the equations say exists but that can't actually be occupied by
anything, including the equations themselves."

**Proposed:** "a point the equations name but that nothing ever meets — every worldline's
approach remains an approach."
**Note:** physics register — "maps to, never is" (`just_physics_v7.7_r1.md:3`). The whole
physics section of the homepage is [P2] rendering, zero evidential weight; consider one
sentence saying so. **Tag:** [P2 correspondence].

### B1.4 · `index.astro:30-32` — convergence-as-evidence framing

**Current:** "those threads keep converging on the same shape, from completely different
directions… Watching domains that have no business agreeing with each other arrive at the
same geometry."

**Problem:** Convergence priced as evidence; canon prices convergences near zero
(`CLAUDE.md:134-136`) and confines evidential weight to preregistered predictions.

**Proposed:**
> Pull on any thread and it leads somewhere — to mathematics, to physics, to biology, to
> philosophy. Whether the threads describe one shape or several is exactly the question the
> framework exists to make checkable. The claim is not "look how much agrees" — agreement is
> cheap. The claim is that the pattern can be stated precisely enough to fail.

### B1.5 · `index.astro:85-90` — the vocabulary card (retired O/G/P)

**Current:** Origin (O) "the center that can't be occupied" · Gradient (G) "the field that
extends from it" · Periphery (P) "the surface where measurement happens" · P→O "any periphery
can become origin for a new frame" · "Four terms. Everything else is elaboration."

**Problem:** Retired vocabulary; `P` has the WRONG REFERENT (in v7.7 `Pₙ :: Gₙ ∩ Bₙ :: 玄`,
the realized crossing — not a periphery); P→O promotion is superseded by the anchoring line
[candidate] and by `Oₙ = P₍ₙ₋₁₎` as one locus under two frame-readings
(`structural_v7.7_r3.md §5`; `CLAUDE.md:66-78`).

**Proposed replacement card:**
> - **Oₙ** :: the origin — the unmet crossing. Arms approach; the meeting never arrives. The
>   frame's local face of P₀.
> - **Pₙ** :: 玄 — the realized crossing. Met, generative, and never constitutable as a frame
>   of its own.
> - **P₀** :: 玄牝 — the generative paradox.
> - **Oₙ = P₍ₙ₋₁₎** — one locus, two frame-readings: met from without, unmet from within.
>   This is where recursion lives. *(The claim that every finer frame is parent-seeded at P —
>   the anchoring line — is [candidate], the discharge's one open step.)*

**Register question for Will:** does the homepage carry subscripts and 玄/玄牝 at all, or a
plain-language paraphrase with the technical card living on the rosetta-stone page only?

### B1.6 · `index.astro:102` — "the center holds by remaining unoccupied"

**Proposed:** "…function emerges from emptiness, persistence comes from circulation, the
center holds by never being met."

### B1.7 · `index.astro:103` — the DDJ paragraph (witness framing)

**Current:** "I didn't go looking for ancient texts to validate a theory. I followed the
implications… and kept arriving at a pattern that someone else had already documented — in
extraordinary precision and detail — twenty-five centuries before."

**Problem:** The strongest witness-typing on the site. v7.7: the DDJ is generatively
upstream; present-day convergence carries no evidential weight (`just_ddj_v7.7_r3.md:5`).

**Proposed:**
> I should be honest about the direction of travel: I built this framework *through* the Dao
> De Jing — the text is where the pattern first spoke to me. That means the framework
> agreeing with the text proves nothing; agreement with your own seed is expected. So the DDJ
> here is not a witness for the theory. It is the subject of translation — and the test of
> the translation is compression: how much of the text snaps into place, and what it refuses.

**Tag:** the compression-quality claim grounds the *instrument*, not the thesis (canon's own
distinction).

### B1.8 · `docs/rsm/rosetta-stone.astro` — full reteach (page-scale)

**Problem:** The page teaches retired O/G/P (wrong `P` referent), occupancy in the definition
of O (line 34), 名 correspondences as settled (lines 74, 132: 無名/有名 mapped to O/P — v7.7
holds 名 obtainedness [open], `CLAUDE.md:104-106`), identity `=` across registers (lines
150-154: `0 = the origin…`, `i = the 90° rotation…`), "Same pattern. Twenty-five centuries
earlier." (line 136 — witness), and instructs readers to find "the hollow/unoccupiable
center" as a method (line 228).

**Proposed structure (I draft full copy after you rule on the skeleton):**
1. The two centers, met vs unmet — Oₙ / Pₙ / P₀ card (as B1.5), 玄 :: Pₙ, 玄牝 :: P₀ [R].
2. The engine — one impossibility (Pₙ never constitutable as a frame), two discharges
   (rotation, recursion). 弗居 :: the seat's non-constitutability, flagged as the strongest
   [G] match (`just_ddj_v7.7_r3.md:77`).
3. The `::` discipline itself, taught to the reader — what the site means when it refuses
   to say "is." Status tags [derived]/[candidate]/[falsifiable]/[open] defined.
4. Notation table with `::` (never `=`): i² = −1 as the two-paths pair [derived]
   (`just_math_v7.7_r4.md:88-94`); the slice identity — one law Q(z)=1ₙ, two real slices,
   "the gradient is the orbit" (`just_math_v7.7_r4.md:57`); the two different quantities
   both written 1ₙ (`CLAUDE.md:83-85`).
5. "The Test" (line 228) recast: not "find the unoccupiable center" but "state the claim
   precisely enough that it could fail; find which crossing is met and which is not."
6. [G]/[R] stratum honesty note: the deepest correspondences are the least anciently
   witnessed (`just_ddj_v7.7_r3.md:102`).

### B1.9 · `claims.astro:19` — the Core Claim box

**Current:** "…stable structures organize around centers they can't occupy, along gradients
that extend to boundaries, where any point can become a new center — recurs across physics,
biology, mathematics, and the Dao De Jing, precisely enough to check."

**Problem:** Seating error + retired P→O + DDJ listed as a domain the pattern "recurs in,
precisely enough to check" (= evidence).

**Proposed:**
> **RSM claims one thing:** a single structural pattern — stable structures persist by
> circulating around a crossing their frame never meets, and are generated at a crossing
> that is met but can never become a frame — recurs across physics, biology, and
> mathematics, precisely enough to check. [candidate]
>
> The Dao De Jing is deliberately *not* on that list. The framework grew out of the text, so
> their agreement is not evidence of anything. The DDJ is the subject of translation here.

### B1.10 · `claims.astro:44-84` — the falsifiable-predictions table

**Problem:** Current rows ("Atomic orbitals map to O₁", falsified by "Orbitals don't exhibit
unoccupiable-center geometry") state the criterion in struck vocabulary and don't match the
v7.7 physics chain, whose §9 is the ONLY evidential section and requires preregistration
(`just_physics_v7.7_r1.md:67-75`).

**Proposed:** replace the table with the chain's own list, at its own tags:
1. Power-law nesting between levels — operationalized via RG critical exponents and/or the
   holographic area law. *The framework must derive the exponent-form before any check
   counts; currently a target.* [falsifiable — target]
2. Structural recession vs ΛCDM — distinguishing signature *unbuilt*. [falsifiable — unbuilt]
3. Unitarity holds — objective-collapse theories violate it by design and are empirically
   live; their confirmation strikes the floor's dynamical face. *A genuine kill-condition.*
   [falsifiable]
4. Items carried from v7.5 pending re-verification are NOT restated (canon declines to
   reproduce them from memory; so does the site).
Plus the two displayed open wounds if we want full honesty on this page: the Bekenstein sort
(area vs volume scaling — a live-contradiction risk with holography) and the two arrows.
[open] **Will rules whether the wounds belong on the public page.**

### B1.11 · `claims.astro:108-109` — "Physics: No claim. RSM doesn't derive physics."

**Problem:** Now under-claims: v7.7 has a Just-Physics rendering.
**Proposed:** "Physics: rendering only — every entry is a correspondence ('maps to,' never
'is') with zero evidential weight, except the preregistered predictions above. RSM does not
derive ℏ, the ½, or Fourier conjugacy." (`just_physics_v7.7_r1.md:3, :27`)

### B1.12 · `confidence.astro` — re-derive the page from status tags

**Problem:** The page's tiers (was Locked/Strong/…; Phase A merged Locked→Strong as a
stopgap) don't map to canon. Several graded claims are wrong under v7.7.

**Proposed:** rebuild sections as [derived] / [candidate] / [falsifiable] / [open], with:
- **[derived]** (each carrying its stated conditionals — "given P1, L1, T"): the σ-invariance
  theorem; i² = −1 (two-paths pair); the slice identity and the metric derived through it
  *with the Finsler caveat stated* (quadraticity closed only among polynomials; the
  generation principle named as the residue, `just_math_v7.7_r4.md:75`); the standoff — "not
  floor-forbidden but excluded-by-constitution; there is no *through*, only *around*";
  index-relationality (the nesting is a tree, not a ladder).
- **[candidate]**: L1 (THE open head — say so); the anchoring line; the complementarity floor
  σ_a·σ_b ≥ 1ₙ/2 (derived shape, no discriminating test); three dimensions *necessary*,
  uniqueness [open] (two rival derivations unadjudicated; S² embeds in ℝ⁴) — this replaces
  the current "minimal and sufficient" row, which overstates.
- **Rows to strike or re-type** (Will rules each): "The center can't be occupied" → the
  met/unmet typing; "Rotation is necessary — the measurement crisis" → v7.5's argument was
  circular (used a radius before the metric existed); v7.7 grounds it F-free via L1 —
  restate or drop; "Euler's identity encodes Contrast/Rotation/Closure" → the C/R/C triad
  is v7.5-era; if an Euler reading stays, it stays as a reading with `::`;
  "Trees instantiate O₁ structure" → NOTE: the pith-toroid was RETRACTED in v7.7 — the living
  generative layer is the cambium, not the pith (`just_physics_v7.7_r1.md:79`). The homepage
  biology section (index.astro:52-54) already centers the cambium, which happens to be the
  post-retraction reading — but the hollow-pith framing ("organized around that emptiness")
  needs Will's ruling.
- Also add the concentration-risk line if leanness is ever advertised: "F, Q, and FU all
  dissolve through the same core… if R8 or L1 fails audit, three postulates re-inflate.
  Lean ≠ robust." (`just_math_v7.7_r4.md:82`)

### B1.13 · `methodology.astro:87-91` — the 5-tier scheme

**Problem:** "Tier 1 (Locked)… Tier 2 (Postulate-Dependent)…" — retired machinery;
"Postulate-Dependent" names a category that no longer exists.
**Proposed:** replace with the four status tags, defined as in `CLAUDE.md:38-43`, plus the
[G]/[R] stratum tags for DDJ correspondences. Line 54's "What postulates?" → "What premises?
(The working set is deliberately small: the Conditional, the Bridge, Definition T, the
seating commitment, L1 — and L1 is the open head.)"

### B1.14 · `faq.astro:57` — the postulate answer

**Current:** "RSM shows that if you accept certain postulates (continuity, frame invariance,
etc.), constants like e and pi emerge as structurally necessary."

**Proposed:**
> RSM works from a deliberately small premise set: the Conditional (a stipulated definition,
> never asserted as true), the Bridge (what is not distinguishable does not obtain), and one
> lemma-candidate — L1, "no unframed distinction," the open head of the audit. Earlier
> versions carried postulates (F, R, Q, frame-universality); the current chain dissolved or
> struck them. The leanness is real but concentrated: if the slice identity or L1 fails
> audit, three postulates re-inflate.

### B1.15 · `faq.astro:90-92` — the present-moment answer

**Current:** "The present moment, like spatial O₁, is a structural position that cannot be
'occupied' in the measurement sense — you're always on one side or the other."
**Proposed:** "The present moment, like the origin, is never met — you're always on one side
of it or the other." (Keeps the correct half; drops the occupancy ratification.)

### B1.16 · `about.astro:31-34, 54`

- :31 thesis restatement → same form as ruled in B1.1.
- :32 "The pattern appears **independently** across mathematics, physics, biology, and
  ancient philosophy" → drop "independently" and pull "ancient philosophy" out of the
  evidence list: "The pattern can be stated in mathematics, physics, and biology precisely
  enough to check. The Dao De Jing is where I learned to see it — which is exactly why it
  doesn't count as evidence."
- :54 "The core insight—Contrast, Rotation, Closure—is stable." → "This is alive and
  changing. It is not sealed: the framework's one named lemma-candidate (L1) is the open
  head of the audit." (C/R/C triad is v7.5-era; whether any triad survives → Will.)

### B1.17 · `hypotheses.astro:45-47` — the θ(p) formula (type merge)

**Problem:** `∇G = chemical gradient, ∇B = boundary surface normal` re-types the loci Gₙ/Bₙ
as physical vector fields — the exact four-type merge that produced the seating error
(`CLAUDE.md:81-85`). The Primary Hypothesis is built on the merge.
**Proposed action (not proposed copy):** this is a measurable-protocol design, i.e. ontology
+ arithmetic. Machine should not redraft it. Options: (a) mark the page "pre-v7.7 — under
re-derivation" pending your rework; (b) unpublish until re-derived; (c) you re-derive the
protocol and I typeset. **Will rules.** (Line 116's "physically unoccupied" falls with it.)

---

## Batch B2 — objects (`src/content/objects/`)

### B2.1 · `origin.mdx` — the framework page of the catalog (worst content file)

Key strikes and replacements (full redraft follows the same rulings as B1.1/B1.5):
- :19 "A center that cannot be occupied" → "A center that is never met."
- :24 "This is **O→G→P**: Origin, Gradient, Periphery." → replace with the two-centers card
  (B1.5). The O→G→P triad and the "Periphery" referent do not survive.
- :33 "**P₀ (Paradox)**: The center that must exist but cannot be occupied" → **highest-
  precision error on the site**: correct symbol, struck predicate. → "**P₀** :: 玄牝 :: the
  generative paradox."
- :34-36 O₁/G₁/P₁ as "first distinction / radiating field / observable boundary" → strike
  (field/boundary readings are the type merge; also O₁ uniqueness is [open], canon open
  item #6 — the page must not assert it).
- :38 "This isn't philosophy. It's the geometry of persistence." → carries no tag; either
  tag it [candidate] or cut.
- :42-46 "The Origin itself is hollow… Fill the void, and the system stops functioning." →
  met/unmet retype; "fill the void" mechanism goes.

### B2.2 · `wheel.mdx` — and the 弗居 opportunity

- :30-33 O→G→P cycle card → same replacement as B2.1.
- :52 "This is the first teaching: 有 orbits 無, creating 用." → re-type under the slice
  identity: 有無相生 names the law itself, not a slice (`just_ddj_v7.7_r3.md:41`) — and the
  claim takes `::`, not "is."
- **Add** (opportunity): the wheel's hub is the natural home for 弗居 :: the seat's
  non-constitutability — "completes yet structurally-cannot-dwell" — the strongest [G]
  match in the chain (`just_ddj_v7.7_r3.md:77`). Draft on approval.

### B2.3 · `atom.mdx`

- :26 "never occupying the center" → "never meeting the center."
- :30 "The atom **demonstrates** circulation…" → "To me, the atom looks just like…" (the
  object pages' standing register should be the prose form of `::`).
- :35-37 O/G/P card → strike per B2.1. Physics entries carry "maps to, never is."

### B2.4 · `tree.mdx` — plus the pith retraction

- :30 "the center need not be occupied for the system to function" → met/unmet retype.
- **Canon correction that must land here:** the pith-toroid reading was RETRACTED — the
  living generative layer is the cambium, not the pith (`just_physics_v7.7_r1.md:79`).
  The page (and index.astro:52-54) should carry the cambium reading; whether the hollow-pith
  image survives as *image* (not claim) → Will.

### B2.5 · `valley.mdx` — the 玄牝 :: P₀ gap (opportunity, not a bug)

The one place the site touches 玄牝 (谷神不死，是謂玄牝) and it doesn't connect to P₀.
**Proposed add:** "玄牝 :: P₀ — the generative paradox. [R]" with the stratum honesty note:
Chapter 6 is received-only; the deepest correspondences are the least anciently witnessed.

### B2.6 · `room.mdx`, `bellows.mdx` — light touch

- room.mdx:40 "You can **occupy** (the interior-void)" — literal room; audit says harmless.
  **Propose: leave.** (Occupancy of a room is not occupancy of a center.)
- bellows.mdx:45 "The cosmos itself breathes through the void it preserves at its center." —
  cosmological overreach that `claims.astro` explicitly disclaims. **Propose: cut sentence.**

---

## Batch B3 — remaining blemishes (single-line rulings)

| # | Location | Current | Proposed |
|---|----------|---------|----------|
| B3.1 | `essays/standing-wave-pattern.md:207` | "Something at the center that can't be occupied" | "A center that is never met" |
| B3.2 | `essays/the-gap.md:188` | "The engine room of reality isn't occupied. That's what makes it run." | "The engine room of reality is never met. That's what makes it run." |
| B3.3 | `essays/the-gap.md:111` | "If you could occupy the center, you'd stop there." | "If the center could be met, you'd stop there." |
| B3.4 | `essays/topology-of-being-alive.md:73` | "The generative center doesn't have to be occupied. It can't be occupied." | "The generative center is met — and it can never become a frame of its own. That impossibility, not emptiness, is what makes it generative." *(NOTE: this essay has the two centers swapped; this line re-types it to the Pₙ reading. Will confirm the referent.)* |
| B3.5 | `translations/chapter-01.md:12-14` | 名 "the cut that creates both poles" at strong | Downgrade + hold open: "名's obtainedness is held open in the current framework; the live [candidate] reading is the 明/名 square-vs-root split (明 :: the quadratic measure, 名 :: its linear read-off)." Level: plausible. |
| B3.6 | `translations/chapter-01.md:96` | "玄 \| paradoxical origin / generative center" | "玄 :: Pₙ — the realized crossing (not the origin; the origin Oₙ is the unmet one)" — table gloss: "the generative crossing" |
| B3.7 | `translations/chapter-42.md:99-104, 131-134` | "3 (sphere): Sufficient / 4+: Redundant" | "3: necessary [derived-in-part]; uniqueness open — two rival derivations unadjudicated; S² embeds in ℝ⁴" — and cut the "✓ Dimensional sufficiency" checkmark line |
| B3.8 | `translations/chapter-25.md:95-104` | O₁/G/P/+1 mapped onto 大/逝/遠/反 | Re-key the table to current vocabulary or drop the RSM column; the 反-as-return row can carry `::` honestly. **Will rules which.** |
| B3.9 | `translations/chapter-11.md` | (gap) | Add 弗居 note per B2.2 — this is the Ch.11 file and 弗居's strongest ground. |
| B3.10 | `pages/guodian.astro:225` | "The geometry was always in the bamboo." | "The bamboo is where the translation begins." |
| B3.11 | `pages/ddj.astro:74` | "The geometry was always in the characters." | "The translation begins in the characters." |
| B3.12 | `pages/ddj.astro:16` | "Reading the DDJ as technical notation, not mystical poetry." | "Reading the DDJ as the subject of a structural translation, not as mystical poetry." |
| B3.13 | `pages/text/what-is-ddj.astro:19` | "treats the text as technical documentation and asks: what were they actually observing?" | "treats the text as the subject of a structural translation and asks: what does the language itself constrain?" |
| B3.14 | `essays/grammar-of-existence.md`, `essays/_between-e-and-phi.md` | (unpublished in Phase A) | Stay unpublished until rewritten against v7.7 or retired permanently. **Will rules, no urgency.** |

---

## Process

1. Will rules per item (annotate this file, or in chat by batch — B1 first).
2. Approved items applied verbatim; AMENDed items applied with Will's text.
3. REJECTed proposals logged in `rsm/audit/divergence_ledger_r1.md`.
4. After each batch: build → grep dist for `occup|unoccupiable|Periphery|P→O` (expected
   survivors: room.mdx's literal room if B2.6 approved) → publish.
5. `python3 rsm/audit/checks.py` before any batch ships (guard: no geometry drifts into copy).
