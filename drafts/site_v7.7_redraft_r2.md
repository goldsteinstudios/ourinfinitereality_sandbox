# Site v7.7 redraft — ruling document r2

**Status: WALKTHROUGH IN PROGRESS. Will rules every QUEUE item individually; nothing ships
unruled.** Items keep their r1 numbers (both machine reviews are keyed to them).

r1 → r2 delta: two machine reviews (Claude, Gemini) were run against r1. Five catches
verified against canon and integrated below (B1.9, B1.7, the invented `[derived-in-part]`
tag, the metric's tag, B1.14, B3.9's 弗居 attribution + the passive-voice micro-catch).
One catch (chained `::`) was **rejected by Will's ruling** — canon itself chains
(`CLAUDE.md:66`) — and goes to the divergence ledger (draft entry at the bottom). The
reviews' `M5/M9/M10` citations are **unverifiable in the repo** (canon's provenance tags
are P1/P2/P3; the "plucked out" ban is real — walkthrough ledger :239 GUARD). Two review
divergences were composed (B3.4 fix+banner; B1.17 unpublish+stub).

**Standing public gloss (pending per-item ruling):** "the generative crossing" for Pₙ in
plain register. Gemini's alternative "the realized seat" is set aside as seating-adjacent.

**Rulings already given by Will (excluded from the queue):**
- **B1.1 = RULED (A):** thesis is "Stable things persist by circulating around a center
  that is never met." Passive form throughout the site.
- **B1.5 chain form = RULED:** the card keeps canon's chain verbatim
  (`P₀ :: 玄牝 :: the generative paradox`). The *placement/register* sub-question is
  still QUEUE.
- **B2.4 = RULED (adopted in full):** pith :: Oₙ · cambium :: Gₙ · rays :: Bₙ ·
  branch node :: Pₙ, shipped on tree.mdx with the pith-toroid retraction noted; ruling
  recorded in the audit trail (draft addendum at the bottom).

Document rules unchanged from r1: `::` never upgraded to identity; status tags are canon's
four ([derived]/[candidate]/[falsifiable]/[open]) — where site translation frontmatter
needs a value it uses the SITE TIER enum (strong/plausible/speculative) and is labeled as
such; no occupancy vocabulary; DDJ = subject of translation; not sealed.

---

## Batch B1 — framework pages

### B1.1 · `index.astro:21` — thesis — **RULED (A)**

> Stable things persist by circulating around a center that is never met.

### B1.2 · `index.astro:23` — thesis gloss — QUEUE

**Current:** "The center defines everything — but you can't stand on it. Try to occupy it
and you either destroy the structure or discover there's nothing there to occupy. So
everything orbits instead. The circling *is* the stability."

**Proposed (carries the engine, per the B1.1 ruling):**
> The center defines everything — but it is never met. Every approach remains an approach;
> the meeting never arrives. And the generative crossing — the one that *is* met — can
> never become a frame of its own. One impossibility, two discharges: rotation and
> recursion. The circling *is* the stability.

**Canon:** `structural_v7.7_r3.md:70-74, :90`; `CLAUDE.md:64-78`. Public gloss for
`Gₙ ∩ Bₙ` = "the generative crossing" (standing gloss, above).

### B1.3 · `index.astro:42` — black hole sentence — QUEUE

**Proposed:** "a point the equations name but that nothing ever meets — every approach
remains an approach." **Tag:** [P2 correspondence]; physics register "maps to, never is."

### B1.4 · `index.astro:30-32` — convergence framing — QUEUE

**Proposed:**
> Pull on any thread and it leads somewhere — to mathematics, to physics, to biology, to
> philosophy. Whether the threads describe one shape or several is exactly the question the
> framework exists to make checkable. The claim is not "look how much agrees" — agreement
> is cheap. The claim is that the pattern can be stated precisely enough to fail.

### B1.5 · `index.astro:85-90` — vocabulary card — chain form RULED; placement QUEUE

**Card text (canon-verbatim chains, per ruling):**
> - **Oₙ** :: the origin — the unmet crossing. Arms approach; the meeting never arrives.
>   The frame's local face of P₀.
> - **Pₙ** :: 玄 — the generative crossing. Met, generative, and never constitutable as a
>   frame of its own.
> - **P₀** :: 玄牝 :: the generative paradox.
> - **Oₙ = P₍ₙ₋₁₎** — one locus, two frame-readings: met from without, unmet from within.
>   *(The anchoring line — every finer frame parent-seeded at P — is [candidate], the
>   discharge's one open step.)*

**Placement QUEUE (reviewers converged, Will rules):** homepage carries a plain-language
paraphrase only; this technical card moves to rosetta-stone. Proposed homepage paraphrase:
> Two crossings do all the work. One is never met — everything orbits it. One is met — the
> generative crossing — and can never become a standpoint of its own. Everything else in
> the framework unfolds from that pair.

### B1.6 · `index.astro:102` — QUEUE

**Proposed:** "…function emerges from emptiness, persistence comes from circulation, the
center holds by never being met."

### B1.7 · `index.astro:103` — DDJ paragraph — QUEUE (criterion amended per catch)

**Proposed:**
> I should be honest about the direction of travel: I built this framework *through* the
> Dao De Jing — the text is where the pattern first spoke to me. That means the framework
> agreeing with the text proves nothing; agreement with your own seed is expected. So the
> DDJ here is not a witness for the theory. It is the subject of translation — and the
> test of a translation is philological: does the reading survive the strips, the parsing,
> and the stratum honesty — and what does the text refuse?

**Canon:** `just_ddj_v7.7_r3.md:5`; the text testifies only where it *resists* a wanted
reading (v7.6c methodology, carried).

### B1.8 · rosetta-stone reteach skeleton — QUEUE (both reviewers: approve)

Unchanged from r1 (six sections: two centers card · the engine + 弗居 [G] · the ::
discipline and the four tags taught · notation table with :: (i²=−1 two-paths [derived];
slice identity [derived]; the two 1ₙs) · "The Test" recast as precision-to-fail · [G]/[R]
stratum honesty note). Full copy drafted after skeleton ruling.

### B1.9 · `claims.astro:19` — Core Claim box — QUEUE (retyped per catch)

**Proposed (catch integrated; premise-honesty added — delta from the reviewer's text is
the clause naming the premise set, since "follows necessarily" alone would compress out
L1's [candidate] status):**
> **RSM claims one thing:** *if* reality is infinitely vast and infinitely divisible, then
> a single structural pattern follows from a small named premise set (one lemma-candidate
> still under audit) — stable structures persist by circulating around a crossing their
> frame never meets, and are generated at a crossing that is met but can never become a
> frame of its own. The claim is conditional; the derivation is what's checkable; the
> renderings into physics and biology are correspondences that carry no evidential weight
> of their own. [candidate]
>
> The Dao De Jing is deliberately not on that list. The framework grew out of the text, so
> their agreement is not evidence of anything. The DDJ is the subject of translation here.

### B1.10 · `claims.astro` predictions table — QUEUE (both reviewers: wounds go public)

As r1: the chain's own §9 list at its own tags (power-law nesting [falsifiable — target];
structural recession [falsifiable — unbuilt]; unitarity kill-condition [falsifiable];
v7.5 carryovers NOT restated) **plus** the two displayed wounds: the Bekenstein sort
(area-vs-volume — live contradiction risk with holography) and the two arrows. [open]

### B1.11 · `claims.astro:108-109` — physics row — QUEUE

As r1: "rendering only — 'maps to,' never 'is' — zero evidential weight except the
preregistered predictions; RSM does not derive ℏ, the ½, or Fourier conjugacy."

### B1.12 · `confidence.astro` re-derivation — QUEUE (tags corrected per catches)

- **[derived]** (each with stated conditionals "given P1, L1, T"): σ-invariance; i² = −1
  (two-paths pair); **the slice identity**; the standoff ("no *through*, only *around*");
  index-relationality (tree, not ladder).
- **[candidate]**: **the metric** (quadraticity conditional on the generation principle —
  the Finsler residue named; moved out of [derived] per catch); L1 (THE open head — say
  so); the anchoring line; the complementarity floor; **dimension-three necessary,
  uniqueness [open]** (two rival derivations unadjudicated; S² embeds in ℝ⁴) — replaces
  "minimal and sufficient".
- Rows re-typed/struck: "center can't be occupied" → met/unmet; "measurement crisis" →
  DROP (v7.5 argument circular; both reviewers concur); C/R/C Euler triad → DROP, no
  replacement triad manufactured (both reviewers concur); "Trees instantiate O₁" → the
  RULED tree mapping (B2.4) with the pith-toroid retraction displayed.
- Concentration-risk line if leanness advertised: "Lean ≠ robust" (`just_math_v7.7_r4.md:82`).

### B1.13 · `methodology.astro` — QUEUE

As r1: 5-tier scheme → the four canon tags + [G]/[R]; "What postulates?" → "What premises?
(Conditional, Bridge, Definition T, the seating commitment, L1 — and L1 is the open head.)"

### B1.14 · `faq.astro:57` — QUEUE (amended per catch)

**Proposed:**
> RSM works from a deliberately small premise set: the Conditional (a stipulated
> definition, never asserted as true), the Bridge (what is not distinguishable does not
> obtain), and one lemma-candidate — L1, "no unframed distinction," the open head of the
> audit. Earlier versions carried postulates (F, R, Q, frame-universality); the current
> chain dissolved or struck them — though Q's strike remains conditional on a named
> residue (the generation principle), still unruled. The leanness is real but
> concentrated: if the slice identity or L1 fails audit, three postulates re-inflate.

### B1.15 · `faq.astro:90-92` — QUEUE

As r1: "The present moment, like the origin, is never met — you're always on one side of
it or the other."

### B1.16 · `about.astro:31-34, 54` — QUEUE

As r1, with the ruled thesis form; no replacement triad (both reviewers concur):
- :31 → thesis form per B1.1 ruling.
- :32 → "The pattern can be stated in mathematics, physics, and biology precisely enough
  to check. The Dao De Jing is where I learned to see it — which is exactly why it doesn't
  count as evidence." *(Note: "stated in mathematics" — the derivation's language — is
  deliberate; math is not a witness domain.)*
- :54 → "This is alive and changing. It is not sealed: the framework's one named
  lemma-candidate (L1) is the open head of the audit."

### B1.17 · `hypotheses.astro` — QUEUE (composed: unpublish + stub)

Unpublish the θ(p) protocol page; route `/hypotheses` to a one-paragraph stub:
> A measurable biological protocol lived here. It was built on a typing error the current
> framework version forbids (treating the loci Gₙ/Bₙ as physical vector fields), and it is
> being re-derived against v7.7. What returns will be preregistered.

---

## Batch B2 — objects

### B2.1 · `origin.mdx` — QUEUE

As r1 (strikes + two-centers card; P₀ line becomes canon-verbatim chain per the B1.5
ruling; O₁-uniqueness not asserted [open]). Per both reviewers: :38 "This isn't
philosophy. It's the geometry of persistence." → **CUT** (not tagged).

### B2.2 · `wheel.mdx` — QUEUE (弗居 corrected per catch)

- O→G→P card → two-centers replacement; :52 有無相生 re-typed under the slice identity
  with `::`.
- **弗居 add, corrected:** the phrase is Chapter 2 / slip A17 (功成而弗居), NOT Chapter
  11. wheel.mdx carries it as a **marked cross-reference**: "the hub is the thematic home
  of a line from Chapter 2 — 功成而弗居, 'completes yet structurally-cannot-dwell' — the
  strongest [G] match in the chain (`just_ddj_v7.7_r3.md:77`)."

### B2.3 · `atom.mdx` — QUEUE

As r1: "never meeting the center"; "To me, the atom looks just like…" register;
O/G/P card struck; "maps to, never is."

### B2.4 · `tree.mdx` — **RULED (adopted in full)**

Ships: **pith :: Oₙ** (point in cross-section, axis in the whole tree — the dual type) ·
**cambium :: Gₙ** (the generative layer) · **rays :: Bₙ** · **branch node :: Pₙ**, with
the pith-toroid retraction displayed ("the generative layer is the cambium, not the pith
— a correction that improved the fit," `just_physics_v7.7_r1.md:79`). The hollow-pith
image survives as the face of the unmet center; generativity moves to the cambium.
`index.astro:52-54` aligned. Ruling recorded in audit trail (addendum below).

### B2.5 · `valley.mdx` — QUEUE

As r1: add "玄牝 :: P₀ — the generative paradox. [R]" + stratum honesty note (received-only;
the deepest correspondences are the least anciently witnessed).

### B2.6 · `room.mdx` / `bellows.mdx` — QUEUE

As r1: leave the literal room; cut the bellows cosmology sentence.

---

## Batch B3 — remaining blemishes

| # | Location | Proposed | Status |
|---|----------|----------|--------|
| B3.1 | `standing-wave-pattern.md:207` | "A center that is never met" | QUEUE |
| B3.2 | `the-gap.md:188` | "The engine room of reality is never met. That's what makes it run." | QUEUE |
| B3.3 | `the-gap.md:111` | "If the center could be met, you'd stop there." | QUEUE |
| B3.4 | `topology-of-being-alive.md:73` | **Composed:** the line fix ("The generative center is met — and it can never become a frame of its own. That impossibility, not emptiness, is what makes it generative.") AND a pre-v7.7 banner on the essay ("Written under an earlier framework version; its two centers are typed to the superseded scheme. A rewrite is pending."). Full rewrite deferred. | QUEUE |
| B3.5 | `chapter-01.md:12-14` | 名 held open: "名's obtainedness is held open in the current framework; the live [candidate] reading is the 明/名 square-vs-root split." Frontmatter level (SITE TIER, not canon tag): `plausible`. | QUEUE |
| B3.6 | `chapter-01.md:96` | 玄 table gloss → "the generative crossing (not the origin; the origin is the unmet one)" | QUEUE |
| B3.7 | `chapter-42.md:99-104, 131-134` | "3: necessary; uniqueness open — two rival derivations unadjudicated; S² embeds in ℝ⁴" — tag [candidate] (the `[derived-in-part]` tag in r1 was invented; struck). Cut "✓ Dimensional sufficiency". | QUEUE |
| B3.8 | `chapter-25.md:95-104` | **Drop the RSM column**; 反-as-return row keeps `::` (both reviewers concur). | QUEUE |
| B3.9 | `chapter-02.md` (was: ch-11) | **Corrected per catch:** strengthen chapter-02's existing 弗居 element with the non-constitutability correspondence — "弗 marks structural impossibility, not contingent negation (不); 成而弗居 :: the seat's non-constitutability, the strongest [G] match." chapter-11 gets at most a marked cross-reference to Ch. 2. | QUEUE |
| B3.10 | `guodian.astro:225` | "The bamboo is where the translation begins." | QUEUE |
| B3.11 | `ddj.astro:74` | "The translation begins in the characters." | QUEUE |
| B3.12 | `ddj.astro:16` | "Reading the DDJ as the subject of a structural translation, not as mystical poetry." | QUEUE |
| B3.13 | `text/what-is-ddj.astro:19` | "treats the text as the subject of a structural translation and asks: what does the language itself constrain?" | QUEUE |
| B3.14 | the two unpublished essays | Stay unpublished until rewritten against v7.7 or retired. | QUEUE |

---

## Draft ledger entries (apply only on Will's confirmation — the audit trail is his)

### Divergence ledger — proposed Entry 002 — The Chained-`::` Objection

**The proposal (Claude instance, reviewing site redraft r1):** that the chained form
"P₀ :: 玄牝 :: the generative paradox" treats `::` as transitive and "launders two hops
into one identity"; proposed replacing the canon chain with a split two-hop form and a new
gloss ("the excluded terminal condition: absolute indistinguishability, which cannot
cohere").
**The check:** canonical, not computational. `CLAUDE.md:66` writes the identical chain;
the `::` doctrine (`CLAUDE.md:8-9`) explicitly defines chains: "A `::` chain (A :: B :: C)
claims all terms converge on one identified invariant" — a sanctioned form, distinct from
transitive identity. The proposed replacement gloss echoes v7.6-candidate grounding
language ("because P₀ is logically incoherent"), a version whose seating was refused.
**What was true inside the false proposal (always stated):** the laundering worry is real
on a public page read without the discipline; met by teaching `::` on the rosetta-stone
page before any chain appears, and keeping the homepage plain-language.
**Same review, second citation issue:** the review cited "M5/M9/M10" tags and a
"provenance map." No M-numbered tags exist in the repo; canon's provenance tags are
P1/P2/P3 (v7.6c methodology, carried into the v7.7 physics chain). One cited ban is real
under a different authority: "plucked out" is a GUARD in the walkthrough ledger (:239).
Source of the M-numbering unconfirmed — **Will: is there an M-tagged provenance map
outside the repo, or is this confabulated citation?**
**Resolution:** rejected by Will's ruling (canon form upheld). The same review produced
five verified catches that were integrated into r2 — logged for the ratio, per protocol.

### Walkthrough ledger — proposed addendum (tree mapping)

**Ruling (Will, 2026-07-14, via site-redraft walkthrough):** the four-part tree mapping is
adopted — pith :: Oₙ (dual type: point in section, axis in the whole) · cambium :: Gₙ ·
rays :: Bₙ · branch node :: Pₙ — proposed by a reviewing instance, ruled by Will, shipped
on the public tree page with the pith-toroid retraction displayed. Provenance: P2 (found),
machine-proposed, Will-ruled.

---

## Process (per batch)

1. Rulings via chat rounds (~4 items each, old→new previews) — APPROVE / AMEND / REJECT.
2. Apply ruled items → `npm run build` → dist grep (`occup|unoccupiable|Periphery|P→O`;
   ruled survivors only) → `checks.py` → `site_vocab_lint.py` → commit → push →
   `./scripts/publish.sh` → live spot-check.
3. REJECTs to the divergence ledger per its standing protocol.
