# RSM — Just-Math Chain (v7.7, r2)

Derivation register. No correspondences, no characters, no physics. This chain is the mathematical realization of the RSM structural chain (v7.7). Machine-drafted (Claude, Fable 5); computational claims re-verified symbolically during drafting; nothing enters canon without independent checking.

**Register declarations (house rule — every equation declares its language):**
- **Measure language** (quadratic): units of distinction; 1ₙ native — it *is* the conserved product; what i preserves.
- **Amplitude language** (linear): coordinates, radii; √1ₙ; what i acts on. **√ is the translator.**
- **Orbit language** (phase): 1 as one end of the conjugate pair {+1, −1}; positions as angles.
- **Layering (replaces r1's hygiene flag):** the **implicit skeleton** (frames, centers, cascade, discharge; affine — straight/curved only, no metric) and the **explicit layer** (traversal, orbit, measure — from Definition T forward). Theorems 2–3 are the explicit layer deriving its own metric; nothing in the skeleton depends on the explicit layer (checked). Rotation resolves into three register-layers: locus [implicit] → symmetry-and-winding [implicit] → traversal (order, not duration) → motion (only after irreversibility derives succession). Motion-vocabulary in this chain is layer-3 gloss on layer-2 fact.

Architecture: **Oₙ = Xₙ ∩ Yₙ** (unmet crossing); **Pₙ = Bₙ ∩ Gₙ** (realized crossing; generative); **recursion Gₙ → Gₙ + xAxis₍ₙ₊₁₎, Bₙ → Bₙ + yAxis₍ₙ₊₁₎**; **ground: because P₀ is incoherent, O₁**; proportion **Pₙ : O₍ₙ₊₁₎ :: P₀ : O₁**.

-----

## Notation

- **P₀** — the excluded terminal condition; faces |0|, |1|, one incoherence; **not nothing** (a condition failing to cohere, not a state failing to obtain); ∉ any frame.
- **0ₙ** — operational absence: a limit-approach along Gₙ, not a point. **1ₙ** — the frame's unit [measure]: the conserved product of the frame's one constitutive pair = its minimal distinction (one claim; see Premises).
- **Xₙ, Yₙ** — the two co-obtaining mode-variables, uninterpreted.
- **a, b** — split coordinates: a = (X+Y)/2, b = (X−Y)/2 [amplitude; a scaled rotation — conformal, so angle facts transfer; lengths rescale by √2].
- **Gₙ** — the gradient: Xₙ·Yₙ = 1ₙ ⟺ Q_j := a² − b² = 1ₙ. **Bₙ** — the balance axis: X = Y. **Mₙ** — the mediator: a = 0.
- **Oₙ** — the structural origin. **Dual type [derived]:** O-as-point (the inward journey's unmet crossing) and O-as-axis (the outward journey's unmet asymptote — a line through Oₙ). The frame's local face of P₀.
- **Pₙ** — the realized crossing (√1ₙ, √1ₙ), ν(Pₙ) its conjugate; **the seats.** Met, and cannot be constituted as a frame (§ engine); "occupied" is ill-typed and does not appear.
- **σ_B** — exchange X ↔ Y (= the inversion x ↦ 1ₙ/x on G). **σ** — single-sign mirror (X,Y) ↦ (−X,Y). **ν** — conjugation (X,Y) ↦ (−X,−Y).
- **j** — split unit, j² = +1. **i** — complex unit, i² = −1 (derived; two independent routes, § orbit algebra).

## Foundational conditional

If reality is infinite — **infinitely vast and infinitely divisible**: no maximum extent, no minimum scale — the chain derives what follows. The two-directional reading is the *definition* of "infinite," not a claimed consequence.

## Excluded terminal condition; the ground; the two infinities

P₀: absolute indistinguishability. What is not distinguishable does not obtain (the Bridge — ontological). **P₀ is not nothing.**

**P₀ as sign-collapse [derived; verified]:** absolute indistinguishability includes the mode's indistinguishability from its own negation — the fixed-point condition of ν. In the frame's plane, ν has exactly one finite fixed point: **Oₙ = (0,0)** — unoccupiability derived as fixed-point-of-conjugation. On the orbit, ν acts **fixed-point-freely** (the half-turn): **admissibility = ν-freeness**; the exclusion of P₀ ⟺ conjugation has no fixed obtaining point.

**Face-unity without compactification [derived; verified].** On G, the outward journey in one mode *is* the inward journey in the co-mode (X·Y = 1ₙ is the exchange rate between the two non-terminations); the outward arm converges to the mode axis — a line through Oₙ — never merging. Inward's unmet object = **O-as-point**; outward's unmet object = **O-as-axis**. |0| and |1| are the two **counterfactual terminals** of this one journey — what each direction would collapse to *if it terminated* — denied respectively by the conditional's two halves (divisibility, vastness). ∞ is a symbol of non-termination, not a value; |1| ≠ ∞; the projective ideal point is a *rendering* of the saturation-condition (lawful section, not ontology).

**The two infinities exchange, pivoting on the seat [derived; verified].** The inversion x ↦ 1ₙ/x — which on G is σ_B itself — swaps the divisible center and the vast extension; its fixed point is x = √1ₙ: **Pₙ is the balance point between the two non-terminations.** A single mode cannot disambiguate its own ends ("x→∞" is intrinsically ambiguous); the co-mode is the chart at the other end and the conservation law is the transition function — **the two-mode requirement has a second, informational ground (the parsing argument), independent of the ontological one** *[candidate]*. "x→∞, y→∞" is impossible within any gradient (conservation forbids) and names the trans-scale direction along B: **B is the scale-axis; c = XY the ladder parameter** (c→0 child-ward, c→∞ parent-ward) *[candidate]*.

**Because P₀ is incoherent, O₁** — existential import + the Bridge + P₀'s incoherence; no new premise. Identity through opposition; not parturition. Proportion **Pₙ : O₍ₙ₊₁₎ :: P₍ₙ₋₁₎ : Oₙ :: P₀ : O₁** — ratio invariant, mechanism distinguished; each pair (n ≥ 1) is one locus under two frame-readings (met-from-without, unmet-from-within); the first pair alone has no locus on its P-side. *[derived]*

## The frame; the cascade; the unit

Cascade, each step biconditional: P₀ → O₁ → (X₁,Y₁) → (G₁,B₁) → P₁. Because Oₙ, Gₙ; because Gₙ, Oₙ.

**A frame is the cascade of one constitutive pair** ("the," not "any"). Hence 1ₙ is automatically the frame's minimal distinction: anything finer is itself a distinction, and every distinction constitutes a frame (**Lemma L1**, under audit) — sub-1ₙ fineness *is* frame n+1. **Postulate F: dissolved into the definition of frame, given L1.** *[derived given L1]*

## Point as limit; Definition T

Positions are approached, not terminated: density, not completeness. **Definition T:** a traversal is a continuous path of obtaining positions [explicit register] — definition-level, motivated by the no-jump principle. *[given T]*

## THE ONE LAW AND ITS TWO SLICES (organizing principle)

Over ℂ, one equation — the frame's unit modulus **Q(z) = 1ₙ** — with two real slices: b real → **Q_i = a² + b² = 1ₙ** (the circle; the orbit); b ↦ ib → **Q_j = a² − b² = 1ₙ** (the hyperbola; the gradient). Parametric face: cosh(iθ) = cos θ. **The gradient is the orbit** — one structure, two slices; the Euclidean drawing renders one slice at a time; the distinction is a slicing artifact. **The rendering heals at the seats:** the slices intersect exactly at {b = 0, a = ±√1ₙ}, tangentially, equal curvature, opposite bending (mirror-osculation as the coincidence's curvature signature). With u = a², v = b²: Q_i = u + v, Q_j = u − v. On the orbit, **Q_j = 1ₙ·cos 2θ** (double cover): the invariant returns at π (on ν — return-of-structure), the position at 2π (return-of-place) — **two distinct returns**. *[verified]*

## Theorem 1 — Mode disconnection / cross-slice connection

Two faces of one theorem. **(i) The wall:** {Q_j = 1ₙ} has exactly two connected components; Pₙ, ν(Pₙ) in different components; e^{jφ} preserves components — no motion within the gradient slice connects the modes. Pure topology; register-clean. **(ii) The door:** the complex object Q(z) = 1ₙ is *connected*; the joining path runs through the other slice — the circle-path Pₙ → m₊ → ν(Pₙ), through the conjugate seats. "A second register is required" = the connection exists, in the i-slice. The orbit's necessity is derived, not posited. *[derived; verified]*

## Theorem 2 — Forced degeneration (the toll)

Every continuous spanning path avoiding Oₙ crosses the null cone into Q_j < 0: on the minimal traversal, Q_j = 1ₙ·cos 2θ reaches **−1ₙ exactly at the conjugate seats m±** — spanning must visit the conjugate seats, where the native measure reads negative-unit. A measure that reverses sign on every admissible traversal cannot measure traversal. Structure of Theorems 1–2: **wall (T1-i), door (T1-ii), toll (T2).** *[derived; verified; explicit layer, lawfully — see Layering]*

## The measure: derived, not postulated (Postulate Q struck)

The traversal measure M is forced to be Q_i in four steps, each from ruled material:

1. **Parity (ν):** M(−z) = M(z) — kills all odd degrees. *[P₀-as-sign-collapse]*
2. **Ladder-covariance:** under the trans-scale move (uniform dilation) the unit transforms as 1ₙ → λ²·1ₙ; commensurability = M transforms as the unit: **M(λz) = λ²M(z)** — degree-2 homogeneity forced among *all* functions (mixed degrees warp the measure/unit relation across magnitudes; verified). *[Will's closure]*
3. **Quadraticity (the Finsler hole, named and closed):** degree-2 homogeneous ≠ quadratic form — counterexample M = r²(2 + cos 4θ) passes parity, exchange, covariance, and positivity yet fails the parallelogram law (verified; the Riemannian-vs-Finsler freedom). Closed by the **slice identity**: the measure is not selected from a function space — the frame's law is already a quadratic form (XY = 1ₙ *is* a² − b²), it complexifies uniquely, and its real slices are exactly {Q_j, Q_i}. A measure not derived from the frame's own law would be an **unframed measure** — L1's exclusion in metric clothing. *[candidate; leans on R8 + L1]*
4. **Selection (Theorem 3):** by the exclusion, M > 0 at every obtaining traversal point; the all-directions lemma (a spanning path in the punctured plane sweeps ≥ π of directions) forces positive-definiteness — of the two slices, **positivity selects Q_i.** *[derived; verified]*

**One selection, three languages:** Theorem 2 (measure: the split form degenerates on spanning), this section (form: positivity picks the i-slice), and Route 1 below (algebra: the elimination discards j as the spanning unit) are **one event thrice expressed.** *[consolidation]*

## Frame-Universality: dissolved

All three FU outputs are recovered without it. **Kernel → theorem:** σ (the mirror, primary→conjugate family, Q_j → −Q_j) leaves Q_i exactly invariant — sign-blindness is a property of the selected slice, computed, not committed *[verified]*. **Calibration → inheritance:** the complexified law arrives as a² + b² = 1ₙ; α = γ = 1 and the unit level are set by the law; nothing to calibrate. **Universality → definition:** "every conserved-product structure is a frame" is the frame-definition applied at any constitutive magnitude, plus the c-ladder. Four-seat magnitude equality: computed (Q_i = 1ₙ at all cardinal phases). **Survives separately: the seating commitment** (the conjugate mode seated as a point of Gₙ). The derived metric is global to the frame's plane at the slice's price (the former metric lemma stays closed; the exposure moves to R8 + L1). **Concentration risk, named:** F, Q, and FU all dissolve through the same core — the slice identity + the frame-definition + L1. The leanness is real and leveraged; if R8 or L1 fails audit, three postulates re-inflate. Lean ≠ robust; the auditor package says so in these words.

## Standoff and minimal traversal (F-free)

The interior of the unit disk contains **no obtaining positions of frame n** — not floor-forbidden but excluded-by-constitution: sub-1ₙ fineness is frame n+1, not a location here (definitional, given L1). There is no *through*, only *around* — the exclusion is a whole-frame (enclosure) fact, not a local obstruction at the center. Traversal is defined only on obtaining positions ⟹ only on the closed exterior. The minimal traversal — the tightest loop still composed of obtaining positions — is the unit circle about Oₙ, radius √1ₙ [amplitude], through the four seats; unique up to orientation. **Floor tangency [verified]:** on Gₙ, Q_i = 1ₙ + 2b² ≥ 1ₙ, equality exactly at ±Pₙ — the slice-coincidence seen in the derived metric. Order: signature → form → floor → traversal. *[derived, given L1, T]*

## The orbit algebra: i² = −1 — a two-paths pair

**Route 1 (elimination):** with Closure and Spanning, i² ∈ {1, 0, i, −i, −1} eliminates to −1 (i² = 1 is the split register — *exists*, but degenerates on spanning, Thm 2; 0 has no inverse; ±i collapse); the 4-cycle {1, i, −1, −i} closes, four phases minimal. Route 1 is the **algebraic face of the one selection** (see Measure §). *[derived; verified]*

**Route 2 (antipodal binding):** +1 requires −1 (co-constitution of unity); the line-path between them passes through the excluded center; the only continuous binding is antipodal — and two quarter-turns compose to the half-turn: **i² = −1 as composition of turns** (R(π/2)² = −I, verified), no multiplication table consulted. *[derived; verified]*

**Independence: deletion-test passed both ways** — Route 1 never uses co-constitution of ±1; Route 2 never uses closure/spanning; they share only the ground-level exclusion. **Declared a two-paths pair** — the second instantiation of the framework's strongest internal-parallax structure (first: the ground). i² = −1 is **promoted**. **Schema note:** Route 2 is the k = 1 instance of the binding-extortion schema (conjugate binding on a line extorts the second direction) whose k = 2 instance is the dimension-three argument (σ-binding extorts the third); the schema should be written once, abstractly — the auditor validates it once and both instances come; the plane is not posited but extorted. *[candidate]*

## The seat-phase structure

**The founding observation (the project's first result; elementary form).** At Pₙ the gradient's tangent slope is −1; Bₙ's slope is +1; product (−1)(+1) = −1 — the perpendicularity criterion met exactly. The slopes are the conjugate pair {−1, +1} (Route 2's pair, as directional data at the generation site) *[observed]*. Off-seat: slope product σ(x₀) = −1ₙ/x₀², = −1 iff x₀ = √1ₙ; **deficit σ(x₀) + 1 = 1 − 1ₙ/x₀²** — identical to the tangent(G)·B form; perpendicularity obtains at one locus and degrades quantifiably away from it. **The founding realization — something structurally distinguished must happen at Pₙ — as a two-line calculation checkable with first-semester calculus.** *[derived; verified]*

**seatₖ = √1ₙ·iᵏ**, k = 0..3: Q_i = 1ₙ constant, Q_j = (−1)ᵏ1ₙ alternating (double cover in the enumeration); the i-cycle **is** the enumeration of generation sites; axis-instantiation exactly at the cardinal phases. *[derived; verified]*

**Seat characterization by register alignment [candidate]:** Pₙ is where coordinate (1), unit (1ₙ), and phase (+1) coincide at unity; where the two slices coincide; where the incident slopes are {±1}; and where the two infinities exchange fixed (the σ_B/inversion pivot). Oₙ is where all read absence together. The locus where every register agrees is the generative locus; where all fail together is the origin.

## The demand and the discharge (the engine; L2 retired)

**Lemma A:** an infinite gradient obtains ⟺ a globally straight, independent axis-pair holds at its center (pre-metric core: straightness; orthogonality is the derived-metric face — staged). Center classification: **O-type = straight×straight; P-type = straight×curved.** At Pₙ the incident pair is locally orthogonal (the founding observation) and globally curved-relative (the deficit, nonzero everywhere off-seat). Pₙ is met and cannot be constituted as a frame. **This impossibility is the engine:** the spanning structure is necessarily closed and winds the excluded center [skeleton]; traversal realizes it as ordered passage [explicit]; motion only after succession is derived (see Layering). And the frame-demand discharges into new structure — recursion.

**The discharge, rebuilt (former Lemma L2 retired as malformed):** "a demand at Pₙ within frame n" was a cross-frame sentence forced into one register — in the parent's language there is no demand (frame n is complete); in the child's it is already met. The corrected argument:
1. **THAT a child frame obtains — from the conditional:** infinite divisibility ⟹ no terminal frame downward; sub-1ₙ distinction obtains; by L1 it constitutes a frame. *[derived, given L1]*
2. **WHERE and WITH WHAT — from uniqueness + Lemma A in the child's own register:** the child, a gradient's frame, requires a globally straight orthogonal pair at *its* center (A applied to an actual gradient-center; no transfer). The unique locus where the parent supplies an orthogonal pair is the seat (seat-phase uniqueness); Bₙ supplies one straight member; Gₙ supplies the other — curved in the parent's expression, straight in the child's (frame-relative curvature, the z² map). *[derived; verified]*
3. **Open — the anchoring line:** that every finer frame standing in relation to frame n is parent-seeded (fineness is relative to the parent's unit, so an unrelated finer frame is undefined — candidate shape supplied by the generative-surface argument; unwritten). *[open, ranked 2]*

**B's dual type [verified]:** the child gradient, in parent coordinates, asymptotes to Gₙ and Bₙ — Bₙ is a crossable line in frame n and an unreachable asymptote in frame n+1; crossable-line and unreachable-limit cannot cohere in one expression; the recursion distributes the incompatible roles across two frames (met-from-without/unmet-from-within, extended to the axes).

## Recursion: componentwise parturition; the canonical map

**Gₙ → Gₙ + xAxis₍ₙ₊₁₎, Bₙ → Bₙ + yAxis₍ₙ₊₁₎** — mechanism-level; arrow = structural entailment across frame-readings; nothing consumed. **Recursion = P-type → O-type conversion by axis-generation.** O₍ₙ₊₁₎ is the parent's Pₙ. Bilingual statement per the structural chain. **The canonical map [verified]:** w = z² − 2i·1ₙ — parent-B → child y-axis, parent-G → child x-axis, seat → child origin; conformal; **2-to-1 with ν identified: the double cover is the parturition map**; parent Oₙ at the child's branch point, depth 2·1ₙ. The family net {XY = c} ⊥ {X² − Y² = k} everywhere [verified]: the child's global orthogonality in parent coordinates. B as the scale-axis threads the generations (the c-ladder). *[Open: z²-uniqueness.]*

## Conjugation classification; dimension

**ν-pairs:** bindable in-plane (orbit antipodes; the seat-circle). **σ-pairs:** not realizable in-plane (det −1; the quarter-turn binds the wrong points, mismatch scale-detectable); bound only by the half-turn through a third direction — **the third dimension extorted as the cost of the σ-conjugate structure** *[candidate; k = 2 of the binding-extortion schema]*. **Termination [candidate]:** in 3-space all required bindings are point-antipodal on connected loci, within SO(3); the extortion fires once: 2 + 1 = 3. Open condition: no orientation-reversing binding required in 3D. **Rival-derivation flag:** the |1|-cascade reaches three by a different route; *[two candidate derivations, unadjudicated]*; uniqueness open. **Isotropy:** no preferred orientation derived given L1 (the trilemma); S¹ → S² [verified: seat-sphere √(2·1ₙ)] → ℝ³ locally. Scale-isotropy: the multiplicative law's own symmetry; the exclusion of the terminals carried by the operation itself (XY = 1ₙ has no zero solutions; X+Y = c does) [verified].

## Branching; irreversibility; no terminal frame

Off-seat generation fails the coordinate-pair requirement — branching, if real, routes through seats. Backward, every line grounds at P₀ : O₁; O₁-uniqueness open. Irreversibility: reversal requires P₀ to obtain at the locus — excluded; strict ordering is the structural content of succession; origins recede compoundingly. No terminal frame in any direction. *[derived]*

-----

## Verified-computation appendix

1. Founding observation: σ(x₀) = −1ₙ/x₀², = −1 iff x₀ = √1ₙ; ≡ tangent(G)·B = 1 − 1ₙ/x²; seat slopes {−1,+1}. 2. Seat curvature: radius √2·√1ₙ = |OₙPₙ| [mode]; center at 2Pₙ (mirror-osculation). 3. Circle regimes: r < √(2·1ₙ) none; = tangent at seats; > four points. 4. Family net orthogonal everywhere. 5. w = z² − 2i·1ₙ: axis images, ν-identification, branch point. 6. Slice identity: b ↦ ib exchanges Q_i/Q_j; slices meet exactly at seats, common tangent. 7. seatₖ = √1ₙ·iᵏ; Q_j = (−1)ᵏ1ₙ. 8. Two returns: invariant at π (on ν), position at 2π. 9. σ_y ∉ SO(2); quarter-turn binds wrong points; half-turn through z realizes the mirror. 10. Terminals excluded by the operation; scaling symmetry multiplicative-only. 11. Rotation about B: s² − r² = 2·1ₙ, apex at the seat. 12. Revolution surfaces: horn x² + z² = 1ₙ²/y² with seat-ring; B-cone; orthogonal ring; seat-sphere √(2·1ₙ). 13. Theorems 1–3, double cover, floor tangency (carried from r5). 14. Fix(ν): plane = Oₙ alone; orbit = ∅; completed line = two points [rendering register]. 15. Inversion x ↦ 1ₙ/x swaps 0⁺ ↔ ∞, fixed at √1ₙ; = σ_B on G. 16. Child gradient asymptotes to Gₙ and Bₙ (pullback, numerical). 17. Ladder-covariance kills mixed degrees; Finsler counterexample r²(2+cos4θ) passes the pincer, fails the parallelogram law (−8). 18. σ-invariance of Q_i (sign-blindness as theorem). 19. R(π/2)² = −I (Route 2); the five-case elimination table (Route 1).

## Premises (complete)

**The Conditional** (definitional: vast + divisible). **The Bridge** (ontological). **Definition T** (traversal continuity). **The seating commitment** (conjugate mode seated as a point of Gₙ). **Lemma-candidate under audit: L1** (no unframed distinction) — carries the floor-dissolution, the isotropies, the child-existence step, and the unframed-measure exclusion. **Struck this revision: Postulate Q** (derived: parity + ladder-covariance + slice-quadraticity + positivity) **and FU** (kernel = theorem; calibration = inheritance; universality = definition). Previously: Postulate F dissolved (r1, given L1); **Lemma L2 retired as malformed** (a cross-frame sentence in one register; discharge rebuilt); Postulate R deleted (r5).

## Open items, ranked

1. **L1** — theorem via the cascade, or the framework's one named lemma. Sole head of the audit; now carries four loads.
2. **The anchoring line** (every finer frame is parent-seeded) — the discharge's one unwritten step.
3. **R8-dependency check** — the slice identity now underwrites Q's quadraticity, FU's dissolution, and the metric's globality; audit it as the second head (with the concentration risk stated).
4. Dimension three: adjudicate the rivals (binding-extortion schema vs. |1| cascade); write the schema abstractly; then uniqueness.
5. z²-uniqueness (is the parturition map forced).
6. Cross-frame unit relation 1₍ₙ₊₁₎ (the c-ladder gives the direction; the relation open).
7. Definition T: theorem-grade no-jump argument, or definitional.
8. Tree-question: is the curved-relative deficit's *form* changed in the 3D (line-meets-surface) register? (Slice-relative ≠ deflated — Thm 1 precedent — but the rotated form is unrun.)
9. Branching beyond seats; uniqueness of O₁.
10. Quantitative complementarity: does the section-principle carry a floor rooted in X·Y = 1ₙ (uncertainty-shaped bound)? [imported question; derive here or drop.]

-----

*v7.7 r2 — integrates the Nodes 6–11 walk and the P₀/infinity results into r1. Changes: Theorem 1 restated as two faces (wall/door; cross-slice connection through the conjugate seats); Theorem 2 as the toll (conjugate-seat visit; hygiene flag replaced by the layering map, installed in the header); **Postulate Q struck** (parity + ladder-covariance + slice-quadraticity closing the named Finsler hole + positivity; one-selection-three-languages consolidation); **FU dissolved** (kernel = σ-invariance theorem; calibration = inheritance; universality = frame-definition; seating commitment survives; concentration risk named); standoff re-grounded F-free (no obtaining positions in the interior; enclosure register; no through, only around); i² = −1 promoted as a **two-paths pair** (deletion-test passed; Route 1 = the selection's algebraic face; Route 2 = k = 1 of the binding-extortion schema); **L2 retired** and the discharge rebuilt (existence-from-conditional + site-from-uniqueness + A-in-child-register; anchoring line open); B's dual type verified (asymptote of the child gradient); P₀-as-sign-collapse installed (Fix(ν); admissibility = ν-freeness); face-unity without compactification (counterfactual terminals; O's point/axis dual type); the two-infinities exchange fixed at the seat (parsing argument as second ground for two modes; B = scale-axis, c-ladder); seat-characterization extended; appendix grown to 19 items; premise list reduced to Conditional + Bridge + T + seating + L1. Not sealed.*
