# Adversarial Pass r1 — attacking the session's premise strikes
Machine-drafted (Claude, Fable 5) attacking its own formalizations. Findings ranked by damage. Not sealed.

## Finding 1 — the Q-strike's step 3 is soft [SIGNIFICANT; partial walk-back]
Claimed: quadraticity closed by the slice identity + L1 (a non-quadratic measure = "unframed").
Attack: the Finsler counterexample M = (3a⁴ − 2a²b² + 3b⁴)/(a²+b²) is a rational function of frame objects — FRAMED. L1 does not exclude it. What actually closes:
- Among POLYNOMIALS: degree-2 homogeneity (ladder-covariance, Will's closure — this step HOLDS) + σ_B kills ab → αa²+γb² only. Genuine.
- The classical closure (C² at origin ⟹ quadratic) is UNAVAILABLE: the origin is Oₙ, excluded; the measure needn't be defined there. The Finsler freedom lives in the excluded point's smoothness.
Status: Q struck CONDITIONALLY — on either (a) a polynomiality commitment, or (b) a named **generation principle** (frame measures are faces/slices of the constitutive law; no quotients) — a genuine commitment, not L1. Premise count 5 or 6 pending Will's ruling. Derivation target: derive the generation principle, or accept and name it.

## Finding 2 — Lemma A deflation risk [MODERATE]
"An infinite gradient obtains ⟺ a globally straight pair at its center." But straightness is register-relative (the child's axes are curved-in-parent, straight-in-child — verified, the z² map). So A must read "straight in the frame's OWN register" — and a frame's own axes are straight in its own register nearly by construction. A may be near-definitional rather than derived. Consequence: harmless for the discharge (D applies A in the child's own register, where near-definitional suffices) but the tag "[derived]" should soften to "[analysis of 'axis'; near-definitional]". Honesty edit, not a structural loss.

## Finding 3 — FU-dissolution survives [CLEAN]
Kernel-as-theorem (σ-invariance of Q_i): pure computation; holds regardless of Finding 1 (whatever measure wins, IF it is Q_i, sign-blindness follows; if Finding 1 reopens the selection, the kernel claim is conditional on the selection, already noted). Calibration-inheritance: holds. Universality→frame-definition: holds. No walk-back.

## Finding 4 — discharge rebuild survives, with the anchoring residual now consolidated [CLEAN + CONSOLIDATION]
C′ (existence from P1 + L1): clean. D (site from seat-uniqueness + A-in-child-register): clean given Finding 2's softened A. The anchoring line's residual (step iv: inheritance as the ONLY cross-frame expression mechanism) = z²-uniqueness — open items 2 and 5 MERGE: "uniqueness of the cross-frame expression mechanism." One question, two former line-items.

## Finding 5 — the two-paths pair survives [CLEAN]
Route 2's geometry (R(π/2)² = −I) is premise-independent of Finding 1. Route 1's elimination leans on Thm 2's degeneration, not on the Q-strike's step 3. Independence verdict unaffected.

## Net effect on the audit heads
1. L1 (unchanged, four loads). 2. The generation principle (NEW — rule: premise or derivation target). 3. Cross-frame expression uniqueness (anchoring ≡ z²; merged). 4. R8-dependency (unchanged). Lemma A tag softened.
