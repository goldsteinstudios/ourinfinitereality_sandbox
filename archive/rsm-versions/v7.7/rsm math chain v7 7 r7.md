# RSM — Just-Math Chain (v7.7, r7)

Derivation register. No correspondences, no characters, no physics. This chain is the mathematical realization of the RSM structural chain (v7.7). Machine-drafted (Claude, Fable 5); computational claims re-verified symbolically during drafting; nothing enters canon without independent checking.

**Register declarations (house rule — every equation declares its language):**
- **Measure language** (quadratic): units of distinction; 1ₙ native — it *is* the conserved product; what i preserves.
- **Amplitude language** (linear): coordinates, radii; √1ₙ; what i acts on. **√ is the translator.**
- **Orbit language** (phase): 1 as one end of the conjugate pair {+1, −1}; positions as angles.
- **Layering (replaces r1's hygiene flag):** the **implicit skeleton** (frames, centers, the coinherence Rₙ, the engine argument; affine — straight/curved only, no metric) and the **explicit layer** (traversal, orbit, measure — from Definition T forward). Theorems 2–3 are the explicit layer deriving its own metric; nothing in the skeleton depends on the explicit layer (checked). Rotation resolves into three register-layers: locus [implicit] → symmetry-and-winding [implicit] → traversal (order, not duration) → motion (only after irreversibility derives succession). Motion-vocabulary in this chain is layer-3 gloss on layer-2 fact.

Architecture: **Rₙ = {Oₙ; xAxisₙ, yAxisₙ; Xₙ, Yₙ; Gₙ, Bₙ; Pₙ}** — the Recursive Frame, a simultaneous coinherent cluster; **Oₙ = Xₙ ∩ Yₙ** (the sign-collapse paradox); **Pₙ = Bₙ ∩ Gₙ** (the balance paradox; generative); **recursion, frame-level: Rₙ → Rₙ, R₍ₙ₊₁₎** (comma = severance-with-coexistence); componentwise **Gₙ → Gₙ + xAxis₍ₙ₊₁₎, Bₙ → Bₙ + yAxis₍ₙ₊₁₎**; **ground: because P₀ is impossible, O₁** (modal — the first distinction necessary given obtaining); proportion **Pₙ : O₍ₙ₊₁₎ :: P₀ : O₁**.

-----

## Notation

- **P₀** — the excluded terminal condition; faces |0|, |1|, one impossibility; **an ontological-logical impossibility** — not nothing, not a state that fails to obtain, not a condition that fails to cohere (failure-language reifies a subject; there is no referent to fail); sense without referent; ∉ any frame.
- **Rₙ** — the Recursive Frame: the coinherent cluster {Oₙ; xAxisₙ, yAxisₙ; Xₙ, Yₙ; Gₙ, Bₙ; Pₙ} of one originating co-constitution — simultaneous, inevitable, coinherent, coreliant; each element obtains iff all obtain. Rₙ is the simultaneous object; "chain" names the epistemic derivation path (this document). Process-words for the constitution are struck.
- **0ₙ** — operational absence: a limit-approach along Gₙ, not a point. **1ₙ** — the frame's unit [measure]: the conserved product of the frame's one constitutive pair = its minimal distinction (one claim; see Premises). **Typing guard:** 1ₙ is the within-frame invariant — it defines the frame's level set and is never a variable coordinate within it; formally distinct from **c**, the family parameter (XY = c), which genuinely varies across the lineage direction.
- **Xₙ, Yₙ** — the two co-obtaining mode-variables, uninterpreted.
- **a, b** — split coordinates: a = (X+Y)/2, b = (X−Y)/2 [amplitude; a scaled rotation — conformal, so angle facts transfer; lengths rescale by √2].
- **Gₙ** — the gradient: Xₙ·Yₙ = 1ₙ ⟺ Q_j := a² − b² = 1ₙ. **Bₙ** — the balance axis: X = Y. **Mₙ** — the mediator: a = 0.
- **Oₙ** — the structural origin. **Dual type [derived]:** O-as-point (the inward journey's unmet crossing) and O-as-axis (the outward journey's unmet asymptote — a line through Oₙ). The frame's local face of P₀.
- **Pₙ** — the balance paradox at (√1ₙ, √1ₙ) [rendering coordinates], ν(Pₙ) its conjugate; **the seats.** Entailed by continuity; cannot obtain as a gradient-position (constitution); cannot be arrived at (divisibility) — see the engine. "Met" is frame-reading/rendering status only; "occupied" is ill-typed and does not appear.
- **σ_B** — exchange X ↔ Y (= the inversion x ↦ 1ₙ/x on G). **σ** — single-sign mirror (X,Y) ↦ (−X,Y). **ν** — conjugation (X,Y) ↦ (−X,−Y).
- **j** — split unit, j² = +1. **i** — complex unit, i² = −1 (derived; two independent routes, § orbit algebra).

## Foundational conditional

If reality is infinite — **infinitely vast and infinitely divisible**: no maximum extent, no minimum scale — the chain derives what follows. The two-directional reading is the *definition* of "infinite," not a claimed consequence.

## Excluded terminal condition; the ground; the two infinities

P₀: absolute indistinguishability. What is not distinguishable does not obtain (the Bridge — ontological). P₀ **cannot** obtain — modal, not contingent; **an ontological-logical impossibility, not nothing** (see Notation).

**P₀ as sign-collapse [derived; verified]:** absolute indistinguishability includes the mode's indistinguishability from its own negation — the fixed-point condition of ν. In the frame's plane, ν has exactly one finite fixed point: **Oₙ = (0,0)** — unoccupiability derived as fixed-point-of-conjugation. On the orbit, ν acts **fixed-point-freely** (the half-turn): **admissibility = ν-freeness**; the exclusion of P₀ ⟺ conjugation has no fixed obtaining point. **Algebraic face [r6]:** P₀ includes the condition − = + *in all ways* — the identity −1 = 1, ν degenerate to the identity map, every conjugate pair (x, −x) collapsed. Frame-level, the equation −x = x has one solution (Fix(ν) = Oₙ, verified); P₀-level, the identity is globalized.

**Face-unity without compactification [derived; verified].** On G, the outward journey in one mode *is* the inward journey in the co-mode (X·Y = 1ₙ is the exchange rate between the two non-terminations); the outward arm converges to the mode axis — a line through Oₙ — never merging. Inward's unmet object = **O-as-point**; outward's unmet object = **O-as-axis**. |0| and |1| are the two **counterfactual terminals** of this one journey — what each direction would collapse to *if it terminated* — denied respectively by the conditional's two halves (divisibility, vastness). ∞ is a symbol of non-termination, not a value; |1| ≠ ∞; the projective ideal point is a *rendering* of the saturation-condition (lawful section, not ontology).

**The two infinities exchange, pivoting on the seat [derived; verified].** The inversion x ↦ 1ₙ/x — which on G is σ_B itself — swaps the divisible center and the vast extension; its fixed point is x = √1ₙ: **Pₙ is the balance point between the two non-terminations.** A single mode cannot disambiguate its own ends ("x→∞" is intrinsically ambiguous — the RSM chain lists this as the third entailed inexpressibility, *directional*); the co-mode is the chart at the other end and the conservation law is the transition function — **the two-mode requirement has a second, informational ground (the parsing argument), independent of the ontological one** *[candidate]*. "x→∞, y→∞" is impossible within any gradient (conservation forbids) and names the trans-scale direction along B: **B is the scale-axis; c = XY the lineage parameter** (c→0 child-ward, c→∞ parent-ward along a lineage; a lineage coordinate, not a global count — see index-relationality) *[candidate]*.

**Because P₀ is impossible, O₁** — existential import + the Bridge + P₀'s impossibility; no new premise; modal: the alternative to the first distinction is not unactualized but impossible, so the first distinction is necessary given obtaining. Identity through opposition; not parturition. Proportion **Pₙ : O₍ₙ₊₁₎ :: P₍ₙ₋₁₎ : Oₙ :: P₀ : O₁** — ratio invariant, mechanism distinguished; each pair (n ≥ 1) is one locus under two frame-readings (met-from-without, unmet-from-within); the first pair alone has no locus on its P-side. *[derived]*

**Index-relationality [derived].** The frame-index is gauge: 1(n-1):trunk :: 1n:branch AND 1n:trunk :: 1(n+1):branch hold simultaneously of the same pair — relabel every frame n → n+k and no structural fact changes. Subscripts name *relations* (parent-of, child-of), never positions on an absolute count. Forced twice: primarily by **arborescence** — the nesting is a tree, not a ladder (multiple seats per frame; indices are path-dependent; no global generation-number exists across branches; "the ladder" is the single-lineage slice of the tree, a rendering artifact); secondarily by **recession** — even along one lineage, the count back to the ground is uncompletable in principle. Consequence: the scale parameter c = XY is always expressed from within a frame reading its own unit as 1 — a lineage coordinate, never the tree's own. (Companion to the proportion: ratio-invariance + index-gauge.)

## The frame Rₙ; the coinherence; the unit

The coinherence, joints biconditional, read in derivation order (ontological simultaneity, epistemic order): P₀ → O₁ → (X₁,Y₁) → (G₁,B₁) → P₁. Because Oₙ, Gₙ; because Gₙ, Oₙ.

**The reciprocity joint — DERIVED (r7; formerly the audit head; machine-ruled under delegation, unaudited — Will's adversarial read required before sealing).** Four steps: (1) **Co-obtaining (P2):** the two sides of a distinction co-constitute; neither can reach a terminal while the other obtains — a one-sided distinction is no distinction. (2) **Exchange symmetry:** the sides are co-equal by co-constitution; the law relating them is symmetric under X ↔ Y. (3) **Class selection [verified]:** the additive class violates (1) — X + Y = c admits zero-solutions, so one side can reach its terminal while the other persists; the multiplicative class satisfies (1) — XY = 1ₙ admits none; the exclusion is carried by the operation itself. (4) **Uniqueness up to reparametrization:** any terminal-excluding symmetric law f(X)f(Y) = c is a monotone reparametrization of the conserved product (log-transport); the product form is the law read in the modes' native magnitudes, and 1ₙ is *defined* as that conserved product. The joint derives; the audit relocates to this derivation itself, to R8, and to Definition T.

**A frame is Rₙ: the coinherence of one constitutive pair** ("the," not "any"). Hence 1ₙ is automatically the frame's minimal distinction: anything finer is itself a distinction, and every distinction constitutes a frame (**the Corollary** — from the Definition of Rₙ plus the coinherence; formerly Lemma L1, struck as double-counting: it was the coinherence wearing the Definition's name; "unframed" is ill-typed, not forbidden) — sub-1ₙ fineness *is* frame n+1. **Postulate F: dissolved into the Definition.** *[derived given the reciprocity joint]*

## Point as limit; Definition T

Positions are approached, not terminated: density, not completeness. **Definition T:** a traversal is a continuous path of obtaining positions [explicit register] — definition-level, motivated by the no-jump principle. *[given T]*

## THE ONE LAW AND ITS TWO SLICES (organizing principle)

Over ℂ, one equation — the frame's unit modulus **Q(z) = 1ₙ** — with two real slices: b real → **Q_i = a² + b² = 1ₙ** (the circle; the orbit); b ↦ ib → **Q_j = a² − b² = 1ₙ** (the hyperbola; the gradient). Parametric face: cosh(iθ) = cos θ. **The gradient is the orbit** — one structure, two slices; the Euclidean drawing renders one slice at a time; the distinction is a slicing artifact. **The rendering heals at the seats:** the slices intersect exactly at {b = 0, a = ±√1ₙ}, tangentially, equal curvature, opposite bending (mirror-osculation as the coincidence's curvature signature). With u = a², v = b²: Q_i = u + v, Q_j = u − v. On the orbit, **Q_j = 1ₙ·cos 2θ** (double cover): the invariant returns at π (on ν — return-of-structure), the position at 2π (return-of-place) — **two distinct returns**. *[verified]*

**The Amplitude Floor [derived; verified].** On the primary gradient slice Q_j = a² − b² = 1ₙ, the amplitude coordinate is bounded below: **|a| ≥ √1ₙ**, equality exactly at the seats (b = 0). (Conjugate family Q_j = −1ₙ: the floor binds |b|, symmetrically.) The frame's invariant √1ₙ is a hard floor for the coordinate; the seat is the unique tangency where the coordinate meets the invariant without slack. (Consolidates floor-tangency and the a² = 1ₙ + b² rearrangement into one citable statement.)

## Theorem 1 — Mode disconnection / cross-slice connection

Two faces of one theorem. **(i) The wall:** {Q_j = 1ₙ} has exactly two connected components; Pₙ, ν(Pₙ) in different components; e^{jφ} preserves components — no motion within the gradient slice connects the modes. Pure topology; register-clean. **(ii) The door:** the complex object Q(z) = 1ₙ is *connected*; the joining path runs through the other slice — the circle-path Pₙ → m₊ → ν(Pₙ), through the conjugate seats. "A second register is required" = the connection exists, in the i-slice. The orbit's necessity is derived, not posited. *[derived; verified]*

## Theorem 2 — Forced degeneration (the toll)

Every continuous spanning path avoiding Oₙ crosses the null cone into Q_j < 0: on the minimal traversal, Q_j = 1ₙ·cos 2θ reaches **−1ₙ exactly at the conjugate seats m±** — spanning must visit the conjugate seats, where the native measure reads negative-unit. A measure that reverses sign on every admissible traversal cannot measure traversal. Structure of Theorems 1–2: **wall (T1-i), door (T1-ii), toll (T2).** *[derived; verified; explicit layer, lawfully — see Layering]*

## The measure: derived, not postulated (Postulate Q struck)

The traversal measure M is forced to be Q_i in four steps, each from ruled material:

1. **Parity (ν):** M(−z) = M(z) — kills all odd degrees. *[P₀-as-sign-collapse]*
2. **Scale-covariance** (formerly “ladder-covariance”; renamed per index-relationality — a within-lineage dilation)**:** under the trans-scale move (uniform dilation) the unit transforms as 1ₙ → λ²·1ₙ; commensurability = M transforms as the unit: **M(λz) = λ²M(z)** — degree-2 homogeneity forced among *all* functions (mixed degrees warp the measure/unit relation across magnitudes; verified). *[Will's closure]*
3. **Quadraticity (the Finsler hole — named, only CONDITIONALLY closed; adversarial pass r1):** degree-2 homogeneous ≠ quadratic form — counterexample M = r²(2 + cos 4θ) passes parity, exchange, covariance, and positivity yet fails the parallelogram law (verified; the Riemannian-vs-Finsler freedom). RETRACTED (pass r1): the counterexample equals (3a⁴ − 2a²b² + 3b⁴)/(a²+b²) — a rational function of frame objects, hence FRAMED; the Corollary does not exclude it (nor did the former L1 — this is where the fourth load died). What genuinely closes: among POLYNOMIALS, degree-2 + σ_B leaves only αa² + γb² (no escape); the classical closure (C² at the origin ⇒ quadratic) is structurally unavailable — the origin is Oₙ, excluded; the Finsler freedom lives in the excluded point's smoothness. **Resolution (r7; machine-ruled under Will's delegation): the residue dissolves by re-typing.** "Which M among all candidates?" imports classical metric-selection — a search over function space the framework does not contain. There is one law, Q(z) = 1ₙ; registers are its readings; **M = Q_i is the traversal-register reading of the one law [definitional-by-register]**. The elimination above demotes to corroboration (no framed polynomial rival survives the constraints); the Finsler freedom is a rendering-register fact, living in the excluded point's smoothness. The "generation principle" is ditched as a commitment — it was the register-discipline fact wearing premise's clothing. **Q: struck unconditionally.** *[definitional + corroborated]*
4. **Selection (Theorem 3):** by the exclusion, M > 0 at every obtaining traversal point; the all-directions lemma (a spanning path in the punctured plane sweeps ≥ π of directions) forces positive-definiteness — of the two slices, **positivity selects Q_i.** *[derived; verified]*

**One selection, three languages:** Theorem 2 (measure: the split form degenerates on spanning), this section (form: positivity picks the i-slice), and Route 1 below (algebra: the elimination discards j as the spanning unit) are **one event thrice expressed.** *[consolidation]*

## Frame-Universality: dissolved

All three FU outputs are recovered without it. **Kernel → theorem:** σ (the mirror, primary→conjugate family, Q_j → −Q_j) leaves Q_i exactly invariant — sign-blindness is a property of the selected slice, computed, not committed *[verified]*. **Calibration → inheritance:** the complexified law arrives as a² + b² = 1ₙ; α = γ = 1 and the unit level are set by the law; nothing to calibrate. **Universality → definition:** "every conserved-product structure is a frame" is the frame-definition applied at any constitutive magnitude, plus the conserved-product family. Four-seat magnitude equality: computed (Q_i = 1ₙ at all cardinal phases). **Survives separately: the seating commitment** (the conjugate mode seated as a point of Gₙ). The derived metric is global to the frame's plane at the slice's price (the former metric lemma stays closed; the exposure moves to R8 + the reciprocity joint). **Concentration risk, named:** F, Q, and FU all dissolve through the same core — the slice identity + the Definition of Rₙ + the reciprocity joint. The leanness is real and leveraged; if R8 or the reciprocity joint fails audit, three postulates re-inflate. Lean ≠ robust; the auditor package says so in these words.

## Standoff and minimal traversal (F-free)

The interior of the unit disk contains **no obtaining positions of frame n** — not floor-forbidden but excluded-by-constitution: sub-1ₙ fineness is frame n+1, not a location here (definitional, via the Corollary). There is no *through*, only *around* — the exclusion is a whole-frame (enclosure) fact, not a local obstruction at the center. Traversal is defined only on obtaining positions ⟹ only on the closed exterior. The minimal traversal — the tightest loop still composed of obtaining positions — is the unit circle about Oₙ, radius √1ₙ [amplitude], through the four seats; unique up to orientation. **Floor tangency [verified]:** on Gₙ, Q_i = 1ₙ + 2b² ≥ 1ₙ, equality exactly at ±Pₙ — the slice-coincidence seen in the derived metric. Order: signature → form → floor → traversal. *[derived given T and the reciprocity joint]*

## The orbit algebra: i² = −1 — a two-paths pair

**Route 1 (elimination):** with Closure and Spanning, i² ∈ {1, 0, i, −i, −1} eliminates to −1. *(Precondition grounded [r6]: the elimination presupposes −1 ≠ 1 — the four-cycle requires its elements distinct. Not an assumption: −1 = 1 is P₀'s algebraic face, excluded in every frame because P₀ cannot obtain. Characteristic-2 structures — where −1 = 1 is coherent mathematics — are exactly the orientation-degenerate ones: no four-cycle, no winding, no ordered traversal; the framework's exclusion and mathematics' degeneracy classification coincide.)* Continuing (i² = 1 is the split register — *exists*, but degenerates on spanning, Thm 2; 0 has no inverse; ±i collapse); the 4-cycle {1, i, −1, −i} closes, four phases minimal. Route 1 is the **algebraic face of the one selection** (see Measure §). *[derived; verified]*

**Route 2 (antipodal binding):** +1 requires −1 (co-constitution of unity); the line-path between them passes through the excluded center; the only continuous binding is antipodal — and two quarter-turns compose to the half-turn: **i² = −1 as composition of turns** (R(π/2)² = −I, verified), no multiplication table consulted. *[derived; verified]*

**Independence: deletion-test passed both ways** — Route 1 never uses co-constitution of ±1; Route 2 never uses closure/spanning; they share only the ground-level exclusion. **Declared a two-paths pair** — the second instantiation of the framework's strongest internal-parallax structure (first: the ground). i² = −1 is **promoted**. **Schema note:** Route 2 is the k = 1 instance of the binding-extortion schema (conjugate binding on a line extorts the second direction) whose k = 2 instance is the dimension-three argument (σ-binding extorts the third); the schema should be written once, abstractly — the auditor validates it once and both instances come; the plane is not posited but extorted. *[candidate]*

## The seat-phase structure

**The founding observation (the project's first result; elementary form).** At Pₙ the gradient's tangent slope is −1; Bₙ's slope is +1; product (−1)(+1) = −1 — the perpendicularity criterion met exactly. The slopes are the conjugate pair {−1, +1} (Route 2's pair, as directional data at the generation site) *[observed]*. Off-seat: slope product σ(x₀) = −1ₙ/x₀², = −1 iff x₀ = √1ₙ; **deficit σ(x₀) + 1 = 1 − 1ₙ/x₀²** — identical to the tangent(G)·B form; perpendicularity obtains at one locus and degrades quantifiably away from it. **The founding realization — something structurally distinguished must happen at Pₙ — as a two-line calculation checkable with first-semester calculus.** *[derived; verified]*

**seatₖ = √1ₙ·iᵏ**, k = 0..3: Q_i = 1ₙ constant, Q_j = (−1)ᵏ1ₙ alternating (double cover in the enumeration); the i-cycle **is** the enumeration of generation sites; axis-instantiation exactly at the cardinal phases. *[derived; verified]*

**Seat characterization by register alignment [candidate]:** Pₙ is where coordinate (1), unit (1ₙ), and phase (+1) coincide at unity; where the two slices coincide; where the incident slopes are {±1}; and where the two infinities exchange fixed (the σ_B/inversion pivot). Oₙ is where all read absence together. The locus where every register agrees is the generative locus; where all fail together is the origin.

## The complementarity floor [candidate theorem; closes open item 10 to candidate]

Over any admissible closed spanning structure — floor-compliant (r(θ) ≥ √1ₙ), ν-symmetric, measured by its winding coordinate — the split-coordinate spreads satisfy **σ_a·σ_b ≥ 1ₙ/2**, saturated uniquely by the minimal traversal. Proof: ν-symmetry zeroes the means; ⟨a²⟩ = ⟨r²cos²θ⟩ ≥ 1ₙ/2 by the floor, same for b; equality forces r ≡ √1ₙ. **Mechanism: floor + symmetry + winding measure — no transform, no noncommutativity; the framework's complementarity is a floor phenomenon.** Load-bearing and demonstrated by counterexample: the winding-coordinate measure (a seat-dwelling measure on the same circle violates the bound). Residuals: force the winding measure from isotropy (no preferred phase), or name it; extend beyond star-shaped structures. [verified: saturation, bound demo, measure counterexample — appendix 22.]

## The demand and the engine argument (the engine; L2 retired)

**Lemma A** *[near-definitional — RULED, accepted (Will, r7): straightness is register-relative — the child's axes are curved-in-parent, straight-in-child — so A reads "globally straight in the frame's own register," an analysis of "axis"]*: an infinite gradient obtains ⟺ a globally straight, independent axis-pair holds at its center, in its own register. Center classification: **O-type = straight×straight; P-type = straight×curved** (register-indexed). **The engine [restored r5]: a local paradox with two denials.** Demand: continuity entails the balance locus (frame-n fact). Denial A (constitution): a gradient-position is an imbalance-state; the balance point is the one position that is not one — equalization cancels the constitutive asymmetry; it cannot obtain as a position. Denial B (completion): divisibility denies arrival — no terminal position; every approach continues. Must exist; cannot obtain; cannot be reached — simultaneously, locally. Satisfaction in the one remaining mode — existence-as-organizing-locus: in-frame, circulation (the spanning structure is closed and winds the locus [skeleton]; traversal realizes it as ordered passage [explicit]; motion only after succession); trans-frame, generation — **the child is the denied approach continuing below the floor** (the sub-floor continuation is frame n+1; its origin is the approached, unreached center: O₍ₙ₊₁₎ = Pₙ as constitution, not as site-selection). The incident-pair deficit (locally orthogonal, globally curved-relative — the founding observation's deficit 1 − 1ₙ/x₀²) is Denial A's geometric face in the rendering: corroborating, not load-bearing. **Rendering guard (sixth predicted expression-failure):** the drawing shows a smooth curve through an unproblematic point; "entailed and cannot obtain" has no Euclidean rendering; the drawing's verdict must never overwrite the constitution's.

**The engine argument, rebuilt (former Lemma L2 retired as malformed):** "a demand at Pₙ within frame n" was a cross-frame sentence forced into one register — in the parent's language there is no demand (frame n is complete); in the child's it is already met. The corrected argument:
1. **THAT a child frame obtains — from the conditional:** infinite divisibility ⟹ no terminal frame downward; sub-1ₙ distinction obtains; its coinherence is a frame (Definition + Corollary). **C′ = Denial B, locked [derived, r7]:** both are P1's divisibility clause applied at the seat — positions-ever-closer are distinctions-ever-finer; below 1ₙ the Corollary types the continuation as the child. One application, two registers; not two arguments.
2. **WHERE and WITH WHAT — from uniqueness + Lemma A in the child's own register:** the child, a gradient's frame, requires a globally straight orthogonal pair at *its* center (A applied to an actual gradient-center; no transfer). The unique locus where the parent supplies an orthogonal pair is the seat (seat-phase uniqueness); Bₙ supplies one straight member; Gₙ supplies the other — curved in the parent's expression, straight in the child's (frame-relative curvature, the z² map). *[derived; verified]*
3. **The anchoring line [candidate; written; one residual]:** suppose a distinction obtains finer than 1ₙ — magnitude **δ, unindexed** (the index is *earned*, per index-relationality). (i) Finer-than is relational: δ vs 1ₙ needs one register. (ii) Comparisons are distinctions; distinctions are framed (the Corollary). (iii) The comparing frame must express both units; expression is by constitutive structure. (iv) The cross-frame expression mechanism is inheritance. (v) The unique locus where frame n supplies an axis-pair is the seat. (vi) So δ's frame draws its axes from n's structures at n's seat — and *thereby* earns the name frame n+1. (vii) A free-floating finer frame is a contradiction: no seeding → no shared register → no unit-relation → not finer-than-n, merely elsewhere. Sibling refinement: commensurability = shared ancestry. **CLOSED BY CONSTITUTION (r7, per Will's ruling 4a):** the child is what the center's existence is (the engine's satisfaction, trans-frame face) — seeding is not a further fact awaiting proof; "where does the child land" mis-posed what "there" means. Steps (i)–(vii) above and site-from-uniqueness demote to **corroborating geometry**. The holomorphy question demotes from premise-candidate to a rendering-level question about the z² map. Residual α (the unit relation) remains open — it is a genuine quantity, not a seeding question. *[derived-by-constitution; geometry corroborating]*

**B's dual type [verified]:** the child gradient, in parent coordinates, asymptotes to Gₙ and Bₙ — Bₙ is a crossable line in frame n and an unreachable asymptote in frame n+1; crossable-line and unreachable-limit cannot cohere in one expression; the recursion distributes the incompatible roles across two frames (met-from-without/unmet-from-within, extended to the axes).

## Recursion: componentwise parturition; the canonical map

**Frame-level form: Rₙ → Rₙ, R₍ₙ₊₁₎** — the whole-cluster statement; comma = severance-with-coexistence. Componentwise: **Gₙ → Gₙ + xAxis₍ₙ₊₁₎, Bₙ → Bₙ + yAxis₍ₙ₊₁₎** — mechanism-level; arrow = structural entailment across frame-readings; nothing consumed. **Recursion = P-type → O-type conversion by axis-generation.** O₍ₙ₊₁₎ is the parent's Pₙ. Bilingual statement per the structural chain. **The canonical map [verified]:** w = z² − 2i·1ₙ — parent-B → child y-axis, parent-G → child x-axis, seat → child origin; conformal; **2-to-1 with ν identified: the double cover is the parturition map**; parent Oₙ at the child's branch point, depth 2·1ₙ. The family net {XY = c} ⊥ {X² − Y² = k} everywhere [verified]: the child's global orthogonality in parent coordinates. B as the scale-axis threads the generations (the c-ladder). **z²-uniqueness reduced [candidate].** The family net {XY = c} ⊥ {X² − Y² = k} IS the level-net of z² (Re z² = XY, Im z² = (X²−Y²)/2; the verified net-orthogonality = the map's Cauchy–Riemann equations). Requiring of a cross-frame expression map: (i) holomorphy (structure-respecting; R6's formal face — premise-candidate), (ii) ν-evenness (the pair expressed as one ⟹ φ = g(z²)), (iii) net-rectification (parent net → the child's straight coordinate net; z⁴ verifiably fails — G maps to a parabola) forces **φ = α·z² + β**: β fixed (seat → origin); **α = the cross-frame unit relation, open item 6.** The merged open item decomposes: "why holomorphic?" (small, R6-adjacent) + item 6 (pre-existing). *[appendix 23]*

## Conjugation classification; dimension

**ν-pairs:** bindable in-plane (orbit antipodes; the seat-circle). **σ-pairs:** not realizable in-plane (det −1; the quarter-turn binds the wrong points, mismatch scale-detectable); bound only by the half-turn through a third direction — **the third dimension extorted as the cost of the σ-conjugate structure** *[candidate; k = 2 of the binding-extortion schema]*. **Termination [candidate]:** in 3-space all required bindings are point-antipodal on connected loci, within SO(3); the extortion fires once: 2 + 1 = 3. Open condition: no orientation-reversing binding required in 3D. **Rival-derivation flag:** the |1|-cascade reaches three by a different route; *[two candidate derivations, unadjudicated]*; uniqueness open. **Isotropy:** no preferred orientation derived via the Corollary — any would-be external distinguisher must obtain, hence be a distinction, hence constitute an Rₙ, and a frame supplies relation, not preference (the old trilemma is a dichotomy); S¹ → S² [verified: seat-sphere √(2·1ₙ)] → ℝ³ locally. Scale-isotropy: the multiplicative law's own symmetry; the exclusion of the terminals carried by the operation itself (XY = 1ₙ has no zero solutions; X+Y = c does) [verified].

## Branching; irreversibility; no terminal frame

Off-seat generation fails the coordinate-pair requirement — branching, if real, routes through seats. Backward, every line grounds at P₀ : O₁; O₁-uniqueness open. Irreversibility: reversal requires P₀ to obtain at the locus — excluded; strict ordering is the structural content of succession; origins recede compoundingly. No terminal frame in any direction. *[derived]*

-----

## Verified-computation appendix

1. Founding observation: σ(x₀) = −1ₙ/x₀², = −1 iff x₀ = √1ₙ; ≡ tangent(G)·B = 1 − 1ₙ/x²; seat slopes {−1,+1}. 2. Seat curvature: radius √2·√1ₙ = |OₙPₙ| [mode]; center at 2Pₙ (mirror-osculation). 3. Circle regimes: r < √(2·1ₙ) none; = tangent at seats; > four points. 4. Family net orthogonal everywhere. 5. w = z² − 2i·1ₙ: axis images, ν-identification, branch point. 6. Slice identity: b ↦ ib exchanges Q_i/Q_j; slices meet exactly at seats, common tangent. 7. seatₖ = √1ₙ·iᵏ; Q_j = (−1)ᵏ1ₙ. 8. Two returns: invariant at π (on ν), position at 2π. 9. σ_y ∉ SO(2); quarter-turn binds wrong points; half-turn through z realizes the mirror. 10. Terminals excluded by the operation; scaling symmetry multiplicative-only. 11. Rotation about B: s² − r² = 2·1ₙ, apex at the seat. 12. Revolution surfaces: horn x² + z² = 1ₙ²/y² with seat-ring; B-cone; orthogonal ring; seat-sphere √(2·1ₙ). 13. Theorems 1–3, double cover, floor tangency (carried from r5). 14. Fix(ν): plane = Oₙ alone; orbit = ∅; completed line = two points [rendering register]. 15. Inversion x ↦ 1ₙ/x swaps 0⁺ ↔ ∞, fixed at √1ₙ; = σ_B on G. 16. Child gradient asymptotes to Gₙ and Bₙ (pullback, numerical). 17. Ladder-covariance kills mixed degrees; Finsler counterexample r²(2+cos4θ) passes the pincer, fails the parallelogram law (−8). 18. σ-invariance of Q_i (sign-blindness as theorem). 19. R(π/2)² = −I (Route 2); the five-case elimination table (Route 1). 20. Free-vortex profile v·r = const is a conserved-product curve [flow-register rendering of G; fluid premise from machine memory — verify against sources]; trunk-taper radius·height = const is the same curve [structure-register: the horn]. One law, two medium-renderings; complementary presence. Plucked-out guard: the exclusion is constitutive, not historical. 23. z²-net identity: Re z² = XY, Im z² = (X²−Y²)/2 (the orthogonal net as the map's level structure); z⁴ sends G to a parabola (net-rectification uniquely selects the square). 22. The complementarity floor: minimal-traversal saturation (σ_a·σ_b = 1ₙ/2); deformed-admissible bound demo (0.78 ≥ 0.5); seat-dwelling counterexample (0.11 < 0.5 — the measure premise is load-bearing). 21. Typed Euler identity e^{iπ}+e^{i2π}=0; general antipodal sum e^{iθ}+e^{i(θ+π)}=0; four-seat balance Σ√1ₙ·iᵏ=0 [observation register; Euler cluster; 0.9x-history flag].

## Premises (complete)

**The Conditional** (definitional: vast + divisible). **The Bridge** (ontological). **Definition T** (traversal continuity — under audit). **The seating commitment** (conjugate mode seated as a point of Gₙ). **The Definition of Rₙ + the Corollary** (no unframed distinction; carries the floor-dissolution, the isotropies, and child-existence). **The reciprocity joint: DERIVED r7** (four steps, above; machine-ruled under delegation — unaudited). **Q: struck unconditionally r7** (M = Q_i definitional-by-register; elimination demoted to corroboration; the generation principle ditched as mis-typed). **FU: struck r2, standing.** Previously: F dissolved; L1 struck (r6, double-counting); L2 retired (malformed); Postulate R deleted. **Current count: Conditional + Bridge + Definition T + seating, with the fresh joint-derivation, R8, and T as the audit heads.**

## Open items, ranked

1. **Audit the r7 closures** — the reciprocity-joint derivation (esp. step 4's "native magnitudes"), the Q re-typing (definitional-by-register), the C′=Denial-B lock, and anchoring-by-constitution: all machine-ruled under delegation in one session; each reversible; Will's adversarial read is the gate.
2. *(closed r7 — the generation principle ditched by re-typing; see the measure section.)*
3. **R8-dependency check** — the slice identity now underwrites Q's quadraticity, FU's dissolution, and the metric's globality; audit it as the second head (with the concentration risk stated).
4. Dimension three: adjudicate the rivals (binding-extortion schema vs. |1| cascade); write the schema abstractly; then uniqueness.
5. **Cross-frame expression** — anchoring closed by constitution (r7); holomorphy demoted to a rendering-level question about the z² map; residual α = item 6.
6. Cross-frame unit relation 1₍ₙ₊₁₎ (the lineage parameter gives the direction; the relation open).
7. Definition T: theorem-grade no-jump argument, or definitional.
8. Tree-question: is the curved-relative deficit's *form* changed in the 3D (line-meets-surface) register? (Slice-relative ≠ deflated — Thm 1 precedent — but the rotated form is unrun.)
9. Branching beyond seats; uniqueness of O₁. (Arborescence is now load-bearing for index-relationality — if generation proved strictly linear, the primary forcing reverts to recession alone.)
10. Quantitative complementarity: **closed to candidate** (the complementarity floor, σ_a·σ_b ≥ 1ₙ/2). Residuals: the winding-measure premise (force from isotropy or name); the non-star-shaped extension.

-----

*v7.7 r2 — integrates the Nodes 6–11 walk and the P₀/infinity results into r1. Changes: Theorem 1 restated as two faces (wall/door; cross-slice connection through the conjugate seats); Theorem 2 as the toll (conjugate-seat visit; hygiene flag replaced by the layering map, installed in the header); **Postulate Q struck** (parity + ladder-covariance + slice-quadraticity closing the named Finsler hole + positivity; one-selection-three-languages consolidation); **FU dissolved** (kernel = σ-invariance theorem; calibration = inheritance; universality = frame-definition; seating commitment survives; concentration risk named); standoff re-grounded F-free (no obtaining positions in the interior; enclosure register; no through, only around); i² = −1 promoted as a **two-paths pair** (deletion-test passed; Route 1 = the selection's algebraic face; Route 2 = k = 1 of the binding-extortion schema); **L2 retired** and the engine argument rebuilt (existence-from-conditional + site-from-uniqueness + A-in-child-register; anchoring line open); B's dual type verified (asymptote of the child gradient); P₀-as-sign-collapse installed (Fix(ν); admissibility = ν-freeness); face-unity without compactification (counterfactual terminals; O's point/axis dual type); the two-infinities exchange fixed at the seat (parsing argument as second ground for two modes; B = scale-axis, c-ladder); seat-characterization extended; appendix grown to 19 items; premise list reduced to Conditional + Bridge + T + seating + L1. r3: index-relationality installed (subscripts as relations; tree primary, recession secondary; ladder demoted to lineage-slice; c renamed lineage parameter; scale-covariance renamed); Q downgraded to conditional per adversarial pass r1 (Finsler counterexample framed; generation principle named); Lemma A softened; anchoring line written (δ-notation; residual merged with z²-uniqueness); open items restructured; whirlpool/tree complementarity and typed-Euler appendix items added. r3 addendum: complementarity floor added as candidate theorem (open item 10 → candidate; appendix 22). r4: Amplitude Floor named (Gemini Edit 1, register-cleaned and symmetrized); 1n typing guard with the c-distinction (Gemini Edit 3, refined); z2-uniqueness reduced (holomorphy + nu-evenness + net-rectification; alpha = item 6; appendix 23); anchoring residual decomposed. r5: the balance paradox restored (demand / two denials / satisfaction-in-mode, per the both-horns ruling); P re-typed in Notation; the child as the denied approach continuing (C-prime identified as Denial B global face); the rendering guard installed; the anchoring line flagged as possibly closing by constitution (ledger). r6 (consolidated propagation pass): L1 struck via the definitional move (Definition of Rn + Corollary; audit head relocated to the reciprocity joint — spelled out to avoid collision with this chain's Definition T; RSM-chain rename queued); the stale fourth load corrected (unframed-measure exclusion was already dead since pass r1 — the quadraticity check resolved); Rn installed (Notation, architecture, frame section; frame-level recursion form Rn -> Rn, R(n+1)); cascade struck (coinherence; type split); P0 re-typed as ontological-logical impossibility (modal ground; failure-language struck); P0's algebraic face added (-1=1 in all ways; Fix(nu) already carried the frame-level fact) with the Route 1 precondition grounded (char-2 corroboration); directional-inexpressibility pointer added at the parsing argument. r7 (the rulings pass): reciprocity joint DERIVED (four steps; machine-ruled under delegation, unaudited); generation principle ditched by re-typing (M = Q_i definitional-by-register; Q struck unconditionally); C-prime = Denial B locked; anchoring closed by constitution (site-from-uniqueness demoted to corroboration; holomorphy demoted to rendering-level); Lemma A near-definitional tag accepted; premises and open items restructured — audit heads now: the fresh r7 closures themselves, R8, Definition T. Not sealed; the r7 closures are explicitly gated on Will's adversarial read.*
