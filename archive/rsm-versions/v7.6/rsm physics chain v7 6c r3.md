# RSM — Just-Physics Chain (v7.6-candidate, r3)

Convergence register. Physics vocabulary names structural facts the derivation has independently established. The convergence is the evidence; physics is one of three registers. Aligned to math chain r4. Status per CAVP: *[derived]*, *[candidate]*, *[falsifiable]*. Entries tagged **[verify against sources]** cite standard results from machine memory and must be checked. Machine-drafted (Claude, Fable 5).

Referent discipline, carried throughout: the unoccupiable center (orbit center, rest point, confinement axis) is **Oₙ**; the paradoxical-generative center (site of generation) is **Pₙ**; every Oₙ is some parent's Pₙ. Where earlier drafts said "forbidden center," read Oₙ unless the context is generation.

-----

## Foundational conditional

If physical reality has no terminal resolution in either direction, then neither a minimum length nor a maximum extent obtains.

## Excluded terminal condition; the ground

Two faces of P₀: a terminal minimum scale (a Planck floor read as an absolute smallest length) and a terminal maximum extent — both structurally excluded. Operational absence and operational distinction are asymptotic limit-states, not termini; the vacuum is not absolute nothing; apparent scale-limits are frame boundaries. *[derived]*

**The ground, physically.** Because P₀ is incoherent, O₁: the exclusion is generative. The vacuum is not absolute nothing *and cannot be* — so structure obtains. The ground has no physical instance as an observable event: observation is frame-internal, frames are post-ground, so every empirically accessible origin is an n ≥ 1 parturition (see recession). Physics never sees the ground; it sees the ratio re-enacted. *[ground derived in math chain; vacuum reading candidate; register rule derivation-adjacent]*

## Inverse conservation: the Gₙ form

Xₙ · Yₙ = 1ₙ. Two clean instances as equalities: wavelength × frequency = c (EM radiation in vacuum) *[good fit]*; pressure × volume = constant (Boyle's law, fixed T) *[good fit]*. The full ideal gas law is three-variable, not an instance except isothermally *[retained correction]*. **Branch honesty:** the two-component structure of the conservation locus (Theorem 1) is inhabited on both branches only in the oscillator phase-space instance below; in the Boyle and λf instances the conjugate branch is not physically realized, and the disconnection theorem has no footprint there. *[honest accounting]*

## Bound relations and the unoccupiable center

The uncertainty relations are floors, not equality conservations: Δx·Δp ≥ ℏ/2, ΔE·Δt ≥ ℏ/2. The zero-product position — simultaneously precise conjugates, the classical rest point — is **Oₙ**: required as the center of symmetry of the conjugate pair, unoccupiable in fact; the floor is the standoff Q ≥ 1ₙ about Oₙ, with 1ₙ :: ℏ/2. Equality conservation and bounded floor are distinct structures. *[derived: center and standoff, modulo the math postulates; operator identifications candidate]*

Unoccupiable centers across regimes: quantum ground states non-zero in energy *[candidate]*; GR singularities as centers the theory gestures toward but cannot inhabit — though under the big-bang reading below these may sort as Pₙ-type (generative) rather than Oₙ-type *[candidate; sorting open]*; tokamak plasma excluded from the field-axis centerline *[candidate]*.

## The oscillator instance — the cleanest single convergence

*[candidate; verify against sources]* The corrected geometry is the standard phase-space geometry of minimum-uncertainty states: the i-flow is free evolution (phase rotation about the origin); the j-flow is the squeeze (x ↦ eʳx, p ↦ e⁻ʳp), orbits the hyperbolas xp = const, two branches; both generators sit in Sp(2,ℝ) ≅ SU(1,1). Oₙ is the classical rest point, excluded by the floor; zero-point motion is the minimal orbit about the point the system cannot occupy. Quadrature variances oscillate at 2ω — the cos 2θ double cover as textbook physics. Mode disconnection is operational: no squeeze protocol connects the two branches of xp = const; the rotation flow does. If it holds against sources: both algebras, the floor, the unoccupiable center, the orbit, the disconnection, and the double frequency in one standard structure.

## Mass and energy

E = mc². Within a child frame, mass and energy as the two modes, c² as the conserved unit. An observed correspondence, not a derived inverse-form conservation. *[candidate; observed]*

## Lorentz form; the two signatures

The split-complex parameterization gives a² − b² = 1 — the 1+1 invariant interval; xy = 1 ↔ X² − T² = 1 exact. *[derived]* The two-signature division now has its necessity derived: **why the traversal must leave the Lorentzian register** — every spanning path crosses the null cone into Q_j < 0 (Theorem 2); **why the replacement is Euclidean** — any measure positive along a spanning path is positive-definite (Theorem 3). In the physical reading the Lorentzian interval and the Euclidean spatial slice correspond to the gradient's and the orbit's algebras. *[two-signature structure derived modulo Q, R; physical identification candidate]* Time enters through the signature, not as a fourth spatial axis: space is orientation, time is traversal. 3+1 physical significance open.

## Time, the ground, recession, and the big bang

Pₙ → Pₙ + O₍ₙ₊₁₎ is irreversible; cleanest at the ground (reversal of P₀ : O₁ is the obtaining of P₀); the arrow of time is this irreversibility; the ordering is linear succession. *[derived]* **The parturition form is co-presence:** parent regimes persist alongside their children — the cosmological regime is not consumed by the galactic regimes within it; nesting is co-presence, not succession, which the old replacement notation contradicted. *[coherence]*

Each generated origin is constitutively unreachable from within the frame that produced it; separation compounds per generation. Candidate reading: accelerating cosmological recession as the structural recession of origins, not the expansion of a substrate — recession without an expanding medium, distinct from a dark-energy term; strongest cosmological correspondence, a candidate, distinguishing signature against ΛCDM open. *[candidate]*

**The big bang :: Pₙ → Pₙ + O₍ₙ₊₁₎** *[candidate]* — a parturition instance, not the ground: the observable universe's origin is an O₍ₙ₊₁₎ born at a parent frame's Pₙ, parent persisting. The initial singularity is the parent's Pₙ read from within the child — constitutively unreachable, a boundary rather than an event, matching general relativity's excision of the initial singularity from the manifold (t → 0 approached, never reached as an event). The framework does not explain the singularity away; the standard formalism already treats the origin as the parturition form requires — the strongest element. Structurally sorts cosmogenesis model classes: the persisting-parent class (nucleation within eternal inflation; black-hole cosmogenesis) over absolute-beginning models. *[manifold-excision fit candidate; model-class sorting pattern-level]*

## Locally flat, globally curved

GR's local-Minkowski / global-curved structure is the spacetime-scale instance of the chain's locally-flat / globally-curved dynamic; the local spatial metric is the generated orbit metric, not an imported Euclidean structure. *[candidate; strong]*

## Three-dimensional substrate at every scale

Physical space is ℝ³ at every regime's scale; the orthogonality of local spatial axes is the derived orbit metric. Recursion produces new coordinate systems, each its own ℝ³, at parent regimes' paradoxical centers — not higher-dimensional structures. Three necessary; uniqueness open. *[derived: three necessary, local metric derived modulo postulates; uniqueness open]*

## Recursion across regimes

Regimes nest as a branching structure of new coordinate systems at parent regimes' paradoxical centers, all co-present with their parents — cosmological, galactic, solar-system, planetary, chemical, molecular, atomic, subatomic, quark. No regime terminal: forward, no smallest or largest; backward, every regime-line grounds at P₀ : O₁, and the ground is not a regime. *[derived structure; branching-at-any-position under test]*

## Toroidal structures under privileged gradients

Where a single gradient operates and direct crossing is forbidden, persistent structure organizes around the Oₙ-centerline: tokamak confinement, magnetospheres, convection cells, vortex rings, accretion disks *[candidate]*; the gut (genus-1), the tree's pith axis with living tissue in toroidal flow *[candidate; "all life toroidal" open]*.

## Energy as misalignment cost

Energy as the cost of misalignment between a structure's symmetry and the privileged gradient at its scale: minimum at alignment, rising with misalignment, diverging at the forbidden crossing. Zero-point energy as the structural correlate of the inability to rest at Oₙ — and, through the ground, the local instance of the exclusion's generativity. *[candidate]*

## Probes and the parallax method

fMRI, CT, optical and electron microscopy, mass spectrometry — each a probe disclosing a property-specific aspect of one underlying structure; multiple probes constitute the parallax method applied to physical material.

## Six falsifiable predictions

1. No persistent physical structure without a paradoxical center. 2. Persistence proportional to gradient depth at boundaries. 3. Terminated recursion co-inherent with a hardened surface. 4. Power-law scaling between nesting levels. 5. At least three spatial dimensions necessary for structural persistence. 6. Energy cost of approaching any paradoxical center diverges. *[all falsifiable]* Prediction 4 is the most operationalizable and the recommended first sharp test. In predictions 1 and 6, "paradoxical center" reads as the unoccupiable center Oₙ.

## Methodological note

Physics vocabulary names structural facts the derivation established. Several correspondences remain convergence-register candidates and are tagged. The convergence across registers is the evidence; no single register is the evidence.

-----

*v7.6-candidate r3 — clean rebuild aligned to math r4. Oₙ referent throughout (strengthening every instance); two-signature necessity derived (Thms 2–3); oscillator instance as flagship candidate with operational mode-disconnection; parturition co-presence; the ground with its register rule; the big bang as parturition with the manifold-excision fit; branch-inhabitation honesty. Not sealed.*
