# v9.51 patch diff report (chain patch set 4)

Applied 2026-08-04. Base: v9.5, in place in `rsm/canonical/chains/v9.5/` (v9.5 is one commit
back, per the patch set). Source filed at `rsm/audit/register_update_2026-08-04.md`. Every edit
exact-match, asserted to hit exactly once.

## Diff, per chain

### Just-physics (`just_physics_v9_5.md` — PATCH 23 + stamps)

**§11 Quasiperiodicity** (heading unchanged), body replaced in full:

Old:
> If the cross-frame ratio is irrational, lineage traversal winds incommensurately — return
> without repetition :: quasicrystalline order, orbital resonance structure. Held light, and
> conditional on that relation existing at all.

New: the unit-step version — the frame's own unit of turn incommensurate with its own closure,
iterates dense, no point repeated; **the correspondence stands on the frame's own arithmetic,
with no cross-frame quantity required**; the stronger cross-frame reading retained as a second
sentence, explicitly gated on the cross-frame unit relation (open items). *The entry stops being
gated on an open item; the correspondence itself is unchanged.*

Title + closing line → v9.51.

### Just-math (`just_math_v9_5.md` — PATCH 24 + stamps)

**Theorems, Complementarity floor** — appended to the entry (floor claim, winding-measure caveat,
and the σ_a·σ_b inequality untouched, as instructed):

> The minimal traversal's four stations are not a chart accident: a distinction co-obtains with
> its conjugate, so a seat-inventory has even parity by constitution, and a three-seat
> configuration is an incomplete inventory rather than a forbidden arrangement. Four is the first
> closure that exists. The geometric argument — that a three-station circuit either transits the
> center or fails antipodality — displays the constitutional fact rather than establishing it.

Title + closing line → v9.51.

### Structural / Just-DDJ — stamps only

Titles + closing lines → v9.51. No content edits.

## PATCH 25 — confirmation

**SKIPPED.** The patch is conditional on an explicit ruling from Will in the task prompt; the
prompt carried none. The two-horns draft stays in the register (A-n12: RULED as a result, chain
placement AWAITING RULING) and is not flagged in the chain, per the patch set's instruction.

## Residual-phrase check (deliverable 3 — report only, not fixed)

- **"cross-frame ratio"** — one occurrence, `just_physics_v9_5.md` §11: *inside PATCH 23's own
  replacement text*, in the deliberately retained gated sentence ("if the cross-frame ratio is
  irrational…"). By design, not residue.
- **"conditional on that relation"** — zero occurrences in the four chains.

## Alongside (per PATCH 26 and the merge instruction)

- `rsm/canonical/chains/v9.5/README.md` retitled v9.51 with the one-line patch summary and the
  PATCH 25 skip recorded.
- `CLAUDE.md` canon block and "Working with RSM Content" item 1 → v9.51 (path unchanged,
  `chains/v9.5/`).
- `reports/open_items.md`: canon pointer → v9.51; the register update merged under the existing
  A/B structure as dated blocks (A-n1–A-n13 + the source's C-n1/C-n2, placed in Section A because
  they close with derivation; B-n1–B-n7), status tags preserved as written, with applied-notes on
  A-n1 (PATCH 24) and A-n7 (PATCH 23) and the A-n12 skip recorded; A4's dependency line updated to
  the post-PATCH-23 gating and cross-referenced to A-n8's nested-closures form.

## Untouched, still pending (unchanged by this patch set)

The share-noun (crotch grain) · the R5/R6 rewrites (flags stand) · the [M] re-tag policy · the
DDJ finalization items · the slip-image errand batch · the 為 survey · everything in the
register's A-n/B-n blocks marked OFFERED or editorial.
