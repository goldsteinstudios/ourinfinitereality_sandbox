# Chain patch set 3 — DRAFT SPEC (for Will's approval; nothing here is applied)

Drafted 2026-08-03 from `rsm/audit/session_consolidation_2026-08-02.md` Part 7.1 and the
late-session addendum. Base: v9.4.2. Convention: **[RULED — wording given]** = the consolidation
supplies approved wording; **[RULED — wording PROPOSED]** = the ruling is Will's, the sentence
below is Claude's draft and needs approval; **[NEEDS SESSION]** = drafting work, not mechanical;
**[AWAITING RULING]** = flagged, unruled. Sweep target lists are exhaustive (grep-verified against
v9.4.2).

---

## PATCH 13 — Transfer → Recursion (the addendum's pending fix)

**[RULED — wording PROPOSED]** (the fix is specced in the addendum; exact replacement sentences
below are drafts).

- Structural §7 heading paragraph: "**The two transfers.** A child frame is constituted by two
  readings of parent structure" → "**Recursion.** A child frame is constituted by **two
  readings** of parent structure" (the two bulleted lines already say "reads" and stand
  unchanged).
- Structural §7: "Under the two transfers that crossing is…" → "Under the two readings that
  crossing is…"; "The identity is derived from the transfers rather than posited beside them."
  → "…derived from the readings…".
- Structural §7, "The parent persists entire": "Nothing is transferred, consumed, or vacated"
  — **keep** (a denial, not transfer-language; listed so the sweep doesn't overcorrect).
- Math Theorems: "**The recursion (two transfers).**" → "**Recursion (two readings).**"; "No map
  realizes the transfers, and none is owed" → "…realizes the readings…"; "a function carrying
  parent points to child points would assert the transfer §7 denies" — **keep "transfer"** here
  (it names the denied thing); "Whether any chart-level map models both transfers at once" →
  "…both readings at once".
- Math engine: "per the two transfers (Theorems)" → "per the two readings (Theorems)".
- **"The constitutive share"** (addendum: "share" is also transfer-language). Occurrences:
  structural open item 3 and math open item 2 ("whether any quantitative relation accompanies the
  constitutive share of §7"). PROPOSED: "…accompanies what a child shares with its parent (§7)"
  — reuses §7's own heading, which survived R24-era scrutiny — or Will supplies a noun.
  [AWAITING CHOICE]
- Not touched: DDJ 法 entry "pattern-taking, not substance-transfer" (a denial); the structural
  strike-list's "Transfer-language for non-events" (the rule itself).

## PATCH 14 — |0|/|1| in the math dimension item

**[AWAITING RULING]** (addendum recommendation on the table): keep "the |1| cascade" as the
route's *name*; convert step-descriptions to word forms — "(the empty face cannot hold →
distinction → two sides; two sides infinitely separated → the full face cannot hold →
perpendicular turn; …)". One edit, math open item 3. Note the steps also say "perpendicular
turn" — under the measure gate that word may want "independent turn" or a caveat; flagging,
not proposing.

## PATCH 15 — R25: remove the P₀/Pₙ differentiator

**[RULED — wording PROPOSED].** "Ditch 'what survives the collapse.' … P₀ is the impossibility;
Oₙ/Pₙ are loci within frames. Different kinds."

- Structural §1, final paragraph ("The difference between the two scopes is what survives the
  collapse. At Pₙ the frame has no locus there… At P₀ there is no elsewhere…") — **delete**, and
  PROPOSED replacement carrying R25's positive content: "P₀ is the impossibility; Oₙ and Pₙ are
  loci within frames — different kinds, with nothing to differentiate. Oₙ is the general rule
  that O₁ follows, as 名可名非常名 is the general rule that 道可道非常道 follows." (The 名/道
  clause is Will's, recorded in R25; whether it belongs in the *structural* chain or only the
  DDJ chain is a placement call. [AWAITING CHOICE])
- Structural §1 also says "**Pₙ is the same species at frame scope**" — R25-compatible (species
  claim, not differentiator); keep.
- Math Notation, P₀ bullet: "P₀ and Pₙ are one species at two scopes; the difference is what
  survives the collapse." → delete the second clause; PROPOSED: "P₀ is the impossibility; Pₙ is
  a locus within a frame."
- Physics §2 "Where it would fail": "…temporal ordering survives the collapse of the before/after
  distinction" — different usage (a break condition, not the differentiator); **keep**, listed so
  the sweep doesn't overcorrect.

## PATCH 16 — R21: the instance set widens to the attested Ch. 2 pairs

**[RULED — wording PROPOSED].** Structural §1: "Where up equals down there is no height. Where
before equals after there is no time. Where void equals form there is no reality." → the
strip-attested inventory. **Working list must come from the Guodian A-bundle version, not the
received** (R21's own note). The dataset (slip A15 region) should be read for the attested pairs
before drafting; received ch. 2 gives 有無/難易/長短/高下/音聲/前後. PROPOSED shape: keep the
three current renderings and add the remaining attested pairs in the same "Where X equals Y there
is no Z" form — with 前後 (before/after) kept, since physics §2 builds on it (dependency
preserved per R21). [NEEDS SESSION for the six renderings' wording]

## PATCH 17 — R20: the notation warrant

**[RULED — wording given for the core; placement PROPOSED].** DDJ chain, register-declaration
section: replace the explanatory paragraph "That licenses the notation these chains use. 有 and
無 are names — 可名, an indexed instance. X and Y are not names of form and void; they are the
slots the two modes occupy…" with the demonstration:

> 又亡相生 :: XY = 1. Not that the DDJ is math, but that its two languages/frameworks explore
> the same dynamic.

(Core sentence is Will's verbatim from R20. The old paragraph ranked 有/無 as names below X/Y as
slots; on this version they are peers.) Surrounding-sentence stitching [NEEDS SESSION]. Note:
R20 uses 又亡 (the strips' graphs) rather than 有無 — consistent with the survey's finding that
the A-bundle writes 亡 for the absence word; keep the strip graphs.

## PATCH 18 — R17: the Mawangdui tag tier

**[RULED — mechanics PROPOSED].** DDJ chain headnote: "[G] marks what is attested in the Guodian
bundles (~300 BCE); **[M]** Mawangdui silk texts (~168 BCE); **[R]** received text only. Trust
order G → M → R; where text appears only in the received, it is taken as classically
interpreted." Then re-tag: current [R] entries that are in fact Mawangdui-attested become [M]
(the Ch. 1 lines 3–4 discussion already cites Mawangdui readings; the methodology lines cite
Mawangdui phrase-grouping). Which entries flip is a per-entry check. [NEEDS SESSION for the
re-tag pass; the headnote mechanics are ready]

## PATCH 19 — R7: parametric expression enters the chains

**[RULED — wording NEEDS SESSION].** New to all chains: "Only in the implicit frame is it a
perfect expression… recursion where the pattern expresses within finite parametric environments."
Placement PROPOSED: structural §0 (a fourth register paragraph, after "Accuracy and precision" —
it is the same family of fact: the implicit fixes structure, the parametric environment is where
expression happens) + one sentence in physics' headnote + the math chain's "What this chain does
not owe" (exponents differ *because* environments differ). Also resolves the many-nodes question
at the register boundary (one B/G/P implicitly; as many meetings as the trunk has, explicitly) —
worth one sentence in structural §5 or the open items. Open question for Will (Part 5): is the
parametric environment *derived*, or is it what "explicit" means? The wording should not
foreclose that.

## PATCH 20 — R8: the `::` drift sweep

**[RULED rule; per-instance dispositions AWAITING RULING].** R8: `::` compares two **explicit
expressions sharing the same implicit logic** — never an explicit expression with the implicit
structure itself. Grep-verified instances that pair a text expression directly with an
implicit-register object, for adjudication (fix pattern per R20: point both at a shared explicit
rendering, or re-type):

- DDJ: 道 :: **the recursive structural model itself** (the referent entry — the sharpest case);
  玄 :: Paradox :: **Pₙ**; 玄牝 :: **P₀**; 始 :: **Oₙ**; 母 :: **Pₙ**; 天地 :: **Gₙ**; 守中 ::
  **Bₙ**; 可/常 :: **the registers themselves** (this one may be exempt — it is the register
  *declaration*, and R19 confirms the split is Will's ruling independent of any key).
- Physics: **Pₜ :: now** pairs two explicit items (clean); **1ₙ :: ℏ/2** pairs an implicit unit
  with a physical constant — same question as the DDJ set.
- Not drift: math-internal uses (常名 :: XY = 1, 可名 :: XₙYₙ = 1ₙ) — explicit-to-explicit
  already, and the R20 model.

This patch should not be applied mechanically; it is the one place the spec recommends a
dedicated session. The alternative reading — that chain `::` entries *define* the anchors and R8
governs prose outside the chains — is also open. [AWAITING RULING on which reading governs]

## PATCH 21 — R6: the 有無相生 and Ch. 40 rewrites

**[NEEDS SESSION — flagged in place since v9.4.1].** 生 one form (woman → mother-woman, child);
相 = co-perception ("an eye sees both tree and not tree simultaneously"); mutual *generation*
imports a causal relation co-perception does not carry. The two `<!-- R5/R6 pending -->` comments
mark the sites. Note the 有無相生 entry's phrase "each is constituted by the other" may survive
(constitution ≠ generation); "arising mutually" in the Chapter-2 method paragraph likely does
not. Drafting is Will's.

## PATCH 22 — closing-line version stamp

v9.4.2 → v9.4.3 (or v9.5 if Will judges the R6/R7/R8 content a canon step rather than drafting
correction). [AWAITING CHOICE]

---

## Explicitly out of scope (consistent with the queue)

The open-items register regeneration (queue item 4, after this patch set) · editorial repairs ·
the R24 fineness/descent gap-check (WATCH item — surfaces during the revision, logged as found) ·
C1/C2/C3 (offered constructions, awaiting check — C1 if accepted would land in the math chain
near the measure section, giving the perpendicularity family its re-typing home).

## Sweep-verification note

All target lists above were grepped against v9.4.2 on 2026-08-03. Nothing in this spec has been
applied; the chains are untouched since the v9.4.2 installation.
