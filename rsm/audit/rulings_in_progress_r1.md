# Rulings in progress — positions stated, not yet ruled

**Date:** 2026-07-27 · **Status:** working document. Nothing here is canon and nothing here is a
ruling. It records positions Will stated in conversation, the arithmetic checked against them, and
what each would close or collide with — so the rulings get made from a written page instead of from
memory.

**Why this file exists.** Four substantive positions were stated in one session and existed nowhere
but a chat log. One of them would close the framework's largest open item. Losing that is the exact
failure the audit trail is for.

**Reading discipline.** Will's words are quoted verbatim and marked **[W]**. Arithmetic is marked
**[checked]** and is mine. What neither settles is marked **[open]**. Per the standing division of
labor, a preference must not be recorded as a derivation — so nothing below is promoted, including
the item that looks most like a closure.

---

## What is actually being asked — three questions, no notation

Everything below this section is backing. **The rulings are all questions about meaning, not about
computation**, and none of them requires reading the arithmetic. The three questions in full:

### Q1 — A word. (Backing: §1)

Two chains say `Pₙ` is "**occupiable** at resolution." The same two chains also say "occupancy
language does not type these loci." Both sentences cannot stand.

The *idea* behind the phrase is not in dispute — the balance really does hold at the frame's own
resolution, and only exactness is denied. That is already what v9 says. So the question is only which
sentence gives way:

- **Keep the rule**, and say "met at resolution" instead — the phrasing the math chain already uses a
  few lines further down; or
- **Keep the word**, and narrow the rule in writing — e.g. "occupancy language does not type `Oₙ` or
  `P₀`," leaving `Pₙ`-at-resolution describable that way.

*One thing to weigh:* "met at resolution" describes a **relation** — the two modes' amounts stand
equal, as far as the frame can tell. "Occupiable" describes a **place** — somewhere a thing could
sit. The chains deny anything sits there ("n has no spare pair to grant it"). The seating error was
exactly the slide from the first to the second, which is why the word is worth a deliberate choice
rather than a tolerance.

### Q2 — RULED: yes, same argument (2026-07-27)

**[W]:** *"it's the same argument. two points can't mirror BECAUSE you think you have a mirror but
then can always zoom in to find further distinction."*

So a mirror-claim **is** a claim about every depth, and falls to the same denial as equality carried
through every depth. The gap flagged below is closed: an involution between two loci is not a
different kind of object.

**What this settles.** T1's fork resolves away from "the second component is the chart's image of the
first." The two branches are genuinely two.

**What it obliges** (each needs Will's sign-off before any chain text changes):

1. **Structural §3 is now wrong as written** — "the sign belongs to the chart — an orientation of
   reading." If the branches are genuinely two and the sign is what distinguishes them, the sign is
   structure. `CLAUDE.md` carries the same sentence and inherits the fix.
2. **Math open item 9 closes**, or nearly — the signed extension is the derived second register.
3. **Both i² routes un-condition.** v9 tags them *"conditional on the binding requirement"*; that
   condition is met. Orbit-necessity, the minimal traversal, and the ν-centroid instances move from
   *rendering* back to *structure* — v9's own list of what falls if the sign is chart.
4. **Structural open item 6 (the involution) and the DDJ's `非` reading** both hang off item 9 and
   move with it.

**Both residuals closed by the follow-on ruling (same day).**

**[W]:** *"a perfect mirror is an impossibility. only by treating two points as end poles of a
diameter where the circumference represents all points equidistant from the shared center, can they
be perfectly symmetrical relative to their shared center."*

- **(b) — closed. Rotation is not an extra step.** The circle is not posited on top of the symmetry;
  it is what "equidistant from the shared center" *names*. Two points can be perfectly symmetric only
  as poles of a diameter, and the locus of all points at that distance from the center is the
  circumference. So the binding and the symmetry arrive together, and the earlier worry — that
  "therefore rotation" needed its own line — dissolves.

- **(a) — closed, and by a reason rather than a choice.** The follow-on says *why* the mirror fails
  and the antipodal does not, which only makes sense on the **strong** reading:

  | | what it asserts | why it can or cannot hold |
  |---|---|---|
  | **Mirror** (reflection) | a correspondence between the two things, feature by feature | the correspondence must hold at every depth; the descent never finishes; **never settled** |
  | **Central** (antipodal) | one relation each thing has to a shared *third* term — the center | a single relation, not an infinite conjunction; **can hold** |

  The mirror is not *disproven* by finding an asymmetry — it is never *established*. That is exactly
  v9's existing form for the balance, and the weak reading is not needed. **(a) is answered.**

**[checked]** — the two involutions are not the same kind of object, and the framework already
encodes the difference: **ν** (x,y) → (−x,−y) has det **+1** — a rotation, the half-turn, in SO(2);
**σ_B** (X,Y) → (Y,X) has det **−1** — a reflection, not in SO(2). Antipodal points are equidistant
from O with O as their exact midpoint, and the four seats are the instance already in `checks.py`
§1/§3. (Backing: Part B, B6 and B14.)

**A new consequence, flagged not asserted — structural open item 6.** v9 frames it as an either/or:
*"the sign-flip (fixed point O) is chart-stated; the mode-swap (fixed point the balance) survives
redrawing. Which is structural…"* The ruling puts the **central** involution (ν, the sign-flip about
O) on the side that can be exact, which is the opposite of "chart-stated." But note that *survives
redrawing* and *can be perfect* are two different properties, and nothing above refutes σ_B's
surviving redrawing. **So the either/or may be the wrong shape**: ν being structural does not require
σ_B not to be. Whether item 6 resolves or dissolves is a further ruling, not settled here.

**Q3 is affected.** The ν-identification was offered as the candidate resolution in the direction
this ruling now closes off. See Q3 below.

---

### Q2 (original framing, kept for the record) — Does one denial cover the other? (Backing: §2)

You said: *"You can't have two branches without rotation around the axis that splits them, because
infinite divisibility prevents perfect symmetry of two disconnected points."*

The framework already denies exactness. But it denies it in a specific way: **equality that holds all
the way down** is a terminal, and there is no bottom, so it cannot hold. That denial is about *depth*.

Your argument is about two things being **perfect mirrors of each other** — side by side, not up and
down.

**The question: is "perfectly mirrored" the same kind of impossibility as "equal all the way down"?**

- If **yes**, your argument stands, and it closes the framework's biggest open question — the two
  branches are genuinely two, they must be bound, and the binding is the rotation. It also
  contradicts a sentence currently in the structural chain ("the sign belongs to the chart"), which
  would then need rewriting.
- If **no** — if mirroring is a different sort of claim — the argument needs its own grounding before
  it can carry that weight.

This is the highest-stakes item in the file, and it is entirely a question about what the denial
covers. Nothing computational bears on it.

### Q3 — Does an answer about the child answer a question about the parent? (Backing: §3)

The math chain says the parent's two branches "read as one structure in the child's register," and
offers that as the candidate answer to the open question about the sign.

What I found: **in the parent they are genuinely two separate things.** They only become one when
read from the child. But the open question is asked about the *parent* — are there two things there,
or one thing drawn twice?

**The question: does an answer about the child settle that, or change the subject?**

If Q2 comes out "yes," this one mostly stops mattering — the question would already be answered by a
different route, and this claim would be a true but idle fact.

*(Separately, and not a judgment call: the same sentence says "the seat maps to the child's origin,"
which is simply wrong as written — a term was dropped. That is a correction, not a ruling.)*

### Not for you

§1's threshold discussion, the `φ` notes, and Part D of the checker are **machine-facing**. Their only
job is to stop a future reader — human or model — from re-finding a dead end and mistaking it for a
result. Nothing there needs your judgment.

---

## 1. "Occupiable at resolution" — the content stands, the word is the question

Relates to: `v9_comparison_r1.md` §2, ruling 1.

**[W]** on why the phrase might be defensible:

> it's not that we can't cross the room fully, it's that once at the other side what we call
> "touching" the other side is probably a frame-relative boundary. zoom out and there's no
> differentiation between person and room and wall. zoom in and you find space between the tip of
> the shoe and the wall.

> An infinite gradient has a limit at it's center rather than a discrete point, but that limit only
> behaves as a limit under a relative threshold as yet undefined or derived or anything like that.

**[W]** sharpening it, and correcting a mistake of mine:

> it's all relational. 1_n is not an integer, it's the minimual unit of distinction within frame n

> I think 1n is supremely accurate and completely without precision, or something like that.

**[checked] — the content is already v9's.** Structural §5 says *"balance at frame resolution
obtains… What cannot obtain is exactness."* So this is not a re-import of the seating error at the
level of claim. The disagreement is lexical.

**[checked] — my threshold analysis was in the wrong register, and is withdrawn.** I offered three
candidate thresholds for "frame n cannot state this imbalance": the `Q_i` excess (`|b| < √(1ₙ/2)`),
the amplitude unit (`|b| < √1ₙ`), and the mode difference (`|b| < √1ₙ/2`). All three are **additive**
quantities. v9 §3 says *"its amount exists only as a ratio to its conjugate"*; §4 says *"things differ
in their proportion of the modes."* Proportion, not difference. All three candidates are therefore
mistyped.

**[checked] — the relational coordinate, and what it shows.** On the gradient the conserved product
never moves (`XY = 1ₙ` at every b — verified). What varies is the ratio. Its additive form is the
rapidity:

```
X = √1ₙ·e^φ ,  Y = √1ₙ·e^−φ    →   XY = 1ₙ  (fixed),   X/Y = e^2φ ,   seat at φ = 0
```

This is the framework's own split action: v9's *"rescaling one mode by λ and the other by 1/λ,
product invariant"* is exactly a **shift in φ** (λ = 2 shifts φ by ln 2 ≈ +0.693; λ = 7.5 by +2.015;
product invariant in both — verified). Re-expressed in φ, my three thresholds land at 0.481, 0.659,
0.881 — nothing round, which is the signature of a coordinate artifact.

**[checked] — a flag that resolves downward.** Threshold three yields `X = φ_golden` exactly, because
`X − 1/X = 1` is φ's defining equation. That is a restatement, not a discovery: any conserved product
with a unit difference produces it, and the content sat entirely in choosing a *difference* threshold
— the wrong register. **Recorded so it is not re-found and mistaken for a result.** (The repo's
`_between-e-and-phi.md` is underscore-prefixed and does not ship; not assessed.)

**[checked] — an error of mine, struck.** I called the resulting neighbourhoods "large" (`X/Y ≈ 2.6`
at the tightest) and treated that as evidence against them. That is a cross-frame magnitude
comparison, which v9 rules out — *"magnitude comparison across frames is not defined"* — and I made
it two paragraphs after quoting the prohibition. The judgment is withdrawn; it was not evidence of
anything.

**What Will's position implies.** If 1ₙ is *supremely accurate and completely without precision*,
there is no numeric threshold to find: 1ₙ is not a value with digits, it is the resolution. So the
question "what ratio does frame n call indiscernible?" asks frame n to do something only frame n+1
can do. By the Corollary, sub-unit distinction is not a fine reading *within* frame n — it **is**
frame n+1. The discrimination is not frame n's to perform.

**[open] — the ruling itself.** Two routes, and Will has not chosen:

- **(a)** Replace the word. The math chain already carries the right phrasing two bullets after the
  offending line: *"Balance status: met-at-resolution, unmet-absolutely."* Adopting that in both
  chains clears three of the four lint hits and loses nothing above.
- **(b)** Narrow the ban explicitly — e.g. "occupancy language does not type `Oₙ` or `P₀`," leaving
  `Pₙ`-at-resolution describable that way. Requires editing the prohibition sentence, not just
  tolerating the collision.

What cannot stand is the present state, where the same document asserts both.

**A caution on (b), for the record.** "Met at resolution" is a status on a *relation* — the two modes'
amounts stand equal to within what the frame can state. "Occupiable" is a modal predicate on a
*locus* — this place admits a tenant. v9 denies the tenant on the same page: *"P cannot serve as frame
n's origin: one constitutive pair per frame, and n has no spare pair to grant it."* The seating error
was exactly the slide from the first to the second. If (b) is chosen, that is the sentence to watch.

---

## 2. The T1 fork — Will's rotation argument [HIGHEST VALUE, HIGHEST RISK]

Relates to: math open item 9 (the signed register); structural open item 6 (the involution);
`v9_comparison_r1.md` §3 and §6.

**[W]**, in full:

> You can't have two branches without rotation around the axis that splits them, because infinite
> divisibility prevents perfect symmetry of two disconnected points.

**The fork this addresses.** v9's T1: *"Its interpretation — whether the components must be bound
(through the circle slice: the door) or whether the second component is the chart's image of the
first — is exactly open item [9]."*

**The argument, restated for checking.** If the second branch were merely the chart's image of the
first, the relation between them would be an *exact* involution holding at every depth. That is
equality carried through every depth — the same terminal condition v9 already denies for the balance
(*"equality carried through every depth is a terminal resolution, and P1 denies terminals"*). So the
symmetry is met at resolution, not exact; the branches are not reducible to one drawn twice; and
something must bind them. The binding is the rotation.

**[checked] — the arithmetic does not refute it, and v9 says why.** Algebraically the chart *does*
show exact ν-symmetry: both branches of `XY = 1ₙ` map onto the same line under `z²` (Im w = 2·1ₙ in
the mode chart), each sweeping it completely — verified at X = 0.4, 1.0, 2.5. But v9's **rendering
guard** states: *"the constitution-layer facts have no rendering; the drawing's verdict never
overwrites the constitution's."* The apparent exactness is in the drawing. The guard was written for
precisely this.

**What it would close.** T1's fork resolves toward *"the components must be bound"* ⟹ the signed
extension is the **derived second register**, not chart residue ⟹ most of **math open item 9**, the
framework's largest open item. It also **un-conditions both i² routes**, which v9 currently tags
*"[verified; both conditional on the binding requirement — item 10]"*, and with them orbit-necessity,
the minimal traversal, and the ν-centroid instances — all of which v9 lists as falling if the sign is
chart.

**The collision, unresolved.** Structural §3 states flatly:

> The modes are **magnitudes**, not signed quantities: the conjugate of a mode is another mode, not
> its negative. **The sign belongs to the chart** — an orientation of reading.

If the rotation argument holds, §3's second sentence is wrong as written. Both cannot stand. This is
not a wording collision like §1 above; it is a substantive one.

**[RULED 2026-07-27 — the load-bearing step is answered.]** The question below was: does "perfect
symmetry" fall under P1's denial of *terminal resolution*, or is that an extension of the denial to a
new class of object? **[W]:** *"it's the same argument. two points can't mirror BECAUSE you think you
have a mirror but then can always zoom in to find further distinction."* A mirror-claim is a claim
about every depth; the descent never finishes; so it is the same object and the same denial. See the
Q2 block near the top for what this settles, what it obliges, and the two residuals.

**The caution below is retained, not struck.** The Divergence Ledger's standing finding is that false
closure is most attractive exactly where an open item wants closing (Entry 001), and that a closure
carrying no flag is harder to catch than one that flags itself (Entry 003). That applies to any
author, not only to a model. **This ruling closes the framework's biggest open item and was made in
conversation, not written to theorem standard.** It should be written up and given one adversarial
read before it propagates into chain text — specifically on residual (a): the strong reading (the
correspondence is never *settled*) is the one that parallels canon; the weak reading (zooming
*reveals* an asymmetry) does not follow from infinite divisibility alone. Nothing should move in the
chains until the strong reading is the one on the page.

---

## 3. The ν-identification — provenance

Relates to: `v9_comparison_r1.md` §3, ruling 2.

**[W]:**

> re v-identification, unaudited. that's AI work I haven't taken time to read etc.

**Why this matters beyond the tag.** v9 offers the ν-identification as *the candidate resolution* to
math open item 9: *"The parturition map's ν-identification is a candidate resolution: the branch and
its image as one structure in the child's register."* So the proposed answer to the largest open item
is machine-drafted, tagged `[verified]`, and unread.

**[checked] — the entry splits three ways.**

- **Holds.** The 2-to-1 identification is true and chart-independent (`w(−z) = w(z)`).
- **Fails.** *"The seat maps to the child's origin"* is false as a coordinate claim in either chart —
  split chart sends the seat to `1ₙ`, mode chart to `2i·1ₙ`. Only v7.7 r4's `w = z² − 2i·1ₙ` gives 0.
- **Needs a read.** The identification holds **in the image**. In frame n the two branches are
  disjoint sets — (0.5, 2.0) and (−0.5, −2.0) share no points — and become one structure only after
  applying `w`, i.e. in the child's register. T1's fork is asked **in the parent**. Answering it with
  a fact about the child's register may change the frame rather than resolve the question.

**Interaction with §2.** If Will's rotation argument holds, item 9 closes by a different route
entirely and the ν-identification's role as candidate resolution becomes moot — it would remain a
true fact about `z²` with no load to carry. **The two items should be ruled together, in this order:
§2 first.**

**[open]** — the `[verified]` tag: coordinate claim (restore the offset) or constitutional claim (drop
the tag, since v9 defines `[verified]` as *machine-checked computation*)?

---

## 4. 1_body — closes a flag I raised

Relates to: `v9_comparison_r1.md` §6 (structural observations).

**[W]:**

> 1_body is specifically the torus surface and what is within it that makes up what I think of as
> "me" as opposed to "not-me"

> There's lots of different frames smaller than me that make up my 1_body and my parametric
> environment is driven by the frames larger than my 1_body.

*(The toroidal geometry in the first quote is set aside at Will's direction — "that wasn't the
important part." Quoted in full rather than truncated, so the omission is visible. This entry
concerns only the one-distinction point.)*

**The flag it closes.** I had raised: v9 defines a frame as *"the coinherence of one distinction…
one constitutive pair per frame,"* but a body is plainly many. The answer is that 1_body **is** one
distinction — me/not-me. The cells and molecules constitute it from below without being it. The
definition holds for a body without strain, because the frame is the boundary, not the inventory.

**What it also supplies, informally.** A direction: constitution from below, parameters from above.
Not a claim the chains currently make in those words.

**Status:** an instantiation of v9's frame definition, not a new claim. No ruling needed. Recorded
because it retires a flag rather than leaving it open.

---

## 5. Branch scope, and the mode signs — RULED (scoping), 2026-07-28

**[W]:**

> the two curves of xy=1 in q1 and q3, when in frame R_1, are both valid but the one in q3 is the
> required conjugate of q1, and q1 is where +void and +form are held in proportion, whereas q3 would
> be -void and -form. I don't know the implications of that other than imagining it has something to
> do with the idea of "dark" or "anti" matter/energy/whatever. So for the moment and for the purposes
> of this work I am acknowledging them both but only focusing on q1 where +void*+form=reality

**What is ruled.** Working scope is **Q1**. Both branches are valid; Q3 is the **required** conjugate,
not an optional one. **This is a focus decision, not an ontological demotion** — recorded explicitly
so a later reader does not mistake the working scope for a claim that Q3 is unreal.

**Prior clarification that produced it.** "Two branches" in T1 means the **Q1 and Q3 curves, entirely
disconnected** — not the x-dominant and y-dominant arms within one curve, which are one connected
curve through the seat. *[checked]* Getting from Q1 to Q3 along the curve would need X = 0, and
`XY = 1ₙ` admits no zero — the class-selection argument is what keeps them apart.

**The two involutions sort onto the two readings** *[checked]*:

| | relates | fixed | det | kind |
|---|---|---|---|---|
| **ν** (X,Y) → (−X,−Y) | Q1 ↔ Q3 | `Oₙ` | +1 | **central** — can be exact (Q2) |
| **σ_B** (X,Y) → (Y,X) | arm ↔ arm *within* a branch | the balance line | −1 | **mirror** — cannot be perfect (Q2) |

So the mirror lives *inside* a branch and the central symmetry lives *between* branches. This makes
the structural-item-6 observation concrete rather than abstract: v9 leans toward the **mode-swap**
being structural, but the mode-swap **is** the mirror. Consequence, new and unrecorded elsewhere:
**the x-dominant and y-dominant arms of a single branch are not exact mirrors of each other**, though
the algebra says they are. The rendering guard covers it, but it is a claim about a single branch's
interior and is new territory.

### Consequences

**(A) Independently confirms Q2.** "Q3 is the *required* conjugate of Q1" is the binding requirement,
reached from the ontology rather than from T1. Two routes to the same place.

**(B) A second push on structural §3, and a typing question.** §3 says *"The modes are **magnitudes**,
not signed quantities… The sign belongs to the chart."* The ruling above speaks of **−void and
−form**. **[open, Will's]** Does "−void" mean a negative *magnitude* — which §3 forbids — or the same
magnitude read under a conjugate orientation? v9's DDJ reads 無 as *"a bounded, shaped, functional
magnitude"* and insists *"void is not zero."* A negative magnitude is not obviously typed. Either
answer bears on §3, which Q2 already put under revision.

**(C) The conserved product is blind to the branch** *[checked]*. `(+2)(+0.5) = +1ₙ` and
`(−2)(−0.5) = +1ₙ`. Both branches sit on the same level set with the same unit. **So nothing in the
product distinguishes Q1 from Q3 — the entire distinction is carried by the sign.** That is a third
independent line arriving at math item 9, and it sharpens what the item is asking: not "is there a
second component" (T1 settles that) but "what carries the difference between them."

**(D) A physics entry switches on, and the speculation splits.** v9 physics §7 is written as a
fork-display: *"Charge conjugation C is a genuine physical involution pairing each particle with its
conjugate — the one antimatter echo the framework may keep [held light]. The entry is conditional on
the signed-register item: **if the signed extension is derived structure, the rendering is live; if
chart, it retires.**"* Q2 resolves that condition, so **the entry goes live** and should lose its
conditional framing.

But the same entry continues: *"**Dark matter and dark energy are parked regardless** — not
conjugation phenomena within physics itself."* So the speculation above splits cleanly: the **"anti"**
half is exactly the entry Q2 just licensed; the **"dark"** half is explicitly parked, with a stated
reason. **[speculation, recorded as such]** — under the physics chain's own discipline it would need a
break-condition before it could enter at all, and none is offered. Not promoted.

---

## 6. 非 tracks the sign-flip — RULED, 2026-07-28. Closes structural item 6.

**The question.** Will proposed *"fei is what sits between two modes. Something like -1|0|1::-1fei+1"*
— but the sentence and the notation pick out different relations, and the difference is the open item.

| reading | relates | the middle is | status of that middle |
|---|---|---|---|
| "between two modes" | form ↔ void, the **mode-swap**, within one branch | **the seat** *[checked]* | met at resolution |
| `−1 非 +1` | the conjugate pair, the **sign-flip**, between branches | **the origin** *[checked]* | unmet |

**[W], ruling:**

> how can the emptiness between the two diverging wings in fei be "met"? it can be held as reference
> but it's the limit at the center, like the 0 that sits between -1 and +1

**Ruled: the middle is unmet — the origin. So 非 tracks the sign-flip (ν), and the notation was right
where the sentence slipped.**

**The ruling restates the framework's own definition of O.** Compare:

> **[W]** "it can be held as reference but it's the limit at the center"
> **structural §3** "Both modes are measured from one reference — the point at which both would
> vanish. That is the origin O: a locus, never a position; nothing stands there."

Same statement. **Priced as consistency, not evidence** — Will is the author, so restating his own
definition is not independent confirmation. What it does establish is that the assignment is
*motivated* rather than brute, the standard the chain applies to 玄牝.

### What this closes

1. **Structural open item 6 (the involution).** 非 tracks the sign-flip. Note this closes it
   **opposite to v9's tentative framing**, which had *"the sign-flip (fixed point O) is
   chart-stated."* Combined with Q2 (the sign is structural), the sign-flip is structural.
2. **The DDJ's open checksum item** on the involution — *"whether 非's structure tracks the mode-swap
   (fixed at the balance) or the sign-flip (fixed at the center)"* — same ruling, same direction.
3. Together with Q2, this is the **second top-level open item** closed in two days. Both should be
   written up and read adversarially before propagating; see the caution in §2.

### Two notes the write-up must carry

**(a) A false conflict to head off.** The reciprocity joint's clause 2 reads *"Exchange symmetry. The
law is symmetric under X ↔ Y,"* and Q2 denies that perfect mirrors obtain. These look contradictory
and are not. The joint's claim is about the **law** — X and Y are interchangeable in the equation.
Q2's denial is about two **loci** being feature-identical at every depth. Different objects, both
standing. State it, because "you just denied the mode-swap and your own joint asserts it" is the
first objection anyone will raise.

**(b) A reconciliation still owed — the one place this could come apart.** The DDJ chain glosses 非's
middle as *"the invariant both flanks share"* — which reads as something positive both *have*. The
ruling makes it emptiness — something neither *reaches*. Will's own phrase bridges them: **"held as
reference."** O is both at once — the shared reference from which both modes are measured, and unmet
by either. Shared, unoccupied; one statement rather than two.

**But the Ch. 1 instance still needs checking.** There the flanks are 可道 and 常道 and the invariant
they share is **道**, which the chain glosses as *"the recursive structural model itself"* — not as an
unmet reference-point. **[open]** Does the 非-middle correspondence hold uniformly, always landing on
O? Or does it vary by instance — 道 in Ch. 1, O in the geometry? This is a translation-fidelity
question, and it is where the reading could still fail.

**Method note.** The ruling reads the *graph* 非 (two diverging wings, space between). Under the
chain's own instrument rules that is legitimate at the **notation layer** — it flags a candidate
distinction — but *"the math must then verify with its own resources."* Verification here is the
geometry: ν's fixed point is the origin, and the origin is unmet. The graph flagged; it did not
establish. *(The project has withdrawn graph-evidence before — the 名 case in v7.7.)*

---

## 7. The DDJ's evidential posture — "generatively upstream" is overconfident, 2026-07-28

**[W]:**

> the "ddj is generatively upstream" is overconfident. It's a language like physics or math or logic.

**Ruled.** This is a claim about Will's own process, and he is its only authority.

**What survives, and what should change.** The DDJ chain's *conclusion* — agreement carries no public
evidential weight — does not depend on the authorship claim. The physics chain reaches the same place
with no authorship claim at all: *"every claim is a correspondence, `::`, meaning maps-to and never
is; no entry carries evidential weight in either direction."* That is the **rendering** ground, and it
applies to the DDJ identically if the DDJ is a language alongside math and physics — which is what v9's
own architecture says it is (a spine plus three renderings).

The authorship clause does block one real bad inference: a 2300-year-old text agreeing with a modern
framework being read as *independent* convergence. But a weaker ground blocks it just as well — the
author is fluent in the text and worked with it alongside the framework, so agreement is not
independent. That claims nothing about generation.

**Recommendation: keep the conclusion, weaken the ground.** Chain edit, Will's.

> **Recorded against myself.** I repeated *"the DDJ is generatively upstream, so that fit is coherence
> rather than evidence"* as though settled, when it is the chain's claim about the author's process
> and not something I can assess. Logged because the same move — restating a chain's self-description
> as established fact — is how an unaudited line acquires the appearance of an audited one.

---

## Re-runnable backing

Every `[checked]` line above is backed by `python3 rsm/audit/cross_model_checks.py` — **Part D** (D1
the rapidity coordinate and the threshold artifacts; D2 the parent-disjoint / image-identified split).
121 checks total; exits non-zero on a false claim.

One incidental fact, since it will otherwise look like a coincidence twice: the difference-threshold's
rapidity is `asinh(1/2) = ln(φ_golden)`, for the same reason `X = φ_golden` appeared — `1/2 + √(5/4)`
*is* φ. Same restatement, wearing a second hat. Not a finding.

## Order of operations

1. ~~**Q2 first**~~ — **RULED 2026-07-27, and both residuals closed the same day.** Ready to be
   written up to theorem standard. One new question it opened: does structural item 6 resolve, or
   dissolve because its either/or is the wrong shape?
2. **Q3** — now largely decided by Q2, but confirm: the ν-identification was the candidate resolution
   in the direction Q2 closes off, so it no longer carries that load.
3. **Q1** — independent of the others; can be answered any time.
4. **Write Q2 up to theorem standard** — done, `q2_mirror_denial_draft_r1.md`, awaiting one
   adversarial read. Then propagate: structural §3, `CLAUDE.md`, math item 9, structural item 6, the
   i² conditionality, the DDJ `非` reading, **and physics §7, whose conditional Q2 resolves (§5D)**.
   Scope for all of it is **Q1**, per §5.
5. **Write §6 up alongside Q2** — 非 tracking the sign-flip closes structural item 6 and the DDJ
   checksum item, and carries two obligations: the exchange-symmetry false conflict (§6a) and the
   Ch. 1 reconciliation (§6b, still open).
6. **§7** — weaken the DDJ chain's evidential ground; keep its conclusion.
7. Then the remaining rulings in `v9_comparison_r1.md`, then amend the `rsm/v9-canon` branch, then the
   site redraft. **The site's `[derived]` table changes with item 4** — i² = −1 is listed there and its
   conditionality moves.

**Running total: two top-level open items closed in two days** (math item 9 via Q2; structural item 6
via §6), plus a scoping ruling (§5) and a posture correction (§7). None has been written to theorem
standard except Q2, and none has propagated into chain text.

**A standing note for whoever drafts here next, human or model.** Will has said plainly that this
math is not his. The rule is: compute it, then state the *consequence* in plain words he can evaluate
structurally, and let him accept or reject that. Handing him notation to adjudicate is how a
preference gets recorded as a derivation — the same mechanism that produced the seating error, run
through a person instead of a model. The "three questions" section above is the required form; the
rest of this file is backing for it.

Nothing here is a ruling. Ontology, referents and readings are Will's.
