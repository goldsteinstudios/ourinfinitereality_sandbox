# Session consolidation addendum — 2026-08-02 (late session)

Covers everything after the v9.4.2 installation. Extends the main consolidation; same
conventions (RULED = Will's; OFFERED = Claude's, awaiting check; KILLED = tested and dead).

---

## Pending chain fix (next patch set)

**"Transfer" → "Recursion."** Will's catch: the v9.4.2 §7 heading "The two transfers" is
transfer-language for a non-event — the paragraph under it has to deny its own heading.
Fix: heading becomes **Recursion**, the two lines stated as **two readings** (yAxis₍ₙ₊₁₎
reads Bₙ; xAxis₍ₙ₊₁₎ reads Gₙ); math theorem retitles "Recursion (two readings)"; sweep
"the transfers" → "the readings" and check "the constitutive share" (§7 — "share" is also
transfer-language). Not yet applied anywhere.

**|0|/|1| numeral faces** (math open item 3): recommendation on the table — keep "the |1|
cascade" as the route's name; convert step-descriptions to word forms (empty/full face).
Unruled.

## Rulings (late session)

**R-pot cluster (the Gemini-thread rulings, Will's):**
- The pot's axis is a **limit, not locus**; the cavity is a magnitude. Three absences, three
  objects: cavity :: Y_pot (constitutive void) · axis :: B_pot (limit) · O_pot sited in the
  **throwing-frame** (wheel surface :: xAxis, spindle :: yAxis) — the artifact renders G, B,
  and the modes; the origin is visible only in the generating frame. Vantage rule in
  ceramics.
- **Void coemerges with form; occupancy is irrelevant in R_pot.** Water is another frame's
  traffic. Any reading where filling diminishes 無 has confused occupancy with
  constitution (new guard).
- **Frame-failure is not substance-failure**: walls to no-cavity = no pot, not pot-of-zero;
  the clay persists. Too-thin = parametric truncation; no-cavity = structural exclusion.
  One object, both failure kinds.
- **R_pot names its gradient multiple ways** (thickness·void = clay; height·width = volume)
  — multiple 可名 of one law; constant-clay is the DDJ metaphor's chosen conservation.
- **The temporal costume of the pair**: what before/after holds :: the sensorimotor
  integration span — between tick and tock; the instant is the axis-limit, never occupied.
  Empirical span is explicit-register (CAVP applies).
- **用 (Ch. 11) HELD** — waiting on Guodian work Will is not ready to bring in. Not open
  for drafting.

**Reality/universe split (Will's):** reality :: R₁ (carries the gated essay-13 largest-frame
candidate); the universe :: R_universe, just a frame. G_universe :: X_universe (形/又) ·
Y_universe (void/亡) = 1_universe.

## Candidates (offered, surviving)

- **Relativity of rest :: no point of B obtains.** b = 0 is rest; absolute rest would be
  occupying B; physics denies absolute rest. New, not in any chain/essay. Strong.
- **c :: the null structure of the universe-frame's rendering** — not a unit, not a speed:
  Q_j = XY, so Q_j = 0 is the *terminals* (P₀'s faces), not B. c = 1 because c is a chart
  seam (conversion within one conjugate pair). Correction logged: Claude initially conflated
  the cone with the mode-collapse; the cone renders terminal-exclusion.
- **Seam argument's cost:** it generalizes to ℏ, G — only dimensionless ratios are
  structural; the "carries the dimensions of action" clause in 1ₙ :: ℏ/2 weakens (the
  entry's inequality-shape finding survives).
- **The real content of c**: that a cone exists at all (Lorentzian vs Galilean) :: the
  two-slice identity. Value = seam; invariance = automatic within the typing.
- **Photon :: inter-configuration difference, not a frame.** Resolves the zero-unit
  exposure as a type-fact (no unit, no paradox, no rest, null-dwelling). In-flight strain
  dissolved by zero proper time (no interior register). Discriminator: invariant mass vs
  chart-relative frequency/photon-number (Unruh). Breaker to watch: a massless quantum
  with a chart-independent magnitude. Strongest unpromoted physics candidate.
- **c :: 1_universe — ruled against** in that form (a unit is a magnitude; the seam
  argument says c isn't one). Will's containment reading (parent unit showing through
  interior denominations) stays alive but cannot use c as evidence.

## Killed

- **Fermion/boson :: frame/difference** — dies on composite bosons (helium-4 is a frame
  and condenses). Statistics tracks spin composition, not frame-hood. Only the narrow
  claim survives: massless quanta are differences.

## Editorial-only (never chain)

- **Light/dark co-emergence** — light :: propagating distinction; dark cannot obtain
  without light to not-be. NOT among the six Guodian Ch. 2 pairs; extension, not attested
  instance. **Fiat lux re-read (fiat :: therefore, entailment not command)** — cross-
  tradition ::, essay material only. (Cut off mid-development; Will may resume.)

## Register additions owed

rest::no-B candidate · dimensional-clause weakening · photon-as-difference (with breaker) ·
zero-proper-time note · killed fermion/boson mapping · light/dark editorial flag · pot
cluster · parametric-truncation-vs-floor distinction · transcription-authority question
(Code's v9.3 vs Will's markdown had diverged; the rebuild made Will's copy canon — confirm)

## Standing queue (unchanged)

1. Transfer→Recursion patch (above) + |0|/|1| ruling
2. R5/R6 rewrites (有無相生, Ch. 40) — flags in place
3. Notation warrant wording (又亡相生 :: XY = 1)
4. Open-items register regeneration (owes B11-closed note)
5. Then: editorial phase (NotebookLM documents)

Errands live: seven source images · icloud tables · slip verifications (絕, 逝, A24, 非) ·
Mawangdui facsimiles · 恆/常 distribution survey.
