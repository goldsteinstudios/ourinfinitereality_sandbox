# The Divergence Ledger — AI Failure Modes and Attractors (r1)

Record of rejected structural proposals, false closures, and mathematical attractors across AI instances. Active filter against machine-hallucinated geometry. Divergences between instances are the data; convergences are priced near zero (shared corpora). Maintained in the primed adjudication context against the verified computational record.

## Entry 001 — The Product/Ratio Orthogonality Attractor (the Image-9 Error)
**The false proposal (Gemini instance, unprimed by the prior correction):** that {XY = c} and {X/Y = k} form a globally orthogonal net, offered as the derivation route for "sealing" the anchoring line.
**The mathematical failure:** grad(XY)·grad(X/Y) = (Y² − X²)/Y² — zero only on the balance axes. The true orthogonal partner of the product family is {X² − Y² = k}, which is the level-net of the z² parturition map itself (Re/Im of z²).
**Precision note — what was and wasn't false:** the B-collinearity of lineage seats is TRUE (via parturition inheritance; canon S1). What failed: the net premise (false), the lineage-continuity premise (circular — presupposes the inheritance it argues toward), the G-membership premise (posited), and the status claim ("sealed" — inflation over an honest gap).
**Attractor status: verified.** Three independent sources have produced this exact false geometry: the project's own archived plot (image 9) and a second AI instance bridging the same gap. The error is attractive precisely where an open item wants closing — the signature habitat of false closure.
**Resolution:** rejected against the verified record; the source instance accepted the correction on computation and co-drafted this entry — a divergence that CLOSED under evidence (the instance-protocol working). The anchoring residual remains open, now decomposed (math r4).

## Entry 002 — The Chained-`::` Objection (site-redraft review)
**The proposal (Claude instance, reviewing site redraft r1):** that the chained form `P₀ :: 玄牝 :: the generative paradox` treats `::` as transitive and "launders two hops into one identity"; proposed replacing the canon chain with a split two-hop form plus a new gloss ("the excluded terminal condition: absolute indistinguishability, which cannot cohere").
**The check — canonical, not computational.** `CLAUDE.md:66` writes the identical chain (`Oₙ :: xaxisₙ ∩ yaxisₙ :: the origin`, `Pₙ :: Gₙ ∩ Bₙ :: 玄`, `P₀ :: 玄牝 :: the generative paradox`). The `::` doctrine (`CLAUDE.md:8`) defines the form explicitly: "A `::` chain (A :: B :: C) claims all terms converge on one identified invariant" — a sanctioned construction, distinct from a transitive identity. The proposed replacement gloss ("absolute indistinguishability, which cannot cohere") re-imports v7.6-candidate grounding language of the kind the seating audit was refusing; cf. G3 (walkthrough ledger): "P₀ is not nothing — it is the co-constitution incoherence itself, a condition that fails to cohere, not a state that fails to obtain." The proposal drifts toward the "absence" misread G3 guards against.
**What was true inside the proposal (always stated):** the laundering worry is real for a public page read *without* the discipline in hand. Met structurally, not by amending canon: the `::` discipline and the four status tags are now taught on the rosetta-stone page *before* any chain appears, and the homepage carries no chains at all (plain-language paraphrase only).
**Same review — a second, separate issue (citation provenance):** the review cited failure-mode tags "M5 / M9 / M10" and a "provenance map." Partial finding, correcting an earlier over-hasty adjudication of my own: an **M provenance class does exist** — the W/M/V map (walkthrough ledger r3: W=seed/Will, M=machine-apparatus, V=verified). What does **not** exist is the **numbering**: there are no M5/M9/M10 as enumerated items anywhere in the repo. So the prefix is real, the specific citations are not resolvable against the record. **Flag for Will: is there an enumerated M-list outside the repo, or is the numbering confabulated?** The distinction matters because the same review produced five catches that verified correct.
**Resolution:** rejected by Will's ruling (canon chain-form upheld, site walkthrough 2026-07-14). Logged for the ratio, per the standing protocol: one rejected structural proposal against five accepted corrections from the same instance — a high-signal reviewer with one attractor-shaped miss (the miss lands exactly where a public page invites an identity read, the signature habitat of false closure).

## Standing protocol
Each entry records: instance and priming state; the proposal verbatim; the computational check; what was true inside the false proposal (always stated); resolution, and whether the source instance accepted correction. Rejections the source instance defends after computation get a defended-divergence flag and priority scrutiny — in both directions.

Entries need not be geometric: a rejected structural/notational proposal about the public renderings is logged here too, checked against canon rather than against `checks.py` (Entry 002).
