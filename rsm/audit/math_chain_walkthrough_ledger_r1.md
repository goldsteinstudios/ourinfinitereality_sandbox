# Math Chain Walkthrough — Consolidated Corrections Ledger (r1)

Source: node-by-node walkthrough of rsm_math_chain_v7_6c_r5.md, Will's rulings.
Status: working ledger for the v8 rewrite. Nothing here is canon until propagated.
Walkthrough position: Nodes 1–4 ruled and closed; Node 5 open (one pending question); Nodes 6–22 not yet walked.

---

## I. GROUND RULINGS (definitions; propagate everywhere)

**G1. Infinity is definitional and two-directional.** Infinitely vast AND infinitely divisible — no maximum extent, no minimum scale. "No terminal resolution" carries this gloss at first use. The antecedent is a stipulated definition; an auditor can decline it but not attack it as an inference.

**G2. The chain is entirely ontological.** "What is not distinguishable does not obtain" is an existence claim. Base form: co-constitution — Nothing cannot obtain without a thing to be no-of; 0 is incoherent without a coinherent 1.

**G3. P₀ is not nothing.** It is the co-constitution incoherence itself — a condition that fails to cohere, not a state that fails to obtain. Explicit guard needed in v8 against the "absence" misread.

**G4. The ground introduces no new premise.** "Because P₀, O₁" = conditional + G2's bridge (contrapositive) + existential import. First-frame cascade, each step biconditional: P₀ → O₁ → (X₁,Y₁) → (G₁,B₁) → P₁.

**G5. Co-constitution is biconditional throughout.** Oₙ ⟺ Gₙ; neither prior. Entailment identities read in derivation order, not productions.

**G6. The conditional is never promoted.** All isotropy/universality claims are consequences of the infinity conditional, not unconditional claims about reality. "Given the conditional, reality-writ-large has no preferred frame/scale/orientation." Whether reality satisfies the conditional is never claimed proven.

---

## II. THE REGISTER SYSTEM (the walkthrough's central finding)

**R1. The structural skeleton is implicit-register.** No parametric obtainedness — no motion, duration, occupancy, or "when." Implicit and explicit registers share logic, not expression. All duration/occupancy vocabulary applied to skeleton facts is ill-typed.

**R2. Casualties of R1 (struck):** the doc's aperture wording ("instantaneous-local, not sustainable"); "occupied by the modes" (line 18); all "cannot persist/dwell" formulations. Proof-hygiene flag: Theorems 2–3 prove via parametric paths while their conclusions are implicit-register (auditor note, not a hole).

**R3. X/Y carry no interpretation in the skeleton.** Form/void, bellows, and all physics/DDJ vocabulary are thinking-aids — legitimate for seeing, forbidden in the spine. Interpretation belongs to the other chains, priced and separated.

**R4. "Aperture" struck from the math register — DDJ→math bleed** (gate-vocabulary, 門; carried event-connotations that fed the ill-typed wording). Contamination is now attested in BOTH directions. Replacement is the seat-phase structure (E2 below). Open item #1 renames: "the seat-phase discharge argument to theorem standard."

**R5. Three math languages, √ as translator between the first two:**
- Measure language (quadratic): 1ₙ native — it IS the conserved product; what i preserves.
- Amplitude language (linear): coordinates, radii; √1ₙ; what i acts on.
- Orbit language (phase): 1 as one end of the conjugate pair {+1, −1}.
All 1-vs-√1 confusions are undeclared translations (incl. the (X,Y)/(a,b) √2 normalization; incl. the diameter-1ₙ circle — one unit in two languages, flag resolved). The glyph "1" plays three roles: coordinate, magnitude, phase. **v8 house rule: every equation declares its language.**

**R6. General principle (multiply attested):** the framework's registers share logic, not expression — implicit/explicit, j/i, measure/amplitude/orbit, parent-frame/child-frame. Undeclared register-translation is the project's characteristic confusion-generator, in both directions.

**R7. Predicted inexpressibility, two types, now a stated meta-result:**
- Internal: Oₙ resists reference because it is where the frame's own distinctions fail.
- Cross-frame: the recursion is a relation between expressions; no single expression states it.
The framework predicts the exact locations where its notation strains; the correction history at those locations is confirmation-shaped, not defect-shaped. Four instances logged in this walkthrough alone.

---

## III. THE TWO CENTERS (re-typed)

**C1. Both centers are paradoxical — one genus, two species.** Genus: a paradoxical center is the crossing-point of a co-requiring pair (Oₙ of the axis-pair; Pₙ of the (G,B) pair, in the same way).

**C2. The species distinction is met-vs-unmet, not occupied-vs-unoccupiable:**
- Oₙ = unmet crossing: arms approach asymptotically, never meet; both modes at operational absence together; four-way distinction fails; equality-as-collapse; local face of P₀; inherited by the child as its origin.
- Pₙ = realized crossing: G and B actually intersect; both modes balanced at equal nonzero magnitude (√1ₙ, both seats); distinction intact; equality-as-balance; generative because there is structure to hand down.

**C3. Met-ness is frame-relative.** Oₙ = P₍ₙ₋₁₎ is identity (one locus), not parenthood (two objects); "parent/child" indexes the frame-readings; "generated" = the frame-reading relation, no event. The same locus is realized in the parent's expression and unmet in the child's. Met-from-without, unmet-from-within; curved-pair as parent's P, straight-pair as child's O.

**C4. Center classification by incident pair:** O-type = straight×straight (locally and globally aligned); P-type = straight×curved (locally orthogonal, globally curved-relative).

**C5. Register-unit alignment characterization [candidate]:** Pₙ = the locus where all three 1s coincide at unity (coordinate 1, unit 1ₙ, phase-anchor); Oₙ = where all three coincide at absence/undefined. The centers are the two points where amplitude, measure, and orbit registers align — one at 1, one at 0.

**C6. 0ₙ is a limit-approach, not a point.** Operational absence approached along Gₙ toward Oₙ; type-distinct from Oₙ (process vs. unreachable limit). Doc's "limit-position" mis-describes it.

**C7. The parent-shift holds for n ≥ 2 only.** O₁'s parent-side is P₀ — an incoherence, not a seat. The ground exception is definitional (identity-through-opposition vs. parturition). O₍ₙ₊₁₎ = the child frame's origin, not the child frame.

---

## IV. THE ENGINE (the chain's crux)

**E1. Pₙ's non-occupiability is the single source of both rotation and recursion.** Pₙ is met but cannot constitute a standing axis-pair (locally orthogonal, globally curved-relative). Traversal cannot pass through → must go around → rotation. Frame-demand cannot be met there → discharges into new structure → recursion. One impossibility, two discharges. The doc's "generates because occupied" inverts the mechanism.

**E2. The seat-phase structure (verified; replaces "aperture"):** seatₖ = √1ₙ·iᵏ, k = 0..3; Q_i = 1ₙ constant; Q_j = (−1)ᵏ1ₙ alternating (double cover in the enumeration). Axis-instantiation exactly at the cardinal phases. The i-cycle IS the enumeration of generation sites.

**E3. The conjugate pair +1/−1 is antipodal, not collinear — and this re-derives i [candidate; strong].** The straight path between conjugates passes through Oₙ (excluded, Thm 1); the realizable path is the half-turn, requiring the perpendicular direction = i. Independent route to i from the co-constitution of unity.

**E4. Lemma A (the demand side):** an infinite gradient obtains ⟺ a globally straight, independent axis-pair holds at its center. Pre-metric core: straightness (the product structure requires global linear coordinates); orthogonality is the derived-metric face. Staging required to avoid metric-before-metric circularity.

**E5. The discharge argument, A-B-C-D structure:**
- A: demand (Lemma A) — closed.
- B: deficit (incident pair at Pₙ globally curved-relative; verified) — closed.
- C: transfer (Pₙ carries the center-role univocally — "in the same way" as theorem, not analogy) — **THE remaining gap.**
- D: discharge (new straight pair entailed; child gradient by A's biconditional) — closed given C.
Open item #1 re-scopes to C: univocity of the center-role.

**E6. Recursion in one line:** P-type → O-type conversion by axis-generation. Canonical componentwise form: Gₙ → Gₙ + xAxis₍ₙ₊₁₎, Bₙ → Bₙ + yAxis₍ₙ₊₁₎ ("must" = structural entailment, no time-word).

**E7. The recursion formula is a cross-register sentence (Will's "limit of language").** Arrow = structural entailment across frame-readings; comma = coexistence-with-severance. v8 states the recursion bilingually (parent-sentence + child-sentence; no third sentence contains both), keeping the arrow as glossed shorthand.

**E8. Deflation guard:** "same thing, different frames" = identity at the locus, novelty of the frame. The point is one; the child's co-constitution cluster (axes, G, B, P, unit 1₍ₙ₊₁₎) genuinely obtains and is not re-description — else the cross-frame unit relation (open item 6) would be vacuous. Expressibility-in-parent-coordinates (z² net) ≠ identity-of-frame.

---

## V. THE ISOTROPY THESIS

**T1. "No preferred frame/scale/orientation" = three claims, three statuses:** scale — nearly earned (recursion self-similarity; the proportion is scale-invariant by construction); orientation — was assumed, now derived per T2; frame — near-definitional (FU). This triple is close to the framework's actual thesis.

**T2. Orientation-isotropy derivation [candidate; math register only]:** internal directions are frame-distinguished but orientation-covariant (pre-metric isomorphism); preference requires an external distinguisher; substrate is excluded by the no-unframed-distinction lemma; other frames give relation-not-preference (regress otherwise).

**T3. The no-unframed-distinction lemma (new; the derivation's load-bearing step):** every distinction constitutes a frame — a distinction requires a unit; a unit fires the G4 cascade. "Unframed distinction" is self-contradictory. **Unifies T1: all three isotropies are corollaries of this one lemma.** Auditor question: theorem via the cascade, or the framework's fourth named premise?

**T4. Smoothness fork [ruled to the safe side]:** framescaling is smooth ACROSS frames (substrate-continuous), quantized WITHIN frames (1ₙ is a hard floor). The within-frame smooth reading is excluded — it breaks 1ₙ. Locally-flat/globally-curved gains a scaling-axis reading: quantized-within / smooth-across. (This is the Bekenstein sort restated; feeds open item 6.)

---

## VI. STRUCTURAL OBSERVATIONS (logged, not pushed)

**S1. B passes through both paradoxical centers** (O is B's own symmetry center; P is where B meets G) — the inter-generational connector; why B serves as the child's y-axis; why the parent's O appears at finite depth in the child. [observed]

**S2. The floor interior has no obtaining positions at framescale n** — describable only via its boundary; simultaneously the child-frame's domain (inexpressible-in-n, not nonexistent). The boundary splits the two faces of infinity: vast outside, divisible inside; 1ₙ the exchange rate. [candidate]

**S3. Circle → sphere shifts the witness of the excluded center:** winding (loops, 2D) → enclosure (shells, 3D). Same exclusion, one register up. [observed]

**S4. Oₙ resists naming by its own definition** — reference strains where distinction fails. Predicted, not a gap. [state explicitly in v8]

---

## VII. CROSS-REGISTER CONVERGENCES (P2; observation only; never derivation)

**X1. 玄 marks the locus the math strains at.** 玄 :: Pₙ (existing ruling) matches the re-typed Pₙ (cross-register locus). 玄 stays assigned to the locus, not redefined as the strain. Priced cheap under shared authorship; independent-blind-reader test is the upgrade path.

**X2. Ch. 1's 始/母 :: Oₙ/Pₙ** — shared 女 root = the center-role genus, different determinatives = the two species. Status: [R] (Ch. 1 not in Guodian); the mechanism 母 points to has [G]-anchored relatives (谷神不死, 玄牝之門). Same pricing and upgrade path as X1.

**X3. Locally-flat/globally-curved :: GR** — P2, register-separated; the math fact (local orthogonal frame / global curved-relative at every obtained point) stands on geometry alone.

**Direction guard on all of VII:** register→observation only. The "aperture" episode (R4) is the standing example of what back-flow does.

---

## VIII. OPEN GATES

**O1. Node 5 pending:** 1ₙ as "minimal unit of distinction" and "the conserved product" — one identity in the measure language (per R5), or two claims needing separate support?

**O2. C-univocity (E5):** the center-role transfer, "in the same way" as theorem. THE remaining gap in the discharge argument; head of the auditor package.

**O3. No-unframed-distinction lemma (T3):** theorem or fourth named premise? Co-head of the auditor package.

**O4. Nodes 6–22:** not yet walked (Theorems 1–3, FU and calibration, Postulate F, i²=−1 elimination, metric, mirror osculation, branching, irreversibility, substrate).

---

## IX. PROPAGATION LIST (mechanical edits the rulings force in r5 → v8)

1. Gloss "no terminal resolution" as vast+divisible at first use (G1).
2. Add the P₀-is-not-nothing guard (G3).
3. Rewrite line 12: 0ₙ as limit-approach, not limit-position (C6).
4. Rewrite line 18: strike "occupied by the modes"; Pₙ as realized crossing that cannot constitute a standing axis-pair (R2, C2, E1).
5. Strike "aperture" throughout; install seat-phase structure (R4, E2).
6. Rewrite the recursion section bilingually with glossed arrow/comma (E7).
7. Re-word "generated frame n" as frame-reading identity (C3).
8. Add register declarations to every equation (R5 house rule).
9. Re-scope open item #1 to C-univocity (E5); add the lemma question (T3) to the premises section.
10. Add the predicted-inexpressibility meta-result (R7) and the naming-strain note (S4).
11. State the isotropy triple with per-leg status and the unifying lemma (T1–T3).
12. Add the smoothness ruling to the substrate section (T4).

---

*Ledger r1 — consolidated from 42 chronological entries. All items Will's rulings or Will's compressions formalized by Claude (Fable 5); [candidate] tags mark machine formalizations awaiting external audit. Not canon until propagated.*

---

# LEDGER SUPPLEMENT — Nodes 6–8 walk + P₀/infinity excursion (r1 append)

*Chronological since the consolidated r1. Same status conventions. Not canon until propagated.*

## Math walk (Nodes 6–8)

**N6. Theorem 1 restated, two faces.** Mode disconnection is a *j-slice fact* (the complex object Q(z)=1ₙ is connected). (i) negative face: no path within the gradient slice (the theorem's name); (ii) positive face: a path exists through the i-slice, joining the modes through the conjugate seats — "a second register required" was always carrying this. Retitle candidate: "Mode disconnection / cross-slice connection." Checksum hit: 相生 names face (ii). Tree-4 template answer: slice-relative ≠ deflated (frames live in their expressions); the 3D-form question stays open. [derived; verified]

**N7. Theorem 2 = the conjugate-seat toll; hygiene flag replaced by a layering map.** The r5 "path-based proof / implicit conclusion" flag was a misdiagnosis: traversals are legitimately explicit-register (Definition T introduces them). Correct statement: **implicit skeleton** (frames, centers, cascade, discharge, Lemma-A pre-metric core — affine, no metric) vs. **explicit layer** (traversal, orbit, measure — from T forward); Theorems 2–3 are the explicit layer deriving the metric; nothing in the skeleton depends on the explicit layer (checked). Theorem 2's real content: spanning must visit the conjugate seats (Q_j = 1ₙ·cos2θ reaches −1ₙ at m±); the native measure reverses sign there → forces Postulate Q. Checksum: 三 :: the conjugate-seat passage (二生三 strengthened — where the third position is and what it costs). Structure: wall (T1−) / door (T1+) / toll (T2). [derived; verified]

**N8. Not yet walked.** Theorem 3 (signature forcing) and the Postulate Q fight (why *quadratic*; candidate: Q is a corollary of measure-language being quadratic-native, R5, not a free postulate) remain queued.

## The rotation/winding/topology thread (R10 + pedagogy)

**R10. Rotation resolved into three register-layers.** (1) circle as locus [implicit]; (2) rotation as symmetry + winding as static topological invariant [implicit]; (3) traversal [explicit, gives order not duration, via T] → motion [only after irreversibility derives succession]. e^{iθ} = one formula, three readings (R9 instance). Engine verified non-circular w.r.t. time (motion-vocabulary = layer-3 gloss on layer-2 fact). "Orbit" is register-ambidextrous (set and path), which hid the code-switching. v7.7 propagation: re-type the engine's "must go around" wording. Checksum: 反者道之動 :: motion as the explicit face of return-structure [candidate, P2].

**Winding/metric pedagogy (for the book, Ch. on topology-before-metric).** Winding number = signed integer count of loop-around-point; robust under any deformation not crossing the point; blind to length/shape/speed — that blindness is the feature (isolates the one forced fact from infinite irrelevant variation). Topological (survives stretching) vs. metric (needs a ruler): the rubber-sheet test. Orthogonality straddles the boundary — independence is near-topological (cheap, early), the specific 90° is metric (expensive, downstream); the chain's "independence fixes no angle" is this distinction. Circumference (metric) and winding (topological) are unrelated — both computable of the one privileged loop by separate arguments; their independence is *why* Theorem 1 (topology) precedes the metric derivation.

## The founding observation (LOCKED into the chains this session)

**F1. The slope-product fact, secured in all three registers.** At Pₙ: gradient tangent slope −1, balance slope +1, product (−1)(+1) = −1 (perpendicularity met exactly); deficit σ(x₀)+1 = 1 − 1ₙ/x₀², zero only at the seat, = the verified tangent·B dot-product form. Seat slopes {−1,+1} = the conjugate pair Route 2 uses to force i [observed]. Marked the project's first result. **Math chain**: locked at head of seat-phase section, wired to engine P-type deficit + appendix item 1. **RSM chain**: register-neutral form at §6 ("continuous deficit distinguishing one locus — the seed"), naming math and DDJ as its translations. **DDJ chain**: 弗居 / 玄 mark the locus lexically. Interlingua demonstrated on the founding result.

## P₀ and the two infinities (the deepest excursion; candidate-heavy, high value)

**P0-1. P₀ = Fix(ν) — then re-typed.** First formalization: P₀ = the fixed-point locus of conjugation (−x = +x, the mode indistinguishable from its own negation); projectively Fix = {0, ∞}, plane Fix = Oₙ alone, orbit Fix = ∅. **Will's correction (accepted):** ∞ is not a value and |1| ≠ ∞ — treating the projective ideal point as |1| reified a non-termination into a point (the C6 type-error, committed outward). Re-typed: the projective fixed-point result is a *rendering-register section* (lawful, R9), not ontology. Surviving [derived] core: Oₙ = the plane's unique finite ν-fixed point (unoccupiability as fixed-point-of-conjugation); ν acts fixed-point-freely on the orbit (admissibility = ν-freeness; exclusion of P₀ ⟺ no fixed obtaining point) — ties Route 2, winding, local-face-of-P₀ into one statement. FU's sign-blind kernel must be checked against ν-freeness (magnitude-sign-blindness ≠ sign-fixed-point-freeness; must not merge).

**P0-2. The two non-terminations are one journey [derived; verified].** On G, outward-in-X = inward-in-Y, with X·Y = 1ₙ the exchange rate; the outward arm converges to the mode axis (asymptote through Oₙ), never merging. **Answers Tree-5:** Oₙ carries the point↔axis dual type — inward's unmet object = O-as-point, outward's unmet object = O-as-axis (the asymptote). Tree, slice geometry, and mode algebra = three registers of one fact. |0|/|1| re-stated as the two *counterfactual terminals* of the one journey, denied respectively by P1's two halves — **face-unity without compactification** (cleaner than P0-1's projective version; register-clean).

**P0-3. Math cannot intrinsically differentiate inward-∞ from outward-∞ [Will's claim; verified in the strongest form].** The inversion x ↦ 1ₙ/x (= σ_B on G) *exchanges* the two non-terminations; fixed point = the seat. So the two infinities are one structure read from opposite ends, pivoting on Pₙ (C5 extended: the seat is the balance point between the two non-terminations). 
- **Parsing argument [candidate; a NEW independent argument for the two-mode requirement]:** a single mode cannot disambiguate its own ends ("x→∞" is intrinsically ambiguous); the co-mode is the chart-at-infinity (w = 1/x); conservation is the transition function. Co-constitution acquires a *parsing/informational* face, independent of the ontological one.
- **"x→∞, y→∞" parsed [candidate]:** impossible in any single frame (conservation forbids); meaningful as the trans-scale direction along B (x·y = x² sweeps {xy=c} without terminal) — an R7 cross-frame statement, unparseable in single-frame notation. **S1 quantified:** B is the scale-axis, c = xy the ladder parameter; inward (c→0) child-ward, outward (c→∞) parent-ward; the two infinities = the two directions of the generational nesting, meeting balanced/exchanged/fixed at the seat.

**P0-4. DDJ checksum [candidate, P2, caged from spine].** Will: "the DDJ calls this yin and yang." The two non-terminations / two 欲-orientations (X8) / the two ends of the one journey :: 陰 and 陽 — candidate for the DDJ chain. Priced cheap under shared authorship; direction-guarded (DDJ→observation). One line, DDJ chain.

## Open gates (updated)

- **L1** — sole head of the audit (floor-dissolution + isotropies + child-existence).
- **The anchoring line** (why every finer frame is parent-seeded) — Tree-2 supplied the candidate shape (child seeds where parent already supplies both a generative surface and a radial member); to be written in the geometry.
- **Postulate Q** — derive from R5's quadratic-native measure language, or carry as premise (Node 8).
- **Tree-4** — is the curved-relative deficit a 2D-slice artifact? (3D line-meets-surface vs. 2D line-meets-curve) — 3D form still to run; N6 blocks the "artifact ⟹ harmless" reading.
- **Compactification paragraph** — if P0-1's projective face is kept at all, the |1|-face-as-ν-fixed-ideal-point needs its P2-at-the-limit justification; but P0-2 may make it unnecessary (face-unity without compactification).
- **Phys-1** — does the section-principle have a quantitative floor rooted in X·Y = 1ₙ (uncertainty-shaped)? [to derive in math]
- Carried: L2 retired-as-malformed; z²-uniqueness; dimension-3 rival derivations; cross-frame unit relation; Definition T; i²=−1 route independence; branching-beyond-seats; O₁ uniqueness; the 天/地 X-conflict; the A37 溺 ladder; Ch. 40 P1 test.

*Supplement r1 — appended to the consolidated ledger. Nodes 1–7 walked and closed; Node 8 queued. Highest-value new material: P0-2 (Tree-5 answered; face-unity without compactification) and P0-3 (the parsing argument — a new independent ground for two-mode co-constitution). Not sealed.*

---

# LEDGER SUPPLEMENT r2 — Nodes 8–11 walked; three premise strikes; r2 set produced

**N8 (ruled by Will).** Postulate Q **struck**. Derivation: ν-parity (kills odd degrees) + Will's ladder-covariance closure (M(λz)=λ²M(z) forced since the unit itself transforms as λ²·1ₙ — kills mixed degrees AND non-polynomials; upgraded beyond his stated claim) + the Finsler hole named (counterexample r²(2+cos4θ): passes parity/exchange/covariance/positivity, fails parallelogram by −8) + closed via the slice identity (the measure is the other real slice of the already-fixed law; an ungrounded measure = unframed measure, L1's exclusion in metric clothing) + Theorem-3 positivity selects Q_i. **One selection, three languages** (Thm 2 measure-face / slice form-face / Route-1 algebra-face). 明/名 checksum ratified by Will (明 :: Q_t, 名 :: √Q_t); forward question: does the 明-cluster mark the *selection*?

**N9.** **FU dissolved.** Kernel → theorem (σ leaves Q_i invariant — sign-blindness computed, verified); calibration → inheritance (the complexified law arrives α=γ=1, unit level set by the law); universality → frame-definition + c-ladder; four-seat magnitude computed. Seating commitment survives separately. **Concentration risk named:** F, Q, FU all dissolve through R8 + frame-definition + L1 — leanness is leveraged; if R8 or L1 falls, three postulates re-inflate. 恆-referent updated (cross-frame invariance now = frame-definition + ladder-covariance); 自然 candidate filed.

**N10 (Will's re-grounding: "you can't walk through a tree, even if the pith is hollow").** Standoff derived F-free: the interior contains no obtaining positions of frame n (definitional via L1 — sub-1ₙ *is* frame n+1, not a location here); the exclusion is structural, not obstructive — no *through* exists, only *around*; a whole-frame enclosure fact, not a local-center fact; the tree speaks it natively in the 3D register (with Tree-3/Tree-5, votes rotated-form-primary). Minimal traversal = the tightest loop still composed of obtaining positions; floor tangency carried.

**N11.** i² = −1 **promoted** — a **two-paths pair** (second instantiation of the strongest internal-parallax structure; first: the ground). Deletion-test passed both ways (Route 1 never uses co-constitution-of-±1; Route 2 never uses closure/spanning; shared ground-level exclusion only). Consolidations: Route 1 = the algebraic face of the N8 selection event; Route 2 = k=1 of the **binding-extortion schema** (k=2 = dimension three) — schema to be written abstractly, auditor validates once, both instances come; the plane itself is extorted, not posited.

**r2 set produced:** math chain r2 (premises down to Conditional + Bridge + T + seating + L1; appendix 19 items; open items re-ranked with L1 #1, anchoring line #2, R8-dependency #3); RSM chain r2 (sign-collapse P₀; face-unity via counterfactual terminals; parsing ground for two modes; O's dual type; B-threads-the-generations; stale L2 genus reference fixed; L1 at four loads); DDJ chain r2 (明/名 ratified; 恆/常 updated; 自然, 陰/陽 candidates filed).

**Walkthrough position:** Nodes 1–11 walked and closed (12 = seat-phase, locked earlier). Genuinely unwalked: the recursion nodes end-to-end under all rulings, branching/irreversibility detail, substrate. **Audit heads: L1, then R8-dependency, then the anchoring line.** Not sealed.

---
# LEDGER SUPPLEMENT r3 — anchoring attempt; adversarial pass; provenance map
**Anchoring line [candidate; residual named]:** finer-than is relational → comparisons are framed (L1) → cross-frame expression required → inheritance is the mechanism → seat-uniqueness fixes the site → "free-floating finer frame" is a contradiction (fineness IS the relation). Sibling refinement: commensurability = shared ancestry. Residual (step iv, inheritance-uniqueness) **= z²-uniqueness: open items 2 and 5 MERGED** ("uniqueness of the cross-frame expression mechanism").
**Adversarial pass r1 (file):** Finding 1 — Q-strike step 3 SOFT: the Finsler counterexample is framed (rational in law-objects); L1 doesn't exclude it; the smoothness closure is unavailable at the excluded origin; Q struck CONDITIONALLY on polynomiality or a named **generation principle** (premise count 5 or 6, Will to rule). Finding 2 — Lemma A near-definitional (straightness is register-relative); tag softened; discharge unharmed. Findings 3–5 — FU-dissolution, discharge rebuild, two-paths pair all survive. Both hits landed on M-tagged items; zero on W-tagged.
**Provenance map (file):** every load-bearing r2 element tagged W/M/V. Pattern: seeds and gap-closures are W; apparatus is M; the pass's findings tracked provenance exactly.
**Rulings needed from Will:** (1) generation principle — name as premise 6, or set as derivation target; (2) accept the merged open item; (3) accept Lemma A's softened tag.


---
# LEDGER SUPPLEMENT r4 — index-relationality, tree-primacy, whirlpool; r3 set locked

**Index-relationality [Will's double proportion; derived].** 1(n-1):trunk::1n:branch AND 1n:trunk::1(n+1):branch simultaneously — the index is gauge; subscripts name relations, never positions. **Tree-primacy [Will's correction]:** the nesting is a tree, not a ladder — arborescence is the PRIMARY forcing (path-dependent indices; no cross-branch count), recession secondary; "ladder" demoted to lineage-slice (R9 rendering artifact); c renamed lineage parameter; "ladder-covariance" renamed scale-covariance. Notation: the anchoring argument's 1_m struck (question-begging), replaced by unindexed δ; the child-index is EARNED as the argument's conclusion. 天地不仁 extended to the frame-isotropy leg [candidate, P2]; ground exempt by mechanism.

**Whirlpool [Will's image; verified core].** Free-vortex profile v·r = const IS the conserved-product law in the flow register; trunk-taper radius·height = const is the same law in the structure register. Tree and whirlpool = structure-face and flow-face of one law; complementary presence (X10 sharpened: the complements share one equation); vortex core renders O-as-axis. GUARD: "plucked out" is picturesque falsehood — the exclusion is constitutive, not historical; nothing was ever removed. 淵 (Ch. 4) filed [P2]. Control-4 §2.3 (vortex cores) gains context.

**Typed Euler identity [Will; verified; observation register].** e^{iπ}+e^{i2π}=0 = the two returns (structure-return at π, place-return at 2π) as ν-conjugates balancing to the excluded center; general antipodal sum; four-seat balance Σ seats = 0. Filed to the Euler/parallax section with the 0.9x-history flag; "cost" vocabulary struck from the π-reading (Will's correction — ratio/phase-measure, not cost; the curing-pile i=ln(−1)/π gloss carries the same ill-typed word); deflation risk of the Euler cluster noted.

**r3 set locked:** math r3 (index-relationality; Q downgraded to conditional with the generation principle named; Lemma A softened; anchoring line written in δ-notation with residual merged into cross-frame-expression-uniqueness; appendix items 20–21); RSM r3 (index-relationality register-neutral; structure/flow complementary renderings with the constitutive-exclusion guard); DDJ r3 (不仁 frame-leg; 淵).

**Rulings still queued for Will:** (1) the generation principle — premise 6 or derivation target; (2) Lemma A's softened tag — accept; (3) the merged open item — accept. Not sealed.


---
# LEDGER SUPPLEMENT r5 — the complementarity floor
**Open item 10 closed to candidate.** σ_a·σ_b ≥ 1ₙ/2 over floor-compliant, ν-symmetric, winding-measured closed spanning structures; saturated uniquely by the minimal traversal (the framework's minimum-uncertainty state is the forced orbit). Two-line proof from canon premises. **Mechanism: floor + symmetry + winding measure — no Fourier duality, no commutator.** Load-bearing premise found by counterexample: the winding-coordinate measure (a seat-dwelling measure violates the bound on the same circle) — residuals: force it from isotropy or name it; the non-star-shaped extension. Parallax record: question imported (Phys-1/Heisenberg), answered natively with a DIFFERENT mechanism producing the same form — the difference is the nuance the import bought. Physics §3 + prediction 8 upgraded (quadrature/coherent-state locus [P2, verify]); math r3 amended (appendix 22). Not sealed.


---
# LEDGER SUPPLEMENT r6 - the relativistic cluster; the miss resolved
Will's ratio question (X/Y = 1n) broke the E=mc2 miss open; corrected assignment ACCEPTED AS PROPOSED. (E, pc) :: (a, b) - the split coordinates; m :: the frame unit (not a mode; the invariant of the particle's own frame); 1n :: (mc2)^2 - resolving the unit double-booking generally (1n maps to whichever unit the rendered frame carries: hbar for action, rest mass per particle - frame-universality rendered). E = mc2 :: the seat statement (floor tangency; E >= mc2, equality at rest); massless :: the null cone (no seat on the null directions - why photons never rest); the rest-ray E/m = c2 :: B as the scale-axis threading all particle-frames' seats (the ratio landing at the trans-frame level); mass-shell hyperboloid = the cluster's rotated face. The miss re-typed as case study: refused under wrong typing (E, m as modes), resolved under right typing (coordinate vs invariant) - the refusal was the method protecting the assignment until the r3 typology existed. c2's three faces filed (coefficient-unity; boost-invariant cone; Qj sign-boundary); no :: between 1n and c2 - type mismatch, the absence itself the finding. All P2, rendering register, maps-to-never-is. Not sealed.

---
# LEDGER SUPPLEMENT r7 — Gemini adjudication; math r4; the z² reduction
Gemini's three edits adjudicated against the verified record: Edit 1 ACCEPTED as consolidation (Amplitude Floor; physics parenthetical STRUCK on entry — register violation; scope symmetrized); Edit 3 ACCEPTED with the c-refinement; Edit 2 REJECTED — the product/ratio orthogonality premise is the image-9 error independently reproduced (attractor confirmed), plus circular lineage-continuity, posited G-membership, and "sealed" status inflation. **Divergence Ledger founded** (divergence_ledger_r1.md; entry 001, with the precision note: lineage-seat B-collinearity true via inheritance; the ROUTE failed). Gemini accepted the correction on computation — a divergence that closed under evidence.
**The z² reduction [candidate]:** the true orthogonal net IS z²'s level-net (Re = XY, Im = (X²−Y²)/2; net-orthogonality = Cauchy–Riemann). Cross-frame maps under (i) holomorphy [premise-candidate; R6's formal face], (ii) ν-evenness [canon], (iii) net-rectification [verified; z⁴ sends G to a parabola] are forced to φ = αz² + β — unique up to α = the cross-frame unit relation (item 6). The merged open item DECOMPOSES: "why holomorphic" (small) + item 6 (pre-existing). Math r4 carries all three changes. Not sealed.
