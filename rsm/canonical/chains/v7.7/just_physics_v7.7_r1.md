# RSM — Just-Physics Chain (v7.7, r1)

**Rendering register.** Per the mission ruling: this chain is a *translation* of the RSM into the language of physics — the second target language, not a witness. Every entry is a correspondence (`::`, imperfect structural pointing, never identity — "maps to," never "is"), priced P2 under shared authorship, and **carries zero evidential weight for the derivation.** Evidential weight lives in exactly one place: the falsifiable-predictions section, and only when a prediction is preregistered and checked. Physics quantities and results cited here are from machine memory and inherit a standing **[verify against sources]** obligation. Aligned to math chain v7.7 r3 and RSM chain v7.7 r3. Machine-drafted (Claude, Fable 5); re-weighings are Will's. Not sealed.

Ordering follows the math spine: each section renders a derived node. Tags: [P2] correspondence; [flagship] strongest instance; [miss] a refusal, displayed; [open exposure] a live threat; [prediction] falsifiable; [target] a derivation the math owes before the physics face can be claimed.

-----

## 1. The excluded terminal :: vacuum and concealment

**|0| :: the vacuum is not absolute nothing** [P2]. The physical vacuum is structured — fluctuations, zero-point energy, no state of literal total absence. Physics has never met |0| and its formalism cannot write one: the framework says why (a condition failing to cohere, not a state failing to obtain).

**|1| :: gravitational self-concealment** [P2; blind-found, control-4 §2.1]. Driving distinction-density toward saturation forms a horizon that conceals the distinctions — the plenum self-destructs into hiddenness as the void self-destructs into incoherence. The |1| face, previously missing from this register, symmetric with the |0| entry.

**The scale-faces** [P2]: apparent minimum scale (Planck floor) and apparent maximal extent :: frame boundaries, not terminal resolutions — per the conditional, no terminal exists in either direction; what physics meets are the *frame's* limits, read from within. (Feeds the Bekenstein sort, §8.)

## 2. The two slices :: Wick rotation and the two signatures

**The strongest new rendering of this revision** [P2, flagship-candidate]. The math's one law Q(z) = 1ₙ has two real slices: Q_i = a² + b² (Euclidean) and Q_j = a² − b² (split/Lorentzian-signature), exchanged by b ↦ ib — and physics possesses exactly this structure as **Wick rotation** (t ↦ iτ): Lorentzian field theory and Euclidean statistical mechanics as two real faces of one analytic object, routinely continued into each other. Physics treats the rotation as a computational device; the framework renders it as the two-slice identity — *the gradient is the orbit* wearing spacetime signature. The **null cone** (the b = ±a directions where Q_j vanishes) :: the light cone; Theorem 2's forced crossing :: no spanning without touching the cone. [Machine-memory physics; verify.] **c², three faces [P2]:** (i) the metric-coefficient ratio between the two split-directions — derived as unity; the empirical value is unit-inheritance (physics' own c = 1 practice agrees); (ii) c-invariance :: the null directions are boost-invariant (e^{jφ} preserves Q_j and the cone; verified); (iii) c-as-limit :: the sign-boundary of Q_j (Theorem 2). No `::` between 1ₙ and c² — different types (magnitude floor vs. shape coefficient); the absence is itself the finding.

**The relativistic rendering cluster [P2; accepted by ruling; the resolved miss].** The conjugate pair is **(E, pc) :: (a, b)** — energy and momentum as the split coordinates. **Mass is not a mode: m :: the frame unit** — each particle is a frame; its mass shell E² − (pc)² = (mc²)² :: its own gradient Q_j = 1ₙ, with **1ₙ :: (mc²)²**. (This resolves the unit question generally: 1ₙ maps to whichever unit the rendered frame carries — ℏ for the action frame, rest mass for a particle frame; frame-universality, rendered.) Consequences, each landing in verified geometry: **E = mc² :: the seat statement** — floor tangency, E ≥ mc² with equality exactly at rest (b = 0); **massless :: the null cone** — m = 0 is Q_j = 0, photons live on the cone (E = pc), which is why they never rest — no seat on the null directions; **the rest-state ray E/m = c² :: B as the scale-axis** — one straight line through O threading the seats of all particle-frames (Will's ratio proposal, landing at the trans-frame level); the mass-shell hyperboloid s² − r² = 2·1ₙ (verified, math appendix 11) as this cluster's rotated face, vertex at the seat. [target, direction only: if rest :: seat, the seat's structure (non-constitutability, discharge) should eventually say something about rest that standard physics says differently — a future predictions-section question, not one now.]

**The two returns :: the double cover in spin** [P2, candidate — held gently]. The math's invariant returns at π while position returns at 2π (Q_j = cos 2θ). Physics carries a celebrated period-doubling of its own: the spinor double cover (a 2π rotation is not the identity on the state; 4π is). The structures rhyme — a two-to-one covering between "structure-return" and "place-return" — but the mapping of which period corresponds to which is *not* settled and must not be forced. Filed as a shape-match awaiting the exact correspondence or its refusal. [target: state the map precisely or drop.]

## 3. The floor and the conserved product :: the uncertainty relation

**X·Y = 1ₙ :: Δx·Δp ≥ ℏ/2** [P2, flagship]. The framework's central law is a *conserved product of two co-obtaining conjugate quantities with a floor at the frame's unit*. Quantum mechanics' central inequality is a *bounded-below product of two conjugate spreads with the floor set by ℏ*. Shape-identity: reciprocal conjugacy, product form, unit floor; ℏ :: 1ₙ's physics face — the quantum of action as a frame's minimal unit of distinction. The old chain rendered this as "the uncertainty floor is the standoff"; the two-slice work sharpens it: position/momentum are Fourier conjugates — one state, two representations — which is the section-principle (R9) in Hilbert space. Guard (Will's, standing): **maps to, never is.** The framework does not derive ℏ, the ½, or Fourier conjugacy. [**upgraded — target derived to candidate (math r3, the complementarity floor):** σ_a·σ_b ≥ 1ₙ/2 over admissible structures, saturated by the minimal traversal, from floor + ν-symmetry + winding measure — **no noncommutativity**. The rendering sharpens: uncertainty-form relations can arise from exclusion floors alone; the exact physics locus for comparison is the quadrature-spread bound with coherent-state saturation (a,b :: the quadratures; the orbit :: the coherent state) [P2; machine-memory; verify]. Same shape, different mechanism — the difference is the finding. Maps to, never is.]

**Unitarity and no-cloning :: the floor's dynamical face** [P2; blind-found, control-4 §5.3.2]. Distinguishable states remain distinguishable under evolution — five decades of the black-hole information episode as the discipline refusing to surrender the principle. Carries prediction 7 (§9).

## 4. The seat-phase structure :: critical points

**Pₙ :: the critical point** [P2; blind-found, control-4 §5.3.3]. The one place physics holds a system at a balance position: occupiable only under continuous external tuning (a misalignment cost); scale-invariant exactly there (the frame's characteristic unit washing out at the generation locus); conformal. The laboratory face of the seat — met, and not constitutable as a standing state without maintenance. The corrected local/global law carried: a single gradient's balance is occupiable where other distinctions carry the load; only the all-gradients balance is excluded.

**The founding observation's physics shadow** [P2, minor]: perpendicularity achieved at exactly one locus and degrading measurably off it :: order parameters vanishing at criticality with power-law approach. Not pushed.

## 5. The engine and recursion :: the big bang as parturition

**Pₙ → Pₙ + O₍ₙ₊₁₎ :: the big bang** [P2, carried]. Not an explosion in space but the obtaining of a new frame at a parent's generative locus; the manifold-excision fit (the singularity as excised point — unmet origin, not early event) carried from the old chain as its strongest general-relativistic reading. The mother persists: the parent frame is not consumed by the child's obtaining.

**Structural recession :: cosmological acceleration** [P2 + prediction-linked; locked canon carried]. Time linear and the universe accelerating *from any frame's perspective* because Pₙ → O₍ₙ₊₁₎ is irreversible (reversal = P₀ obtaining, excluded) and each new origin is constitutively unreachable from the frame that generated it. Rendered against dark energy as the standard account's placeholder — **the distinguishing empirical signature remains the open operationalization** (§9, prediction 4; and the explicit open task: distinguish structural recession from ΛCDM observationally, still unbuilt).

**The two arrows [open exposure; audit pending].** This chain derives the arrow from parturition irreversibility. The control-4 run derived it from frame-scale shear (fine-grained distinguishability conserved; coarse-grained entropy frame-relative; the arrow as their divergence). Rivals, or a two-paths pair — the break-test stands: is there within-frame entropy increase with no generation event? Thermodynamic relaxation looks like exactly that. Burden on the bridge; unresolved; carried honestly.

## 6. Index-relationality :: the relativity principle

**The index is gauge :: no preferred frame** [P2, new]. The math's r3 result — subscripts name relations, never absolute positions; every frame reads itself as "n"; no self-reading privileged — is the *shape* of the relativity principle: physics' own insistence that no inertial frame is distinguished, that coordinates are labels, that only relations carry content. One line further, held gently: index-gauge :: gauge freedom generally (physical content = the gauge-invariant) [candidate, one line, not pushed].

**The renormalization group :: the lineage structure** [P2, ranked first among the blind-found anchors — control-4 §5.3.1]. RG flow as frame-nesting in working clothes: coarse-graining :: the map between lineage rungs; fixed points :: scale-invariant (frame-free) loci; universality classes :: frame-independence of structure; relevant/irrelevant operators :: which distinctions survive passage toward the parent scale. **Directionality flag carried:** RG coarse-grains fine→coarse (child→parent); the framework's generation runs parent→child — the flows are opposed, and the correspondence must be stated as inverse maps or it is wrong. RG's real contribution is *numbers* (measured critical exponents), which is why it feeds prediction 4 rather than the catalogue. [Machine-memory; verify.]

## 7. The tree and the flow :: vortices, taper, mass-shell

**The whirlpool :: G in the flow register** [P2; core verified in-math]. The free-vortex profile v·r = const *is* a conserved-product curve — speed and radius reciprocal, divergent toward the unreachable core, vanishing outward [fluid premise from memory; verify]. The trunk taper (radius·height = const, the horn) is the same curve in the structure register. One law, two medium-renderings; each register's presence the other's void. **Vortex cores** [P2; blind-found, control-4 §2.3]: the empty core (superfluid: density driven to zero) :: the unmet center; the filled core (superconductor: a *different phase*, neither bulk mode, at the center, order persisting around it) :: co-presence — the parturition's co-presence face, explicitly *not* its generative face (the core-phase is not a child frame).

**The generative surface :: the cambium** [P2; correction propagated]. The living toroidal/generative layer of a tree is the **cambium** (not the pith — the old chain's pith-toroid claim is dead and stays dead; this rebuild removes it). Cambium :: the G-surface (the revolved gradient; the wheel's rim); medullary rays :: B (spokes, hub-to-rim); pith :: O with its **dual type** — point in the cross-section, axis in the whole (the math's O-as-point/O-as-axis, displayed in wood); branch node :: the parturition locus (ray ∩ cambium; trunk-frame reads it as a crossing on itself, branch-frame reads it as its own origin — one locus, two frame-readings, in cellulose).

**The mass-shell** [observed, one line]: rotating G about B yields s² − r² = 2·1ₙ — a hyperboloid of revolution, vertex at the seat, asymptotic to the 45° cone through O; the mass-shell geometry (p² = m², asymptotic to the light cone) with the seat as the rest state. Priced as found-not-predicted; not pushed.

## 8. The Bekenstein sort [open exposure — the chain's honest wound]

Finiteness: **closed.** The bound (finite distinguishable states in a bounded region) is the frame-floor's face — the distinction budget of a bounded region is finite *within a frame* (1ₙ), while no-terminal-resolution is a claim *across* frames, each child carrying its finer unit. The bound is not a counterexample to the conditional but the frame-bound face of the floor.

Area-scaling: **open, and it is the sharper threat.** The bound scales with *area*, not volume; a naive cross-frame story (finer distinctions per unit volume) gives volume scaling and *contradicts* holography. Nothing in the framework currently forces area over volume. Filed as the place to be adversarial, not to bank: if the cross-frame unit relation (math open item 6) is ever closed, its first test is whether it produces area-scaling; if it produces volume-scaling, the framework has a live contradiction with holography. [The sharpest available number for prediction 4: S = A/4l_P² as a power law between nesting levels. Machine-memory; verify.]

**Particle number is frame-relative** [P2; blind-found, control-4 §5.3.4]: the Unruh effect — the most manifest-seeming quantity in physics carries no frame-independent value; mode-content is frame-relative. Rendered without further comment.

## 9. Falsifiable predictions (the only evidential section)

Carried and extended; each requires preregistration before any check counts:

1. **Power-law nesting** [ranked first]: cross-frame unit relations, if closed, take power-law form between nesting levels; operationalize through measured RG critical exponents and/or the holographic area law. [The framework must *derive* the exponent-form before the check; currently a target.]
2. Structural recession vs. ΛCDM: a distinguishing observational signature (the explicit open task; unbuilt).
3–6. Carried from the prior chain's list unchanged pending re-verification against the source document [not reproduced here from memory — pull from canon before citing].
7. **Unitarity holds** [new, blind-found]: objective-collapse theories violate it by design and are empirically live and increasingly constrained; their confirmation would strike the floor's dynamical face directly. A genuine kill-condition.
8. **[candidate-derived; not yet a prediction]** The complementarity floor σ_a·σ_b ≥ 1ₙ/2 now exists in the math (r3). To become a prediction it needs an operationalization: a system where the floor-mechanism (exclusion, not noncommutativity) makes a *distinguishable* quantitative claim. Unbuilt; honest status: derived shape, no discriminating test yet.

## 10. The misses (displayed, not hidden)

**E = mc²** [miss → case study; resolved, r1 addendum]: refused for two chain-versions when E and m were forced into the mode slots — and the refusal was CORRECT: they are not modes (E is a split coordinate; m is the frame unit/invariant). The rendering became possible only after the r3 typology existed (split coordinates; frame-unit-as-invariant; seat-as-floor-tangency; B-as-scale-axis) — see the relativistic cluster. Record: refused under wrong typing, resolved under right typing; the refusal was the method protecting the assignment until the types were available. The method's ability to decline, and to un-decline for stated structural reasons, displayed in one entry. **The pith-toroid** [retracted]: killed by the source (Esau; the living toroid is the cambium); the retraction improved the fit (§7) — the record of a correction that made the rendering better, kept visible. **Dimensionality** [downgraded]: "three necessary [derived]" is not this chain's to claim; the math carries two unadjudicated rival derivations; this chain renders neither until one is promoted.

-----

*v7.7 r1 — physics chain rebuilt from scratch. Posture: rendering register per the mission ruling (translation, not witness; predictions-only evidence). New in this build: the Wick-rotation/two-slice rendering (flagship-candidate); index-relativity :: the relativity principle; the whirlpool/taper/mass-shell cluster with the cambium correction propagated (pith-toroid removed); the RG entry with directionality flag; unitarity + prediction 7; the |1| face installed; the two-arrows exposure and the Bekenstein area-vs-volume exposure carried as open wounds; the spinor double-cover shape-match filed gently; the uncertainty flagship re-grounded in the section-principle with its quantitative-floor target. All quantitative physics from machine memory flagged [verify against sources]. Predictions 3–6 to be pulled from the prior canonical document, not from memory. r1 addendum: complementarity-floor target derived to candidate (math r3); §3 and prediction 8 upgraded with the mechanism-difference stated. r1 addendum 2 (ruling accepted as proposed): relativistic rendering cluster installed; E=mc2 miss rewritten as the refused-under-wrong-typing case study; c-squared three faces installed with the no-correspondence-to-unit finding. Not sealed.*
