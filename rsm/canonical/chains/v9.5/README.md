# RSM v9.5 — the current working set

Produced 2026-08-03 by applying chain patch set 3 (FINAL for the v9.4 line) to v9.4.2. Rulings
are Will's (sessions 2026-08-02/03); drafting Claude's; wording approved in session. **Not
sealed.** Markdown is the canonical layer; the v9.4 directory is kept as history.

| Chain | File |
|---|---|
| Structural (the spine) | `structural_v9_5.md` |
| Just-Math | `just_math_v9_5.md` |
| Just-Physics | `just_physics_v9_5.md` |
| Just-DDJ | `just_ddj_v9_5.md` |

**What v9.5 changes** (a content step, not a drafting correction):

- **Transfer → Recursion** — §7 and the math theorem restate the recursion as **two readings**;
  transfer-language survives only where it names the denied thing. "The constitutive share" is
  HELD, replacement noun unruled.
- **P₀'s presentation set** — the numerals return to P₀: {|0|, |1|, −1 = 0 = +1} as empty face,
  full face, sign-collapse (the trivial ring); numeral notations name faces, never values. The
  |1| cascade is withdrawn (unattributed); **"exactly three" is open with no standing candidate
  derivation.**
- **The register typing of the P-series** (R25 completed) — P₀ is beneath the register split
  itself; Pₙ (n variable) is implicit; any particular index is explicit. The 名/道 naming rule
  enters the DDJ chain only, with Frame 1's own 可名: 1 :: Reality :: 大.
- **The instance set widens** (R21) — structural §1 now carries all six strip-attested Ch. 2
  pairs; 前後 kept (physics §2 dependency). Attestation report in the diff report — note the
  strips read 先, not 前, with the 後-slot graph unresolved in the dataset.
- **The notation warrant** (R20) — the DDJ chain demonstrates rather than explains:
  又亡相生 :: XY = 1, neither expression ranked above the other.
- **The Mawangdui tag tier** (R17) — [G]/[M]/[R] with trust order G → M → R; the re-tag flips
  are session work (candidate list in the diff report).
- **Parametric expression** (R7) — new §0 paragraph; one sentence each in math ("no shared
  number owed") and physics (headnote); new structural open item 6 (is the environment derived,
  or what *explicit* means?).
- **The one `::` fix** (R8) — 道 now pairs with *the entailment as these chains state it* (a
  rendering), not the implicit structure as such; all other pairings stand as
  explicit-to-explicit.

Out of scope, standing: the 有無相生 / Ch. 40 rewrites (R5/R6 flags in place; co-perception set
aside by ruling) · the share-noun (held on crotch grain) · the DDJ finalization items (恆/常
footing, slip 34 ○, the 心-radical class, 用) · the open-items register regeneration (next
task) · editorial repairs.

Diff trail: `reports/v9_4_patch_diff_report.md` → `v9_4_1_patch_diff_report.md` →
`v9_4_2_diff_report.md` → `v9_5_patch_diff_report.md`.
