# RSM — Just-Math Chain (v9.52)

*The explicit realization. The implicit skeleton is affine — straight/curved only, no metric; this chain renders it and states its own measure. Everything a rendering adds — coordinates, a sign convention, a metric, a completed circle — belongs to the rendering, and where a result depends on one it is said so in place. Not sealed.*

## Notation

- **P₀** — the total paradox: the master pair (distinction/indistinction) at equality. Presenting faces: the empty face and the full face, sitting in native coordinates at nothing and without limit. Neither is a numeral. The presentation set is threefold: the empty face (|0|), the full face (|1|), and the sign-collapse (−1 = 0 = +1 — the trivial ring, arithmetic's face of total indistinguishability). The numeral notations name faces of the impossibility, never values. P₀ is the impossibility, beneath the register split; Pₙ (n variable) is implicit; any particular index is explicit. An ontological-logical impossibility.

- **Rₙ, Oₙ, Xₙ, Yₙ, Gₙ, Bₙ, Pₙ** — frame; origin; the two modes (magnitudes, and slots rather than names); the gradient Xₙ·Yₙ = 1ₙ; the balance line Xₙ = Yₙ; and Pₙ = Gₙ ∩ Bₙ, at (√1ₙ, √1ₙ) in rendering coordinates.

- **Bₙ does not obtain at any point** (structural §5). Neither does Pₙ. The coordinates above are where the drawing puts them, not places anything is.

- **σ_B** — the mode-swap X ↔ Y; on G the inversion x ↦ 1ₙ/x; in split coordinates it fixes a and negates b, fixed set b = 0. **σ_B is the structural involution.**

- **ν** — componentwise negation, fixed point Oₙ. **ν is explicit.** A sign is an indexed comparison, not a second obtainer.

## The reciprocity requirement

1. **Co-obtaining (P2).** Neither mode reaches a terminal while the other obtains.

2. **Exchange symmetry.** The law is symmetric under X ↔ Y — the structural symmetry.

3. **Class selection**. Additive conservation fails twice: it admits X = 0 (a terminal as an attainable value) and it caps both modes. Multiplicative conservation does neither: XY = 1ₙ admits no zero and no ceiling. Both of P₀'s faces are excluded here, at the gradient's two ends, by the law's own shape.

4. **Uniqueness up to reparametrization.** Any terminal-excluding symmetric law is a monotone reparametrization of the conserved product; the product form is the law in the modes' native magnitudes, and the native coordinates are fixed by the impossibility itself — they are the ones in which P₀'s faces sit at *nothing* and *without limit*. The chart is anchored by the very thing it excludes. The native-magnitudes step is unsettled.

## The frame, the unit, the floor

1ₙ is the constitutive magnitude and the minimal unit in one claim: any distinction beneath it is, by the Corollary, frame n+1 ("sub-1ₙ" is unit-relative typing, not size — cross-frame magnitude comparison is undefined). The floor is the Definition's own boundary; no floor postulate exists. 0ₙ is a limit-approach, never a value.

## The one law and its two slices

Over ℂ with z = a + bi, one equation Q(z) = 1ₙ carries two real slices:

- **Q_i = a² + b²** — the circle slice.
- **Q_j = a² − b²** — the gradient slice, with split unit j, j² = +1; in split coordinates a = (X+Y)/2, b = (X−Y)/2, the gradient XY = 1ₙ is exactly Q_j = 1ₙ.

For magnitudes X, Y the sum a is positive, and **the mode-expressible domain is the branch a ≥ √1ₙ** — the Amplitude Floor. The branch is what the modes express; the remainder of the circle is the chart's continuation of it, and b = 0 on that branch is Bₙ, which does not obtain.

**The circle slice** as a complete object requires a < 0 and is — a legitimate indexed continuation. Its content where it is mode-expressible stands.

**Scale and lineage.** Within a frame, scale is motion along G: the split action e^{jφ} — hyperbolic rotation — is the orbit of rescalings (λX, Y/λ), product invariant. The family parameter c = XY names the lineage direction along B: c→0 child-ward, c→∞ parent-ward, root-capped; a lineage coordinate, never a global count. The lineage direction is not scale.

**The two non-terminations exchange, pivoting on Pₙ**: the inversion x ↦ 1ₙ/x — σ_B on G — swaps the fine center-ward direction and the vast extension. Both non-terminations are the gradient's own ends, within the frame. This is the chain's cleanest computation of the structural involution.

## The Archimedean demonstration

The explicit register's cleanest exhibition of the framework's central facts is twenty-three centuries old. Archimedes bounded π between inscribed and circumscribed polygons: the inner perimeters climbing, the outer falling, the value trapped between two sequences that tighten forever and meet at no finite stage. Read structurally:

- **A limit approached from both sides and occupied by neither.** The inner and outer sequences are the two-sided approach to a locus no member of either sequence attains — the same species of fact as the gradient's approach to the balance line, and the explicit register's way of exhibiting it.
- **Each polygon is a frame.** The hexagon's ratio is exactly 3 — correct for a hexagon. The 96-gon's is 3.1416 — correct for a 96-gon. Each refinement is accurate at its own resolution and is not the thing it bounds. Indexed statements of one invariant: 可名 of a single 常名.
- **The non-terminating expansion is the engine's signature in a number.** Each further digit is the pursuit of exactness continuing below the previous digit's floor. The expansion does not terminate, and not from weakness of method: what it measures is the relation between straight and curved, and those are the skeleton's own two categories — the affine layer knows exactly this distinction and no metric. That the ratio between them can be bounded to any precision and completed at none is deferred exactness, rendered numerically.

Placement, stated: π's numeric content — lengths, the value itself — enters with the measure, downstream, like perpendicularity. What is register-clean is the shape of the situation: minimal closure by straight elements takes three sides (an affine fact); the two-sided bound; the refinement without end.

## Theorems

**Disconnection**. Bₙ does not obtain, so the X-dominant and Y-dominant portions of the mode-expressible branch have no path between them: any path joining them meets b = 0. The two portions are related — each is the other under σ_B — and not connected. This holds in the modes' own magnitudes, with no sign convention and no second component.

**Revolution**. The relation between the two portions is carried by revolution of Gₙ about Bₙ. A half-turn about B realizes σ_B as continuous motion, so the two portions exchange with nothing passing through b = 0. Revolution requires a direction the plane does not have; how many such directions the structure requires is open. The rotational freedom about B is the ring of orientations, and one selection is one branch.

**Recursion (two readings).** yAxis₍ₙ₊₁₎ reads Bₙ and xAxis₍ₙ₊₁₎ reads Gₙ; the child's origin follows as their crossing, O₍ₙ₊₁₎ = Gₙ ∩ Bₙ = Pₙ (structural §7). No map realizes the readings, and none is owed: "reads" is the vantage relation, not a transformation, and a function carrying parent points to child points would assert the transfer §7 denies. Whether any chart-level map models both readings at once is a rendering question, unasked by the structure.

**Indexed comparison across the frame boundary.** What survives from the earlier chart model: one referent in the parent may carry two indices in a child's chart, differing by ν. This is how a sign can be measured while being explicit — the measurement compares indices; it does not detect a second obtainer.

**T2 — crossing** — computation checked; the claim belongs to the rendering. Any closed spanning path crosses the null cone Q_j = 0; at the sign-image of Pₙ the gradient form reads −1ₙ. Spanning presupposes two components to span.

**The two returns** — computation checked; the claim belongs to the rendering. Along the circle, Q_j = 1ₙ·cos 2θ: the invariant returns at π, the position at 2π. A double cover — a fact about the circle slice. Euler's identity is this fact compressed, and the compression hides a winding: the familiar **+1** is ambiguous between e^{i·0} and e^{i·2π}, which share a value and differ in winding, so the identity decodes two ways. The traversal reading is

**e^{iπ} + e^{i·2π} = 0**

— out and back: the far pole and the returned position, summing to the center. The constitutional reading takes the same pair at θ = 0, without traversal. Both are instances of **one operation at two angles**: the general form is e^{iθ} + e^{i(θ+π)} = 0 for every θ, and no single θ produces 0 — the center is not on the circle. It is named only as the sum of a conjugate pair of poles: the ν-centroid's most famous instance, and the chart's way of saying that the symmetric whole names the locus none of its members occupies. All of this belongs to the rendering, and is entered as such.

**i² = −1, two routes**. Elimination over the five candidate closures, and the antipodal-binding argument. Results about the signed continuation.

**The ν-centroid**. Any sign-symmetric configuration sums to the center.

**Complementarity floor** — belongs to the rendering; the winding-measure premise is unsettled. Over admissible closed spanning structures, σ_a·σ_b ≥ 1ₙ/2, saturated by the minimal traversal. The minimal traversal's four stations are not a chart accident: a distinction co-obtains with its conjugate, so a seat-inventory has even parity by constitution, and a three-seat configuration is an incomplete inventory rather than a forbidden arrangement. Four is the first closure that exists. The geometric argument — that a three-station circuit either transits the center or fails antipodality — displays the constitutional fact rather than establishing it.

## The measure

The measure is definitional-by-register: Q_i is the circle slice's own quadratic form, and distances within the rendering are read from it. Whether quadraticity is forced rather than adopted is open — a non-quadratic candidate satisfying the coarse constraints exists and fails only the parallelogram condition. Until quadraticity is forced, "orthogonal" is a rendering fact: the skeleton says *independent*, and perpendicularity enters with the measure, downstream. The register the measure is defined on is itself explicit as a complete object.

## The engine (realized)

Continuity entails B, and G meets it at Pₙ. Denial A (origin): one constitutive pair per frame; n has no spare pair to grant the locus. Denial B (exactness): at exact equality nothing distinguishes the modes, so the condition does not obtain. Satisfaction as organizing-locus: in-frame, circulation about a locus nothing occupies; trans-frame, generation — **the child is the pursuit of exactness continuing below the floor**, its origin the approached locus read from below, its axis the parent's balance line, per the two readings (Theorems). Recursion is exactness, permanently deferred.

**No address.** Pₙ's coordinates are where the rendering puts it; nothing is sited there. The standing residue of sub-unit imbalance entails the child. The incident-pair deficit (1 − 1ₙ/x₀², rendered as perpendicularity once the measure is in force) is corroborating, never load-bearing.

## What this chain does not owe

No exponent, no dimensionful constant, no scaling law. The implicit register fixes structure and fixes no parameters; a branching law with exponent two and one with exponent three instantiate the same structure. Quantities enter only with an instance, and instances are the explicit register's business. Exponents differ between instances because parametric environments differ; the shared structure owes no shared number.

## Open items

1. **The open closures** — the native-magnitudes step; the measure's definitional typing; anchoring-by-constitution (resolved negative at the structural chain; downstream consequences open).
2. **The cross-frame unit relation** 1ₙ ↔ 1₍ₙ₊₁₎ — whether any quantitative relation accompanies the constitutive share. An explicit-register question.
3. **Dimension** — revolution about B requires at least one direction beyond the plane, and the full argument runs further: with no orientation privileged, the complete set of arcs over the center sweeps a sphere, and a sphere requires at least three dimensions while requiring no fourth. A second route reaches the same object statically: a circle is all potential points equidistant from a shared origin in a plane; a sphere is all potential points equidistant from a shared origin in three dimensions. "At least three" stands on these routes; the premise (no preferred orientation) is register-clean, while equidistance is measure-content and the quadraticity of the measure is open. **"Exactly three" is open, with no standing candidate derivation:** the binding-extortion schema rests on the binding requirement, which is chart; the previously listed cascade is withdrawn. That nothing *requires* a fourth is still not that nothing can use one.
4. **Cross-frame expression** — how a parent states any fact about a child's interior.
5. **Definition T (traversal)** — the formal object realizing ordered passage. Traversal as previously stated runs in the signed chart and needs restating in the mode-expressible domain, where the disconnection theorem now lives.
6. **The tree-question** — sibling structure and commensurability (shared ancestry as the only common register).
7. **Branching beyond Pₙ** — whether any relation other than the constitutive pair can be inexact in the constituting way. Candidate, unruled: every conjugate equality within the frame reduces to x = 1ₙ/x, and the other points of B belong to other frames.
8. **Quantitative complementarity** — the winding-measure premise; the non-star-shaped extension. A question about the rendering.

---

*v9.52 — the chains state the framework as currently known. Unresolved material lives in the open-items register, not in the chains.*
