# RSM Framework Context

This repository contains the Recursive Structural Model (RSM) and its convergence with the Dao De Jing and physics. Read this file first to understand the framework before working on any RSM-related content.

## The :: Discipline

`::` denotes **imperfect structural correspondence** - two expressions from different registers point toward the same structural fact, neither captures it fully. 

- `::` forbids substitution, upgrade to identity, and congruence by default
- A `::` chain (A :: B :: C) claims all terms converge on one identified invariant
- The convergence of independent imperfect pointings is the evidence

**In prose**: Use "to me, that looks just like..." rather than "X is Y"

## Foundational Conditional

> If reality has no terminal resolution in either direction, then neither a minimum scale nor a maximum extent obtains.

This is the load-bearing premise. Everything follows from it.

## The Three Chains (v7.5 - Authoritative)

| Chain | Register | Location |
|-------|----------|----------|
| Just-Math | Derivation | `rsm/canonical/chains/v7.5/just_math_v7.5.md` |
| Just-Physics | Convergence | `rsm/canonical/chains/v7.5/just_physics_v7.5.md` |
| Just-DDJ | Convergence | `rsm/canonical/chains/v7.5/just_ddj_v7.5.md` |
| Parallax | Through-lines | `rsm/canonical/chains/v7.5/parallax_v7.5.md` |
| Signature Forcing | Exploratory | `rsm/canonical/chains/v7.5/signature_forcing_v0.1.md` |

**Note**: The signature_forcing document is machine-drafted (Claude/Fable 5) and requires independent verification before entering canon. It refines the gradient-to-orbit passage and introduces the Pₙ → Cₙ/Sₙ± correction.

## Status Tags

- **[derived]** - Established in math chain, proven
- **[candidate]** - Convergence register, not yet earned as :: claim
- **[falsifiable]** - Empirical prediction; if not observed, framework is wrong
- **[open]** - Not yet resolved

## Key v7.5 Closures

These were previously open, now derived:

1. **Orthogonality**: Independence-to-orthogonality gap is closed. The Euclidean inner product is generated, not imported.
2. **Two-signature structure**: j² = +1 (gradient, Lorentzian) and i² = −1 (orbit, Euclidean) are a derived division of labor.
3. **Forced traversal**: The unit circle is forced by spanning under standoff constraint.
4. **i² = −1 by elimination**: The orbital operator is fixed by closure and spanning alone.

## Key v7.5 Correction

**Pₙ splits into two objects:**
- **Cₙ** (center): Origin of the (a,b) plane; crossing of asymptotes; unoccupiable; orbit center
- **Sₙ±** (seats): G ∩ B; occupiable; where gradient and orbit meet

## 11 Through-Lines (Summary)

1. **No terminal resolution** - |0| and |1| excluded
2. **Center required but unoccupiable** - Pₙ ∉ Fₙ
3. **Recursion generates coordinate systems** - Pₙ → O₍ₙ₊₁₎
4. **Direct crossing forbidden; forced traversal** - Standoff √1ₙ; unit circle
5. **Two modes require third position** - +1 → +i → −1
6. **Orthogonality generated, not imported** - Derived metric
7. **Local accuracy, frame-relative geometry** - Each frame is 可道
8. **Recursion generates coordinates, not dimensions** - Each frame its own ℝ³
9. **Toroidal persistence under privileged gradients** - Forbidden center → circulation
10. **Energy as misalignment cost** - 無為 :: aligned persistence
11. **Parallax is the method** - No single probe sees the whole

## Open Items

- **Dimensional uniqueness** - S² embeds in ℝ⁴ and higher; not structurally excluded yet
- **Branching-at-any-gradient-position** - Under test, not derived with Pₙ rigor
- **Postulate Q** - Quadratic distinction measures (carried implicitly, not derived)
- **Postulate R** - γ = 1 by register agreement (internally motivated, still postulate)

## Core DDJ Correspondences (v7.5)

| DDJ | Structure | Status |
|-----|-----------|--------|
| 道 | The structural substrate | - |
| 常 | Frame-independent, parameter-free | derived |
| 可 | Frame-relative, parametric | derived |
| 非 | Same-origin, different-register | derived |
| ⽞ | Pₙ (paradox-condition) | derived |
| 牝 | Generative capacity | derived |
| ⽣ | Parturition with prior state (Pₙ → O₍ₙ₊₁₎) | derived |
| 相⽣ | Mutual parturition (有 ⇌ 無) | derived |
| 名 | i (orthogonal cut) | derived |
| 守中 | Bₙ (balance axis) | derived |
| 天地 | Gₙ (two-mode gradient) | derived |
| 反者道之動 | Four-position closure cycle | derived |

## Reference Locations

- **Canonical chains**: `rsm/canonical/chains/v7.5/`
- **Structured data**: `data/rsm/v7.5/`
- **Earlier RSM versions**: `rsm/canonical/rsm_v0988.md` and earlier
- **NotebookLM sources**: `sources/notebooklm/` (flat dump for analysis)
- **Research archive**: `research/archive/`

## Working with RSM Content

When analyzing or creating RSM content:

1. **Check status tags** - Don't treat [candidate] as [derived]
2. **Use :: discipline** - Never equate across registers
3. **v7.5 is authoritative** - Supersedes earlier versions where they conflict
4. **The conditional is load-bearing** - Everything traces back to "no terminal resolution"
